# 需求档案（热血三国复刻 · E:\Deepseekdb）

> **本文件是需求的唯一台账，每轮接到新提问后必须同步更新。**
>
> **记录规则**
> 1. 新增一轮时，在「逐轮明细」末尾追加一节，并在「总览」表加一行；状态只填
>    `已完成` / `部分完成（写明缺什么）` / `未做（写明原因）`。
> 2. **原文优先**：v20 起保留用户提问的**逐字原文**（不改写、不概括、不润色）。
>    v1–v19 的对话记录已不可完整回溯，只能依据 `DESIGN.md` 各章与工作记录归纳，
>    已逐条标注「归纳」——**若要补成原文，需要用户提供当时的输入**。
> 3. 每轮要记录**由需求引出的真 bug**（用户没提、但排查时发现的），
>    这类问题往往是需求背后的真实原因。
> 4. 遗留项必须写在最后一节「遗留与待用户确认」，不要留在正文里自生自灭。

---

## 总览

| 轮次 | 日期 | 需求数 | 主题 | 状态 |
|---|---|---|---|---|
| v1–v3 | 2026-08-31 | — | 立项：纯前端单机框架 + 数值系统接入 + 数值数据字典 | 已完成 |
| v4 | 2026-09-12 | 归纳 | 按原版交互架构对齐 | 已完成 |
| v5 | 2026-09-12 | 归纳 | 修主循环崩溃与存档静默失败、建三件套测试 | 已完成 |
| v5.1 | 2026-09-12 | 归纳 | 资源数值可见增长 | 已完成 |
| v6 | 2026-09-12 | 归纳 | 叙事层：历法天时 / 年号 / 史册 / 羁绊 / 奇遇 | 已完成 |
| v7 | 2026-09-12 | 归纳 | 守将加成 / 装备入战 / 首页改造 / 存档体系 | 已完成 |
| v8 | 2026-09-12 | 归纳 | 建筑功能体系（客栈招募等）+ 自动存档 | 已完成 |
| v9 | 2026-09-12 | 归纳 | 自动升级（开关默认关） | 已完成 |
| v10 | 2026-09-12 | 归纳 | 地块分类配色 + 地图观察框 12×12 | 已完成 |
| v11 | 2026-09-12 | 归纳 | 装备获取 / 将领资质五档 / 死属性复核 | 已完成 |
| v12 | 2026-09-12 | 归纳 | 图标手绘 SVG / 任务体系 / 材料 / 菜单重构 | 已完成 |
| v13 | 2026-09-12 | 归纳 | 材料体系 / 2.5D 场景 / 三方式出征 / 野外城池 | 已完成 |
| v14 | 2026-09-12 | 归纳 | 外城按城独立 / 州特产 / 名城岁贡 | 已完成 |
| v14.1 | 2026-09-12 | 归纳 | 将领 id 撞号等可玩性修复 | 已完成 |
| v15 | 2026-09-12 | 归纳 | 野地重建：线性加成 / 采集 / 掠夺占领 / 等级动态 | 已完成 |
| v16 | 2026-09-12 | 18 + 4 | 体验修复 18 项 + 资源方案 P2 四项 | 已完成 |
| v17 | 2026-09-12 | 归纳 | P0 四项（科技接线 / 存档瘦身 / 粮食后果 / 攻城链） | 已完成 |
| v18 | 2026-09-12 | 归纳 | 行军机制 + 界面重构 | 已完成 |
| v19 | 2026-09-12 | 10 | 体验修复十项与界面规范化 | 已完成 |
| v20 | 2026-09-12 | 13 | 地块几何重做（正方形网格）、将领分次呈现、弹窗翻页等 | 已完成 |
| v21 | 2026-09-12 | 5 | 伤兵归位、三层界面统一、字体层级、备注清理 | 已完成 |
| v22 | 2026-09-12 | 3 | 去下拉框、将领肖像、城池视图纯粹化 | 已完成 |
| v23 | 2026-09-12 | 5 | 已占地块经营、等级角标、官府宫殿、统计菜单 | 已完成 |
| v24 | 2026-09-12 | 9 | 地图精简、征收入官府、城外 40 格、兵营队列、队列入公文 | 已完成 |
| v25 | 2026-09-12 | 14 + 0 | 城墙环、商城背包全页、平板适配、图标美术、任务详情、信息分层、统计公文美术 | 已完成 |
| v26 | 2026-09-13 | 5 | 将领经验可操作、左头像右五维（新增速度维）、天时入顶栏、观察框 12×8、城内城外入顶栏 | 已完成 |
| v27 | 2026-09-13 | 9 | 去城池操作板块、公文精简、野地守军规则化、战报兵种损耗、**回合制文字战场**、去下拉框、图标与统计重做、两栏等高、1180×820 自适应 | 已完成 |
| v28 | 2026-09-13 | 9 | 数值诊断（距离/攻守/兵种阶梯）、建筑 12 级与满级专精、将领 12 席细条、民心民怨一行、军营队列与上限、批次购买使用、拆毁/移动按钮、募兵加速 | 已完成 |
| v73 | 2026-09-15 | 5 | 黄金闸门 / 高资质概率再降10倍 / 种田秘境（材料+资质链条）/ 将领页标题精简 / 建筑弹窗去顶图+底栏吸底 | 已完成 |

---

## 逐轮明细

### v1–v3 · 立项与数值接入（2026-08-31）

> 归纳（来源：`DESIGN.md` 第 1–11 章、`docs/_参考/数值系统检索报告.html`）

- 做《热血三国》的复刻版；**纯前端单机**（无后端、无数据库），localStorage 存档
- 数值必须来自检索报告（13 章数据字典），不许拍脑袋
- 交付物：8 个 JS 模块 + `index.html` + 存档

### v4 · 按原版交互架构对齐

> 归纳（来源：`DESIGN.md` 11.4）

- 参考原版的界面分区与操作路径（左侧信息区 / 中央场景 / 顶栏功能）
- 明确"只学交互设计，不照搬素材"

### v5 · 三个致命缺陷与测试体系

> 归纳（来源：`DESIGN.md` 11.6 / 11.7）

- 读秒不动 → 真因是 `main.js` 主循环崩溃（`$` 未定义）
- 存档静默失败 → 派生数据（500×500 地形）撑爆 localStorage 5MB
- 要求："测试必须能抓到这类错误" → 建立 smoke-test（逻辑层）+ 后来补 e2e（真实 DOM）

### v5.1–v11 · 从"能跑"到"像个游戏"

> 归纳（来源：`DESIGN.md` 第 12–17 章）

| 轮次 | 需求（归纳） |
|---|---|
| v5.1 | 资源数字要肉眼可见地增长（`U.numHTML` 千分位 + `/秒` 增速） |
| v6 | 加"能带来惊喜"的系统；羁绊只要正面、不显示在界面（纯后台） |
| v7 | 守将加成要真生效；装备要进战斗；首页要能选存档续玩 |
| v8 | 每个功能建筑都要有实际作用（客栈招募 / 招贤馆 / 仓库 / 市场 / 马厩） |
| v9 | 自动升级：按等级从低到高自动升级，资源不足暂停；**默认关闭** |
| v10 | 地块不要再全是棕色；地图不要 500×500 塞满画布（改 12×12 观察框） |
| v11 | 装备怎么获取；将领资质"差异要大、高资质概率低但更夸张"。**并质疑"装备加的体力好像没加到将领属性上"，要求复核同类问题** → 查出 5 处死属性 |

### v12–v15 · 视觉重制与玩法补齐

> 归纳（来源：`DESIGN.md` 第 18–22 章）

| 轮次 | 需求（归纳） |
|---|---|
| v12 | 图标不要"色块 + emoji"，要手绘 SVG；加任务系统；缺背包菜单；菜单去重；系统提示保留期 |
| v13 | 材料分系列分品阶；2.5D 等距场景；出征分侦查/掠夺/占领；加野外城池；建筑不显示升级时间；随机任务上限 5 |
| v14 | 外城资源地块要按城池独立；州特产与地理绑定；名城要有持续岁贡（修"占完即断供"） |
| v14.1 | 点第一行将领却变动第二行（id 撞号）；募兵报"参数错误"；将领详情入口；长列表分页 |
| v15 | 野地加成改每级线性；加采集；掠夺得资源/占领得地盘；野地等级要动态（衰减、驻军可保） |

### v16 · 体验修复 18 项 + 资源方案 P2 四项（归纳，来源 `DESIGN.md` 第 23 章）

> 归纳。原文仅有编号线索（#1–#18 + P2-8/P2-9/P2-10/P2-11），保留在 `DESIGN.md` 23.2–23.7。

要点：#1/#2/#3 城内布局浮空错位 → 改标准 2D 等距；#4 显示比例；#5 配色改正；
#6/#18 菜单与消息；#7 募兵只能选义兵；#8/#9 自动研究与任务排序；#10 黄金被仓库上限卡死；
#11 弹窗固定尺寸；#12 操作分区（危险操作单列）；#13 升级可看可取消；#15 铁匠铺打造按钮失效；
#16/#17 弹窗与操作规范；#14 兵种归属（器械走工匠作坊）；P2 征收黄金 / 科技改耗金 / 平原筑城。

### v17–v19 · 收敛与规范化

> 归纳（来源：`DESIGN.md` 第 25–27 章）

| 轮次 | 需求（归纳） |
|---|---|
| v17 | P0 四项：科技有效率 7/24 → 24/24；存档 55.9KB → 3.2KB；粮食要有后果；攻城伤害链 |
| v18 | 加行军队列（出征要先"到达"）；地块改平行四边形；城墙改窄带；商城分类分页；图标暗黑精细风 |
| v19 | 升级改为纯后台（施工期间功能照常可用）；侧栏与城池属性去重；弹窗尺寸规范；菜单按类型分组；城内建筑可移动/交换；城外可改建；仓库解除数量限制 |

### v20 · 13 项（**原文**）

> 原文保留。当轮最重要发现：**"地块全飘"是几何错误而非审美问题** ——
> 图标高 62px > 地块 48px，必然戳出格子。据此彻底废弃倾斜投影路线，城内/外/地图全部改正方形网格。

```
1，建筑的名字，等级写在图标里，不要在下边好几行，升级所需时间不显示
2，现在地块全飘起来，根本看不清，懂不懂简洁。UI这么差该怎么解决，你垃圾就去找懂行的skills。全是一个地块上边飘个图标，这就是你的审美？真给我搞个平行四边形，懂不懂适配，要人类的好看，不要你搞工程的美感。全错位，全错位，搞什么，我问你，搞什么。图标代地块
3，将领界面太局促了，没必要一下把信息全塞一个界面，分此次呈现
4，兵营根本无法选择兵种进行募兵，搞好
5，城池属性不要显示"可征人口"，不要显示每小时产出多少金（而且这个数根本不对）
6，背包放到上方菜单栏，跟商城相邻。背包下边的任务菜单和上方菜单栏的任务重复，仅保留菜单栏
7.地图的界面时，不要显示左侧的统计栏，只有在城池界面才显示
8.野地掠夺胜利没有收获，调整，并在战报里体现
9.弹窗界面避免使用下拉条，调整到适合大小，使用翻页
10.左侧资源显示中，名称，存量，产量都按末尾文字、个位数对齐，不要错位，难看
11.鼠标悬停在产量时，小弹窗显示其基础产量及各类加成/扣除数值
12.城池属性，不要税收（金/时）这个数据，去掉
13.地图现在还是一个菱形跨在界面上，找个好点的角度，让我看起来舒服点好吗
```

**当轮引出的真 bug**：3 处「重绘错对象」——面板开在弹窗里，控件却调 `refreshView()` 只重绘中央视图，
表现为"点了没反应"。

### v21 · 5 项（**原文**）

```
1.伤兵应该放在校场里，以及行军菜单里，不要放在设置里
2.尝试统一界面，大界面，弹窗界面，地图界面
3.统一风格
4.各字体适当处理，同级别界面的字体一致性要好，不同功能的字体大小颜色得当
5.当前界面上的备注型信息可以去掉，信息型信息可以保留呈现
```

**当轮判据（后续各轮沿用）**：**"游戏怎么运作 / 该怎么操作" = 备注型，删；
"当前事实是什么" = 信息型，留。**
**当轮引出的真 bug**：`.lord-meta` 后一段丢了选择器的孤儿 CSS；`.map-legend` 重复定义；
`e2e-test.js` 一处 `U` 未定义的越界引用导致整段测试中断。

### v22 · 3 项（**原文**）

```
1.避免下拉框，避免下拉框
2.将领详细界面，显示一张将领图片，联网检索人物网图实时适配后存储（在招募将领后进行）
3.🏯 新城池 [275,225] 官府Lv10
人口 18.2万/19.4万 · 驻军 9664 · 城防 292 · 城外 39/39块
就绪　·　以民心换金（民心 -2）
显示比例，州郡岁贡。这些去掉，找个合适的地方放，界面纯粹，只保留城池和地块
```

**说明**：需求 2 的"联网检索网图"在纯前端 + `file://` + localStorage 5MB 的约束下不可行
（跨域被拦 / base64 撑爆配额 / 冷门将领无图 + 版权）。经用户拍板改用
**史实名将 AI 生成专属头像 + 普通将领程序化 SVG 立绘**，存档只存一个 `portraitSeed` 整数。

### v23 · 5 项（**原文**）

```
1.对已占领的地块的处理，野地可以驻军，撤军，采集，放弃等。平地还可以建城，但是没有采集价值。城池占领后，可以打开该城池的城池界面。注意自己有多个城池的处理，各个城池可以单独管理
2.等级显示在图标内右上，仅数字即可
3.野外地块也是，内部右上显示等级数字
4.固定官方的4个地块位置，融合呈现为1个建筑，绘制宫殿状
5.左侧统计数字只显示当前城池情况，而不是所有城池的。可以设置一个"统计"菜单，显示所有城池的汇总情况
```

**当轮引出的真 bug**：地图只认"首城"（`playerCity()` 只返回 `cities[0]`）——
后果不只是点不到第二座城，而是**自己攻下的城会被当成 NPC**，点它弹出「出征」面板，等于可以打自己。

### v24 · 9 项（**原文**）

```
1.去掉地图的这些备注：
湖泊 · 粮
草原/沼泽 · 粮
森林 · 木
荒漠 · 石
山地 · 铁
平地（可筑城）
🏯 名城（红=都城　金=州城）
🏰 我方城池
已占野地
点击城池可出征 · 点击野地格可占领 · 用方向键或坐标框移动观察框
2.上下左右和坐标放在右下方，不要占界面一大行
3.把天下大势，观察框 12×12 格　·　全图 500×500　·　109 座名城。这些备注信息去掉
4.征收放在官府建筑内，普通城池可以征收5大资源，名城可以征收特殊高价值材料。不要放在左侧统计界面，太拥挤
5.名城的特产怎么收集？
6.去掉左侧这种统计信息：
🧱 城防
292 · 驻军 19664
🌾 城外地块
39 / 39 块
全境 2 城 39/72
7.城外地块补足40，缺1个很奇怪
8.每个兵营有单独的募兵队列，每座兵营只能有一个队列在执行募兵，别的需要排队等待，等级5，等级10分别解锁1个等待队列（也就是一个兵营最多3个募兵队列，并且只有最早那个队列执行募兵倒计时，完成后自动进行下一队列）
9.屏幕下方的建造队列实时播报，募兵队列，自动升级这些不要显示了，在公文里显示吧
```

**需求 5 的答复**（后续未再追问，视为已解决）：特产经三条途径收集 ——
① **州郡岁贡**（每现实日自动入府，无需操作）② **官府征收**（冷却 1 游戏小时）
③ **州治加成**（握有本州州城时产量 ×1.5，司隶以都城为限）。

**当轮引出的真 bug**：
- `tickOnce`（在线）与 `simulateBulk`（离线）**各有 1 份**队列推进循环，只改一处 → 队列齐步走。
- `waiting` 用"下标 > 0"判定错误，把**正在募兵**的那条标成"排队等待"（渲染探针抓到，单测漏过）。
- 征收只产黄金且数值失控（19 万人口一次 35 万金，冷却仅 30 现实秒），压掉税收与岁贡两条线。

### v25 · 14 项（**原文**）

```
0.把所有历史提问整理到一个md文档，并持续根据新提问更新
1.城墙在周边围起来，离建筑地块一定距离，全包围，显示为环形图标，点击进行操作，不要 城墙 Lv9 · 点击升级这种
2.商城，背包是弹窗，建议跟其他菜单统一，为全页面界面
3.显示比例放在设置里吧
4.总界面按平板电脑大小，各菜单模块的界面据此适配一下
5. 州郡岁贡放在特殊城池的官府吧，不要占左边屏幕空间
6.建筑图标比地块小，适配到仅比地块小一点点
7.对图标进行再优化，要美术，美术，还是美术
8.对非名城可以重命名
9.左上角统计，去掉这个：城池 2 城；头像搞帅点，游戏名称放在头像下方，不要占用行
10.菜单图标，任务和统计放一起。爵位放到君主里，不作为单独菜单
11.任务菜单，单个任务只显示名称，点击名称图标再弹窗一个固定大小和格式的任务介绍，包括任务背景，任务需求，任务奖励，放弃任务等
12.说了备注型信息去掉，像商城这种：当前黄金 69,940,753　·　单价 = 元宝价 × 100 金　·　分 材料 共 24 件
13.游戏型（玩家需要直接获知的）信息才保留，备注型信息和以悬窗显示，获知找合适的地方提供说明（触发式的），不要占用固定界面，本来就不大，全是这些无用信息
14.统计，公文的信息，不要搞什么数据座舱之类的看板，设计一下，要好看，美术，美术，还是美术。要符合游戏背景的美术！
```

**落地**：见 `DESIGN.md` 第 33 章。

**当轮引出的真 bug / 隐患**：
- 无新增功能性 bug（本轮以界面重构为主）；
- 删掉因改版成为**死代码**的 `ui.questCard`（任务卡片墙渲染器），否则结构审计会一直报；
- 修掉三条**测试自身的缺陷**：断言写死尺寸（`TILE_W === 88`）、
  断言匹配到注释里的词（`/显示比例/` 命中注释）、手写正则错字（`wv` 应为 `vw`）。

**需求 7「图标要美术」的落地尺度**（需用户确认是否满意）：
- 可验证的部分已做：图标占格 58% → 100%（需求 6）、全部图标加统一光照层（左上暖光/右下冷影 + 暗角）、
  地块加"脚下暖光池"、新增 11 枚顶栏线描图标、统计/公文改竹简卷轴美术。
- **无法自行确认的部分**：具体图形好不好看 —— 当前模型无法读图，需要用户看一眼定夺。

---

### v26 · 5 项（**原文**）

> 用户开头那句「就这样吧，继续：」是对上一轮的收尾确认 —— v25 不再回炉。

```
1.将领战斗可获得经验，也可以由商城购买的练兵经验获得，对这个进行调整，让经验可操作
2.将领头像放左边，右边放四维，四维里不要"合计"，不要"丹"，丹药加的属性直接加到基础上，嗑药的怎么就不基础了？"作用"不要计算值，直接介绍四维的功能就行，比如勇武，每点增加全军攻击1%。增加一维，成五维，速度，每点速度增加全军行军速度和战斗中速度1点，也就是速度是一个求和计算。
3.天时放在顶部菜单栏偏右边，在菜单，存档中间，不要在城池属性里了
4.地图地块显示为12*8吧，扩展一些。
5.城池的城内，城外图标放到顶部菜单栏，城池=城内，城外=城外
```

**落地**：见 `DESIGN.md` 第 34 章。

**当轮引出的真 bug / 隐患（四项，都是"看着有、其实没生效"）：**

1. **丹药被算了两次** —— `systems.useItem` 用永久丹药时已经 `g[attr] += 1` 写进基础，
   而 `genAttrs` 又把 `g.perm[attr]` 加了一遍。所以吃一颗丹药实际 +2，
   且详情表的「基础/装备丹/合计」三列加起来也对不上。这正是用户说的
   「丹药加的属性直接加到基础上，嗑药的怎么就不基础了？」—— 他的直觉是对的，是我这边重复计了。
2. **速度是个只显示不生效的死属性** —— `DATA.GEN_BASE.speed = 0`、装备表里没有任何 `spd`、
   套装只有 +2/+7/+12，于是全场将领速度几乎恒为 0；而且即便有值，它也**只**进战斗先手，
   对行军毫无作用。既然要把它升为第五维，就必须先让它有出身值、有成长、有消费点（行军 + 先手）。
3. **经验入口散落四处** —— 攻城 / 侦察 / 据点 / 采集各写一遍 `gen.exp += x` 再调 `checkLevelUp`，
   公式（`level*level*100`）也在三处各写一遍；玩家更是完全看不到自己涨了多少经验。
   统一成 `GAME.battle.gainExp()` + `GAME.expNeedOf()` 单一口径，并把经验写进战报与公文。
4. **e2e 骨架"绿着崩"** —— 删掉 `ui.setCitySub` 后主流程在 2065 行抛异常，
   而 `.catch` 直接 `finish()`，于是输出是「0 失败 + exit 0」，**后面 49 条断言根本没跑**。
   已改为异常必计失败 + 打印"本运行已中断"。（这与 v21 那次 `.lord-meta` 后丢选择器、
   e2e 越界引用中断整段是同一类问题，说明"崩了要显性失败"是测试骨架的硬要求。）

**顺带清理**：审计报出的 3 个不可达 case（`open-bag` / `open-marches` / `open-shop`）——
v25 把这三者改成顶栏视图后就再无触发点，留着会掩盖以后的新问题。
以及 `.sky-line` 三条样式随元素一起删除，不留死样式。

**已完成**。

---

### v27 · 9 项（**原文**）

```
1.去掉左侧栏“城池操作”这个板块，城池属性稍微适配一下，不要下拉进度条
2.公文里不要：公告 · 当前要务，不要：系统状态
3.各级别野地的兵种和数量划定一个范围，每24h根据规则变化，侦查可以获得准确数值，包括野地将领，兵种，数量，掠夺可得宝物，占领可采集资源，可获得宝物的类型
4.战报里详细列出兵种战斗的损耗
5.建立回合制文字战斗场景系统（抽象成一个网格地图，根据马匹速度和兵种速度，选择一个合理距离，假设攻打野地，距离为2000，攻打城池，距离为4000。回合制，速度高的兵种先行动，向对方移动该兵种速度（受将领加成）的距离，直至接触对方最前方的兵种，发动攻击，攻击值为兵种基础值+将领、科技等加成乘以兵种数量，杀伤效果为攻击值除以受攻击的部队的兵种生命值，注意，战斗属性均受双方将领加成。
6.任何有下拉框的地方都给我深思熟虑，可以可以去掉？超出的内容用什么方式呈现更好看
7.菜单图标太大了，一点不好看。统计菜单里的信息呈现更是恶心
8.为什么城池的 左侧统计栏，比右侧的城市的页面长
9.整体界面的像素改成那1180×820，最好可以随网页的百分百大小调整显示
```

**落地**：见 `DESIGN.md` 第 35 章。

**当轮引出的真 bug / 隐患（三项）：**

1. **需求 8 有明确答案，而且是一个 CSS 覆盖关系导致的**：
   旧布局里 `.view-box` 写死 530px 高、`.auth-side` 却允许到 606px
   ——需要**侧栏被允许比中央高 76px**，"左侧统计栏比右侧城池页面长"因此是必然结果。
   更隐蔽的是还有**第二份 `.main` 规则**（v18 时代的 `align-items: start`）叠在新规则后面，
   新规则没写的属性就一直是旧值 —— 两栏根本不会一起拉伸。修法是删掉旧那份、在唯一一份里显式写 `align-items: stretch`。
2. **将领选择其实一直是"下拉框"**：`.chips.gen-chips { max-height: 96px; overflow-y: auto }`
   ——96px 的滚动列表，要滚才能看到后面的将领，和下拉框的实际体验一模一样
   （还把滚动条挂进了"本不该有滚动条"的弹窗）。改成两列网格后一次呈现。
3. **单轮齐射没有溢出与上限，导致"回合制战场"只能跑 1 个回合**：
   按"攻击值 ÷ 生命值"一次结算，几千弓兵第一轮就能把整支守军吃掉，
   战场条带只剩一帧 —— 需求 5 要的"回合制"就没有内容可看。
   加了**溢出杀伤**（打完一支顺势打下一支）与**单轮三成上限**后，
   战斗自然拉成 3~8 回合，"速度高的先手、推进、接敌"才看得见。

**顺带清理**：`.stat-card` 一整套样式随卡片删除（不留死样式）；
`simulate` 拆成"分派 + 旧引擎 simulateDice"，两套引擎返回字段完全一致，可随时回退。

**已完成**。

---

### v28 · 9 项（**原文**）· 本轮

> 0.数值合理吗，距离，攻守之类
> 1.建筑等级上限调整 为12级，满级后增加一个合理的功能
> 2.将领界面直接一个界面显示，细条一下，12个栏位
> 3.城池属性的民心、民怨用一行显示为 xx/xx，节省空间
> 4.每个军营都有其募兵批次，而不是每个城池
> 5.募兵加一个上限按钮，根据可用人口和资源，直接产生一个上限募兵值。募兵队列显示在军营界面中，募兵操作在募兵界面中，这样方便看队列。
> 6.商城，背包物品加一个数值，可以批次购买和使用
> 7.拆毁建筑这个按钮放在建筑界面左下角，小一点。移动/交换按钮放在右下角，小一点
> 8.募兵队列不是有宝物可以加速吗，把功能加上

#### ⓿ 先量后改：数值到底合不合理

用户问的是"合不合理"，所以先用探针把数拿出来再动手。

- **距离**：2000/3000/4000 三档纵深的损兵实测 **59.8% / 71.8% / 59.8%** —— 非单调。
  原因是当时**双方都推进**，初始距离只改变"走路回合数"，交换比与距离无关。
  **结论：距离在旧实现里只是观赏，不是策略。**
- **攻守**：1.2 万打 6000 县城**全灭**，守方只损 26%；城防减伤最高 55%，
  而攻方唯一的对抗手段是剧情羁绊（默认 1.0）。**攻城器械（床弩/冲车/投石车）标了
  `craft:true`，但攻城加成只取 `story.siegeMult`（默认恒为 1）→ 器械对城池毫无额外伤害**，空承诺。
- **兵种**：人口约束下**最贵的兵最弱** —— 铁骑 1833 打不过长枪 5500
  （`atk/pop`：长枪 150 / 铁骑 117；HP 只决定"能撑多久"，杀伤由攻击决定）。

#### 落地

| # | 需求 | 落地 |
|---|---|---|
| 0 | 数值是否合理 | 修 4 处缺陷 + 重排兵种阶梯（见下） |
| 1 | 12 级 + 满级功能 | 上限 10→12（造价表按**末段增速外推**，不逐张手写）；**满级专精 16 条**，每条接真实消费点 |
| 2 | 将领 12 栏位细条 | 卡片墙（3 列 × 9/页）→ **一屏 12 席、一行一人**；空席写明"招贤馆需 N 级" |
| 3 | 民心/民怨一行 | 合并为「❤️ 民心 / 民怨　78 / 22」 |
| 4 | 每军营独立募兵批次 | 数据结构 v24 起就是 `queue.bIdx`；本批补上**军营面板列出本营队列**（此前点了军营只看到"可募兵种 12/18"） |
| 5 | 上限按钮 + 队列入军营界面 | 「上限」按钮（`GAME.maxTrainCount` 取人口与资源的短板）；军营面板列本营队列 + 队列位说明 |
| 6 | 批次购买/使用 | 商城「买 N 个 · 总价」；背包「用 N 个」；**买得起就买满**、**够用就停** |
| 7 | 拆毁左下 / 移动右下 | 两个带标题的 op-zone → 一行 `.bldg-foot`，两端分居、均改小 |
| 8 | 募兵加速宝物 | 新 `S.boostTrainQueue`（按「城 + 军营格」精确定位）；队列表加「加速」列 + 弹窗；韩信三篇补 `once` |

#### 需求 0 修掉的四处缺陷

1. **攻城器械生效**：内建攻城倍率 2.2 + 拆城（每类器械使守方减伤 ×0.8）
   + **提高单轮突破上限**（每类 +60%，最多 3 类）。
   最后这条是因为：杀伤还受单轮三成上限约束，攻方一旦饱和，再高的倍率也换不来更多杀伤 ——
   实测带 3000 投石车只比纯兵少损 16%，器械形同虚设。
2. **城头火力，让纵深真正有意义**：守城时守方远程的有效射程按城防值放大
   （县城 ≈1700 / 郡城 ≈2400 / 州城 ≈3450 / 都城覆盖全场），且**守城方全军固守**。
   攻守梯度因此单调：野地 46% → 县城 57% → 郡城 88% → 州城/都城攻方全灭。
3. **斥候不参战**（`nocombat`）：spd 3000（次快兵种 3 倍）却只有 20 射程，参战等于白送；
   且它被计入 `aStart` 会被误记为"阵亡"。
4. **高级兵种阶梯重建**：13 个兵种的攻/血校正，使「每人口攻击」与「每人口有效生命」
   双双不低于长枪兵基准（特化兵种除外：射程/坦度/后勤各按定位）。
   实测：铁骑 1833 现在能打赢长枪 5500。

#### 当轮另挖出的三个真问题

- **城墙 desc 承诺的三条效果里两条从未实现**（「守军防御 +10N%」「远程射程 +3N%」）——
  借此机会落地为"城头火力"。
- **体力类宝物没有"已满则拒绝"**：`useItem` 的 stamina 分支只做 `Math.min(100, …)` 却照样返回 `ok`，
  批量使用时会把整叠烧光（丹药、经验有上限判断，体力漏了）。
- **e2e 主流程中断过一次**（读 `undefined.elapsed`），靠上一轮加固的"崩了必须显性失败"才暴露。

---

### v29 · 16 项（**原文**）· 本轮

> 0.轻骑兵理论上可以一步到墙（兵种速度高，先手，基础速度+将领/科技等加成），考虑一个合理数值，使得骑兵类可以1-2步到墙。为兵种增加一个采集效率的属性，高级兵种的单个采集效率应当更高（并设计采集公式）。野地派驻的军队数量应当有数值上限，建议可派驻数量为野地等级乘以1万（仅限制派驻数量，一驻扎的军队，后续采集时不因野地等级变化，限制数量下降而自动回城）。
> 1.地图的界面空间大，怎么不整体放大一点，尽量满屏。
> 2.将领根据资质，等级上限不一样，我只说一点，其他你设计，天授级别的等级上限为240级。
> 3.导航条的位置固定，像首页，下页，页码这些。应该固定一个位置，上方界面如果相应物品/内容不足，留空即可，导航条动的话很别扭。导航/页码条可统一固定在屏幕下方一个合适位置显示。
> 4.背包还是有下来的导航条，合理设计空间，分类，搞页码，不要下拉
> 5.设置里的"显示比例"的比例选项，跟时间倍率的图标统一一下好吧，反正都是百分数。自动升级和自动研究放在同一行，再加上自动出征，可以设定将领，军队数量，目标（野地，城池），目标等级，出征类型，自动出征频率等。以便将领自动攻击周围地块，实现经验获取。因此，建议把自动搞成单独菜单，放"设置"旁边，而菜单栏的"存档和新游戏"在设置里有，就不要留在顶部菜单栏了
> 6.公文里不要队列这种信息，保留战报和消息就行
> 7.统计菜单里，全境汇总应该左右分列，同一类型的信息在同侧。不然左右看很费眼睛
> 8.左侧的统计界面。资源部分和驻军部分都应该随着选择的城市的不同而显示当前城池的信息。
> 9.名城在城池名字后边提示，比如洛阳[都城]，寿春[州城]
> 10.官府可对非名城进行重命名，该操作将命名更新同步给所有引用城池名称的界面（注意引用逻辑）
> 11.将领的体力作为一维，对军队生命进行加成，体力随等级增长，受到资质，内政的影响。公式看有没有现成的，或者规划一个合理公式。装备属性不要单独列，作为次要维度。攻击和防御直接加成到各兵种。攻防也规划合理的对冲公式，使得数值均衡，互相制约
> 12.铁匠铺目前的打造界面是有问题的，整一个像商城那样的弹窗界面
> 13.工匠作坊应该如同兵营，有独立的制造器械队列
> 14.商城，背包物品的图标整好看的，当前图标可以再设计一下，左侧物品贴图，右侧物品介绍，下方左侧是购买的数量输入框（带加减号）和右侧点击购买按钮
> 15.招贤馆不在保留名录，因为菜单栏已经有将领菜单了，去掉相关引用
> 16.将领界面，上方以3x2显示将领及其基本信息

#### ⓿ 先量后改（延续 v28 的方法论）

| 诊断项 | 实测（改前） | 结论 |
|---|---|---|
| 骑兵到墙 | 轻骑 野地 3 步 / 城池 5 步 | "机动突袭"在战场上完全看不出来 |
| Lv240 可达性 | 累计需 **4.6 亿**经验，而全游戏最大经验道具 30 万 | 240 级上限写了也到不了 —— 又一个"看着有、其实没生效" |
| 采集 | 所有兵种都是 `basePerHour × 人数` | 兵种差异在采集上为零 |
| 攻防 | 防御是"放大生命"，攻击是独立乘算 | 两者之间**没有制衡关系**，堆防就是血厚，与攻击力无关 |
| 野地驻军 | 无上限 | 可以把全城兵力倒进一块地 |

#### 落地

- **需求 0a 机动**：新增 `T.MARCH_UNIT = 2.4`（每 1 点兵种速度 = 每回合推进 2.4 丈）。
  轻骑 2400/回合 → 野地 1 步、城池 2 步；虎豹骑/突骑兵野地 1 步；
  西凉铁骑/铁骑野地 2 步；长枪/刀盾 3 步；器械 6~11 步（靠射程先开火）。
  `T.stepsToWall(spd, range, kind)` 是全站唯一的"几步到墙"取值口。
- **需求 0b 采集效率**：每个兵种新增 `gather`（民夫 2 / 义兵 3 / 枪盾 4 / 弓 5 /
  轻骑 6 / 铁骑 9 / 虎豹·西凉铁骑 10 / 大象 12 / 器械 2 / 斥候 1）。
  收成 = **采集力** × (1 + 野地等级×0.35) × 时长，采集力 = Σ(数量 × 效率)，封顶 `powerCap = 30000`。
  旧档只记人数时按民夫基准折算，不断档。
- **需求 0c 驻军上限**：`等级 × 10000`，**只在派驻时校验**；掉级不把已驻军队赶回城。
- **需求 1 地图满屏**：观察框从写死 12×8 改为**随窗口自适应**，
  格距上限 68 → 96，目标格距 88；1440 宽下画布从 816×544 变为约 1044×696。
- **需求 2 资质上限**：凡品 60 / 良材 100 / 英杰 140 / 名世 180 / 天授 240。
  **同时重规划经验曲线**：Lv ≤ 30 与原版 `等级²×100` 逐点一致，
  Lv > 30 线性放缓（Lv240 单级 20.3 万、累计 3180 万 ≈ 107 份治军之道）。
  否则 240 级是一句空话。
- **需求 3/4 固定导航条**：新增 `#bottom-bar`（固定高 46px，挂在 `.main` 之外）。
  `ui.pagerHTML` 改为把分页条**登记到 `ui._bottom`**、由 `renderView`/`repaintView` 收尾时统一落盘；
  内容区不再输出分页 HTML（返回空串）。背包四页（装备/材料/图纸/宝物）全部按格/按行分页。
- **需求 5 自动化**：设置里三行点选控件统一为 `ui.chips`（时间倍率/显示比例/税率同款）；
  新增顶栏「自动」菜单与 `ui.autoHTML()`，自动出征参数六行
  （将领 / 兵力 / 目标类型 / 目标等级 / 出征类型 / 频率）；
  顶栏移除「存档 / 新游戏」（设置里本来就有）。
  自动出征红线写死在实现里：只派空闲将、体力不足不出发、器械/斥候/辎重不编入、只从当前城取兵。
- **需求 6 公文**：队列整段撤出，只留战报与消息；队列总览迁到**官府弹窗 · 在办事项**。
- **需求 7 统计**：全境汇总 `lgLeft` / `lgRight` 两栏 + `.lg-cols` 网格，
  左栏"我据有什么"（疆域/军民/在外）、右栏"我积累了什么"（府库/君主/满级专精）。
- **需求 8 侧栏随城**：新增 `GAME.prodBasePerHourOf(city)` / `GAME.cityProdPerSec(city)` /
  `GAME.prodBreakdown(r, city)`；侧栏「/秒」改取**本城产量**，存量仍标为全境府库，
  驻军栏标题带城名。
- **需求 9 名城标注**：`GAME.cityTierName / GAME.cityLabel / ui.cityLabelHTML`，
  自建城不标（"自建城"三个字没有信息量）。
- **需求 10 重命名**：入口常驻官府（不能改时置灰并写明原因，而不是藏起来）；
  全站城池名一律走上面那个取值口，改完 `refreshAll` 即同步。
- **需求 11 体力第六维 + 攻防对冲**：
  体力上限 = `100 + (等级−1) × 资质成长 × 1.0 × (1 + 内政/1000)`；
  生命加成 = `hpCap × 体力 / (体力 + hpK)`（渐近 +80%，不硬顶）；
  装备不再是独立一列，攻击/防御并入兵种，生命只按装备自身数值折算次要加成。
  攻防对冲 `2A/(A+D)`：攻防相当 ×1.00、纯攻上限 ×2.00、攻不破防趋近 0。
  防御从"放大生命"改为独立一维（`T.perDef`），不再与生命重复计算。
- **需求 12 铁匠铺**：改 `openShell` 固定尺寸弹窗（lg），品质页签 + 物品行，
  材料逐项列出并**标出出产州**（自己据有则标"已据"）。
- **需求 13 工匠作坊队列**：队列条目加 `kind`（`train` / `craft`），
  `trainQueueKey` 含 kind → 军营组与作坊组天然分开；
  `trainQueuesOf / trainSlotsLeft` 加 kind 参数，作坊队列位与军营同一阶梯；
  器械不再要求军营等级（`canTrain` 对 `craft` 跳过 `junying`）；
  旧档迁移时把器械队列从军营迁到作坊。
- **需求 14 物品行**：`ui.itemRow`（左贴图 / 右介绍 / 下：数量 + 操作）+
  `ui.qtyInput`（数量输入框，带 −/＋/最多）+ `ui.itemArt`（品阶底纹承台 + 品阶珠）。
  商城改为两列 8 行/页、背包宝物页同版式；数量以**本行输入框**为准。
- **需求 15 招贤馆**：去掉将领名录（与顶栏「将领」菜单重复），
  改为只讲房间数 / 满级专精 / 缺房怎么办。
- **需求 16 将领界面**：上方 **3×2 卡片墙**（每页 6 席，横向卡：左肖像、中身份与六维、右状态），
  下方为**选中将领的档案**（六维表 + 经验/体力/忠诚/装备/守将效果 + 操作）；点卡片切换。

#### 本轮另挖出并修掉的问题

- **战役经验曲线与等级上限互相矛盾**：上限写 240，曲线却要 4.6 亿经验（最大道具 30 万/件）。
- **`ui.matSpecialtyStates` 曾是死函数**（只被旧打造面板用过）—— 新版接进打造行的材料来源标注。
- **同一件事三处三样**：时间倍率是 `<button>`、显示比例是 chip、税率又是 `<button>` —— 统一为 `ui.chips`。
- **`ui.renderBag` / `ui.renderShop` 绕过 `renderView`**：切页签后底部条还挂着上一个页签的页码，
  点下去会翻错列表。收敛为 `ui.repaintView`。

## 遗留与待用户确认

| # | 事项 | 状态 |
|---|---|---|
| 1 | v1–v19 的提问**原文**已无法回溯，现为归纳记录 | 待用户提供当时的输入才能补成原文 |
| 2 | 需求 7「图标要美术」为主观项，AI 侧只能做**可验证**的改进（统一光照、尺寸、细节），最终观感需用户确认 | 待确认 |
| 3 | 需求 9「头像搞帅点」：已改用程序化立绘（与将领同一套美术）；是否要换成 AI 生成需用户拍板 | 待确认 |
| 7 | v25 需求 10 的「菜单图标」按"每个菜单一项手绘线描图标"落地；若用户本意是其他样式（如平涂彩色图标）需说明 | 待确认 |
| 8 | 平板基准取 1024×768；若实际使用设备是 1180×820（iPad Air）或 1280×800，可再上调地块尺寸 | 待确认 |
| 9 | v26 需求 2：速度的**出身值**给 10（史实名将 20）+ 每级 +1。若希望速度只靠装备/坐骑拉开、不随等级成长，说一声即可改 | 待确认 |
| 10 | v26 需求 2：速度改为**线性求和**（原来战斗先手是 ×(1+spd/200) 的乘算）。高速度将领的相对收益因此下降，战场节奏会略变 | 待确认 |
| 11 | v27 需求 5：新战场默认启用（`state.settings.battleEngine='dice'` 可回退旧引擎）。**所有战斗结果都变了**，胜率与战损需实际打几场看是否合意 | 待确认 |
| 12 | v27 需求 5：单轮齐射对同一支部队**最多打散三成**（`T.VOLLEY_CAP`）—— 这是我为了"回合制有内容可看"加的上限，若希望更爽快可调到 1/2 | 待确认 |
| 13 | v27 需求 3：野地守军按等级区间 + 每现实日重掷；10 级野地平均规模约 4,000 人、八成有守将，若觉得过强/过弱可调 `DATA.WILD_DEFENSE` | 待确认 |
| 4 | 未开发系统：联盟楼（需联盟系统）、计谋 18 计、官职三职位（城守/主将/军师）、结拜/婚姻/迁都/称帝 | 未做 |
| 5 | 科技逐级消耗、装备强化成功率、计谋完整数值 —— 报告未公开，现为近似 | 未做 |
| 6 | 项目根目录遗留 `check-refs.js` / `test-core.js` / `test-ui.js` / `garden.html` | **已清理（v67）** |
| 14 | v28 需求 0：兵种阶梯按"每人口战力"重排过（13 个兵种的攻/血）。**资源效率**仍随兵种等级递减（义兵每千资源产出最高）—— 这是"便宜量大 vs 精锐"的取舍；若要改成"精锐更省资源"，需再调一轮造价表 | 待确认 |
| 15 | v28 需求 1：满级专精 16 条中，马厩/鸿胪寺/铁匠铺用的是「全城产量 +6%」（这三类暂无更贴切的消费点）。若想各有特色（如铁匠铺强化成功率）需先补对应系统 | 待确认 |
| 16 | v28 需求 5：`GAME.maxTrainCount` 只算**人口与四种资源**，未计入训练时间与队列位（按上限募兵会排很久）。若希望上限也考虑"队列够不够"需说明 | 待确认 |
| 17 | v28 需求 0：攻城现在"必须带器械"（纯兵打县城也全灭）。这是有意为之的攻守梯度；若觉得过严，可下调攻城倍率或提高攻方基础攻城加成 | 待确认 |
| 18 | v29 需求 0：机动系数 `T.MARCH_UNIT = 2.4` 是按"骑兵野地 1~2 步、步兵 3 步"反推的。若希望整体节奏更快/更慢，调这一个数即可（步数会同步变） | 待确认 |
| 19 | v29 需求 2：经验曲线 Lv>30 的放缓系数 `DATA.EXP_CURVE.grow = 0.006`（Lv240 累计 3180 万）。若觉得 240 级仍太远，把它调大即可 | 待确认 |
| 20 | v29 需求 0：采集力上限 `powerCap = 30000` 是"让 5000 民夫与旧版收益完全一致、同时留出精锐优势"这两个约束夹出来的值 | 待确认 |
| 21 | v29 需求 11：攻防对冲 `2A/(A+D)` 把"攻防相当时"钉在 ×1.00，所以**基线战损与 v28 同量级**；但它同时让"低攻打高防"的伤害趋近于 0（不是 0，但不破防）。若希望破防下限更高可加常数 | 待确认 |
| 22 | v29 需求 13：器械改由作坊制造后，**军营等级对器械完全不再要求**（原先要军营 8/9/10 级）。这是有意的语义修正，若想保留"军营也得出力"可加回 | 待确认 |
| 23 | v29 需求 16：将领席位仍是 12（招贤馆每级 +1 席），但**每页只显示 6 席**（3×2）。若希望一屏 12 席（3×4）说一声 | 待确认 |
| 24 | v29 需求 5：自动出征只在**在线主循环**里驱动（离线补算不跑）—— 离线一次补几百分钟会在瞬间把攒下的兵打光 | 待确认 |

### v30 · 1 项（**原文**）

> 贴图能不能参考人家这整好看一点，所有物品，头像，图标，建筑，野地，城池，装备之类。
> 还有城内城外贴图 （附原版截图 4 张：大地图/将领面板/城内视图/物品弹窗）

**落地**：画面区「暗框亮画面」改造 —— 地图（草皮纹理+密林+层叠山+波光湖+立体城池）、
城内外地块（草地绿+资源分色）、图标（调色板提亮+暖土承台）、材料图形分化修复、
装备死引用修复、立绘暖棕背景。docs/_史料/设计史.md 第 43 章有全部细节。

### v31 · 1 项（**原文**）

> 头像，物品，建筑，将领，之类的图标还是太丑了，要设计感

**落地**：造型差异化 + 材质立体感 + 品阶色彩跳变。
- 材料 24 按 6 系重画（铁/木/皮/筋/玉/布独立造型，品阶靠金环数量跳变）
- 兵种 18 按 4 大类重画（步兵/弓兵/骑兵/器械独立剪影 + 装备质感）
- 资源 6 全部重画（粮袋/木堆/石堆/矿坑+熔光/金币堆+金光/村民一家）
- 物品类型 11 全部重画（每类独立造型 + 工具/军备按 id 细分）
- 头像程序化立绘重做（眼有神/鼻有光/嘴有唇/肩甲有铜扣）
docs/_史料/设计史.md 第 44 章有全部细节。

### v32 · 1 项（**原文**）

> 图标依然难看，难看至极，建议采用网图

**落地**：改用开源游戏图标库 **game-icons**（4134 个，CC BY 3.0 免费可商用），
经国内镜像获取，共接入 **69 个矢量素材覆盖 81 项游戏元素**（建筑16/城外4/资源6/
地形8/兵种18/材料6系/装备12/物品11），零缺失。

渲染方式为**光照浮雕**：原始剪影路径 → 按语义分配斜向渐变 → `feSpecularLighting`
真实光源计算高光。材料额外做了 6 系 × 4 品阶共 24 组配色（品阶越高越亮越饱和）。

实现上采用**延迟绑定包装层 + 自动回退手绘**，零风险；手绘实现保留为兜底。
署名见 `docs/game-icons-CREDITS.md`。docs/_史料/设计史.md 第 45 章有全部细节。

**调研留痕**：同时试过 fluent-emoji-flat / noto / emojione 三套彩色 emoji 库，
30 个游戏元素命中率仅 21/21/15（game-icons 为 30/30）—— 它们缺的恰好全是
游戏专属件（枪兵/粮仓/铁匠铺/兵营/铠甲/头盔/长枪/战靴/腰带/项链）。

### v36 · 2 项（**原文**）

> 1.官府的贴图没变
> 2.自动化仅在自动菜单里，去掉设置里的重复自动化，相关引用也调整

**落地**：
1. **官府贴图** —— 根因是官府有专属渲染器 `ui.govPalaceHTML`（v23 跨 4 格宫殿），
   内联手写 SVG、绕过图标层，所以 v35 换素材时它没变。现加位图优先分支 +
   `ICON.bitmapSrc` 查询口 + `.gov-img` CSS；手写 SVG 退为兜底。
2. **自动化去重** —— 设置页的「🤖 自动化」卡整段撤出，开关与参数只在「自动」菜单
   一处；设置页只留倍率/比例/税率与存档（`.set-card` 5→4 张）。
   顺带修好两个测试基建缺陷（素材层在 smoke 里失明 4 个版本、断言锚在最终输出而非层级）。
docs/_史料/设计史.md 第 45 章有全部细节。

### v37 · 3 项（**原文**）

> 1.募兵界面的兵种卡片不需要显示募兵资源数量，改为悬停显示，这样有更多空间，图标可以放大一点
> 2.所有悬停显示的信息均在界面最上层，且显示在界面内
> 3.商城的物品图标，以及背包的宝物图标等显示异常，似乎大小不匹配

**落地**：
1. **募兵卡瘦身** —— 卡面 `.tstat` 3 行降 2 行（资源行移入悬停浮层，含消耗/人口/耗粮/耗时/锁定原因），
   图标 52px → **84px**。
2. **悬停浮层收敛为全站唯一一层** —— 内容源留原地但不再自己显示，显示统一交给
   body 末尾 `position:fixed` + `z-index:9000` 的 `#tip-layer`；
   `ui.tipPos` 纯函数落位（下方 → 空间不足翻上方 → 四面夹进视口）；
   事件委托驱动，滚动/改尺寸即收起。旧的三个 CSS 伪元素浮层全部退役。
3. **图标尺寸** —— 根因是 `.ia .ia-art svg` 只写标签名（全站唯一一处），
   位图 `<img>` 匹配不到规则 → 回退固有尺寸 1024px 被裁。
   改为 `.ia .ia-art .ico`；同类第二处 `.xc-row .xc-ico svg` 一并修正；
   新增"图标容器不得用 svg 标签选择器定尺寸"的静态门禁。
docs/_史料/设计史.md 第 46 章有全部细节（含一次误删 39 条断言的事故与恢复过程）。

### v38 · 4 项（**原文**）

> 1.点击自动升级还是会跳转到设置，解除路径，保留单一的自动化路径
> 2.将领装备栏做成一个人形，在相应部位设置装备栏
> 3.装备打造再区分一下套装，不然跟散件混在一起了，为套装增加套装属性，满足3，5，7，11件时增加额外效果
> 4.装备打造界面按商城界面改造，不要搞那么长下拉框

（另有一条概念提问「断言是什么意思」，已当面解释，非改动项。）

**落地**：
1. **自动化单路径** —— `doToggleAutoUpgrade` 末尾的 `setView('settings')` 改为原地刷新；
   自动化只剩「自动」菜单一条路径。
2. **人形装备栏** —— 12 部位按身体位置贴在人形剪影上（落点集中在 `ui.DOLL_POS`），
   右侧给六维汇总、装备贡献（带装−裸装）、套装进度（件数 + 四档 + 下一档）。
3. **套装** —— 门槛统一 3/5/7/11（原来倚天 13/15/16 不可达、名将除首档全死、
   **神武一档都拿不到**）；三套各补足到 12 件使四档全可达；套装件数值对齐同品质散件 ×1.12；
   新增 `GAME.setProgressOf` 作单一来源；打造界面套装/散件分栏。
4. **打造界面** —— 去 `one-col` 改商城两列网格；加类别筛选条（全部/套装/散件）+ 保留品质页签；
   底部列套装效果一览；套装件标签显示具体套名。
docs/_史料/设计史.md 第 47 章有全部细节（含三处自伤与修复）。

### v39 · 6 项（**原文**）

> 1.目前背景颜色太深了，整体调浅一点，或者整几个颜色模式，以护眼，适宜长时间玩耍为主
> 2.目前建筑的主色调都是黄色，区分度偏低，尝试按系列调整一下，比如农田偏黄，伐木场偏青绿，
>   采石场偏灰色色，铁矿场偏棕黑，其色块颜色也相应有所区分。
> 3.城池内建筑也是如此从建筑高度，建造材料，色调，档次在目前的基础上搞出一点区分度。
>   目前色块颜色还带花纹，太费眼睛了，能不能把底部色块去掉的？
> 4.公文好多下拉框，你懂的
> 5.商城统一整成4列商品展示，目前为两列，有点空
> 6.背包一页太多行了，跟商城同步搞成4行，图标大小也与商城靠拢

**落地**：
1. 四套主题（墨玉默认提亮一档 / 素绢 / 青竹 / 夜阑），设置页点选切换；
   机制是"结构色全变量化 + 三个 RGB 三元组"，JS 里零色值。
2. 城外资源地：地块色 + 图标色双向呼应（田黄/林青绿/石灰/矿棕黑，实测 4/4 唯一）；
   城内建筑按 **7 个系列**（官署/民居/仓廪/文教/军事/工商/驿传）分色，
   系列归属是数据字段（`series`）。
3. 城内建筑：明度铺开（实测极差 42）+ 色相 6/6 唯一；等级 → 长高（lv12 +20%）；
   **地块去掉花纹与逐格描边**，改纯色铺成一片地面（相邻格实测同色）。
4. 公文页三层框（卷轴壳 + 战报框 + 消息滚动框）全部去掉，改扁平页 + 列表行。
5. 商城 2 列 → 4 列，每页 16 个。
6. 背包 5 列 → 4 列、每页 16 格（4 行），图标 44 → 68px。

docs/_史料/设计史.md 第 48 章有全部细节。

**遗留待确认**：第 4 条"公文好多下拉框"——排查后确认全站**零个 `<select>`**，
按"三层框套在一起"理解并做了去框改造。若老板指的是别的，请指认具体位置。

### v41 · 6 项（**原文**）

> 1. 城外，地图的地块间隔稍微再区分开一点，不要显示粮，石之类的资源标识
> 2. 将领左侧显示姓名清单，右侧显示详细属性和装备图（不需要再整一个装备图的弹窗），
>    把完整档案的全部信息也整合过来，不需要再整个单独弹窗
> 3. 不要给在行军这个菜单名上产生数值（出征队列数？）
> 4. 有新的战报时，公文这个菜单的图标背景变成黄色闪烁，1.5 秒一次
> 5. 地图的野外地块，在图标中显示名称（如湖泊），图标右上显示等级。图标调整一下，
>    间隔大一点，湖泊就全部是水，整一些波纹。荒漠平一点，延绵一点。
>    草原平一点，草有个平均高度。平地不放图形，直接浅青绿色就行
> 0. 军营，烽火台，校场，招贤馆太灰了（与上面同批的另一条消息）

**落地**：
- 地块留缝：地图地块四边内缩 2px（缝里透出草皮纹理）；城外 `.tile-face` inset 2px；
  城内保持连成一片（老板只说城外与地图）
- 地图**去掉**粮/木/石/铁资源标识；野地改为**图标中显地形名 + 右上角显等级**
- 平地不放图形，底色改浅青绿；湖泊/荒漠/草原按老板描述重出素材（另附沼泽）
- 将领改「左姓名清单 + 右完整档案（含人形装备栏）」，**两个弹窗全撤**
- 行军菜单去掉数值徽标
- 公文有新战报 → 菜单图标闪黄（1.5s 一轮，进公文页即止）
- mil / edu 两族配色在 v40 已提亮（军营/烽火台/校场/招贤馆）

docs/_史料/设计史.md 第 49 章有全部细节。

---

> **补记说明（2026-09-15）**：v29–v72 未在本文件逐轮登记 —— 那一段的登记以
> `docs/AI工作备忘.md` 与 `docs/设计规范.md` 为准（逐轮明细见 `docs/_史料/设计史.md`）。
> 本轮（v73）起因老板新需求恢复逐轮登记。

### v73 · 5 项（**原文**）

> 1.限制黄金的获取，2.限制高资质将领的直接获取，概率再降10倍，3.官府可进入另外一个菜单，种田秘境（背景是个人种田空间，可种植装备打造、将领提升资质的植物，设计一个完整链条，将这个作为高级别材料的获取途径）
> 4.将领的界面顶部，怎么有个头像加一个飞机啊？不要搞（本城 2 / 11 席 · 全境 2 / 11 席）这些文字，直接将领加本城/全境切换按钮就行。
> 5.建筑点开界面，顶部的图标也不要留，还是旧图标。取消升级和关闭按钮要固定在底部，对不同建筑来说位置都要固定

**落地**：
- ① 黄金四进项全挂 `DATA.GOLD_GATE`（税收/俸禄/岁贡 ×0.3）+ 征收黄金 0.06 → 0.02；
  `prodBreakdown` 同步（展示与结算同口径）。
- ② 资质权重再 ÷10（累计：天授 ÷100 / 名世 ÷80 / 英杰 ÷60）；名将直取 0.30 → 0.03。
- ③ 种田秘境：官府「🌾 种田秘境」入口 + 6 块灵田 + 10 种作物（6 材料 / 4 灵草）；
  灵草为 `rank_up` 道具（凡→良→英→名→天），使用走 `GAME.rankUpUse`；生长与离线同口径（`tickFarm`）。
- ④ 将领页标题精简为「将领」+ 本城/全境 chips（🧑‍✈️ 与席位文字撤下）。
- ⑤ 四处建筑弹窗去顶部图标（`.dlg-ico` 退役）；`.bldg-foot` 吸底（真机实测底沿差 1px）。
- 测试：smoke 2015/0 · e2e 693/0（新增 v73 节：秘境全流程 + 底栏结构 + 两栏范围）。
- 细节：`docs/设计规范.md` §11 / §14；`docs/AI工作备忘.md` §16。

---

### v74 · 7 项（**原文**）

> 1.取消将领对人口上限的加成
> 2.规定大界面像素，推动浏览器边框时，界面内容不要随之移动。作为一个整体界面
> 3.将领的简介：良材 ★★ · 均衡 / 赵子龙 / Lv1 / 100　·　良材　·　空闲（新城池）　·　装备 3/12 / 可当一郡之任。等级上限 100。　每级成长 +2
> 改一下，太啰嗦：赵子龙 良材 ★★ 均衡 / 可当一郡之任 /（等级上限 100，每级属性成长 +2）作为鼠标悬停到"良材"的时候都备注显示
> 4. 每点作用也作为六维名称的鼠标悬停备注，六位新增加号按钮，不然商场或其他道具没法使用，就在原来每点作用这一列吧。属性跟右边装备栏固定对半空间分配，固定界面
> 5.不同类型（均衡，猛将等）将领，升级自动加点不同。根据资质，每级除了其成长之外，还有等于成长值的自由属性点（增加一行自由属性点，用于玩家自行决定加点，其他属性的加号可以选择道具或自由属性点进行手动增加，属性点只能手动增加，不能手动减少）
> 6.不要这个备注：带兵 6.9k / 人口上限 +6.9万 / 本城产量 +68% / 行军 / 战斗速度 +45
> 7.完善出征界面，

**落地**：
- ① 守将统率 ×1000 人口加成撤除；人口上限只由民房决定；`DATA.POP_PER_TONG` 下线。
- ② `#screen-game` 固定 1440×900（`--app-w/--app-h`）；视口断点全撤；弹窗固定 px + 极小窗口兜底。
- ③ 简介两行化；上限/成长进资质徽章悬停；Lv 进经验行；旧 Lv·状态·装备行撤除。
- ④ 每点作用进六维名称悬停；原列位改「加点」＋（六维都有）；属性/装备栏 1fr 1fr。
- ⑤ 类型化成长（复用 GEN_STYLES.mul 归一化）+ 自由属性点（每级 = 成长值；旧档补发；
  唯一出口 addFreePoint，只增不减；速度/体力走 spdAdd/staAdd）。
- ⑥ 「带兵/人口上限/本城产量/速度」四行备注块撤除。
- ⑦ 出征界面：兵力总览 + 战力估算 + 全带/清空。
- 测试：smoke 2037/0 · e2e 693/0（v73 基线 2015/693；v74 新增 22 条断言 + 翻转 20 余处旧守卫）。
- 细节：`docs/设计规范.md` §15；`docs/AI工作备忘.md` §17。

---

### v75 · 5 项（**原文**）

> 客栈的招募界面大一点，尽量所有候选都能在同一页，美人灯标识去掉，候选将领的成长+2/级这个备注也去掉，改成鼠标悬停在资质上时浮现备注，尽量单个将领一行显示。地下的各资质四维，成长和概率也不显示

**落地**：
- ① 弹窗新增 xxl 档（980×800，五档制）；客栈改三段式，候选区单滚动（旧版双滚动条收成一道）。
- ② 「美人」标（`.tag-beauty`）整条退役。
- ③ 「成长 +N/级」收进**资质徽章悬停**（`rankBadge.title`）；行内备注撤除。
- ④ 候选行两行 → **单行**（行高实测 36px；紧凑几何覆盖共用卡片基线 —— 基线在文件更后处，同权重后者胜）。
- ⑤ 资质一览（`rankTable` / `.rk-box`）整块退役 —— 四维 / 成长 / 概率不再显示（数值口径未动）。
- 容量实测（真机 1440×900）：**14 位**（客栈 Lv12 + 满级专精 2）与 **16 位**（县城上限）零溢出一页装下。
- 测试：smoke 2046/0 · e2e 695/0。
- 细节：`docs/设计规范.md` §11.1（五档）、§16；`docs/AI工作备忘.md` §18。

---

### v76 · 4 项（**原文**）

> 1.城池的左侧统计框没有到底，跟右边的建筑范围框宽度不一致。
> 2.底下有一个导航栏，可以考虑把地图的导航栏（上下左右，坐标之类）放到这个导航范围内
> 3.将领界面，左侧将领列表12个空格每页，平分空间吧，当城池超过12个将领，自动在底部导航栏产生翻页菜单
> 4.建筑的升级，拆除（1级，只能逐级拆除），移动/交换放在同一行上，并进行你的设计小巧思。建筑界面的关闭原因在弹窗界面的最底部好吗。这种备注去掉：招募将领（每级+1停留将领），市井传闻查名将坐标。拆毁可返还累计投入的 50%：粮食 58.2万 · 木材 359.2万 · 石料 184.3万 · 铁锭 75.6万

**落地**：
- ① 城池页左栏（`.lord-card` 大卡）铺满到底：flex 链 lord-card → 末段 → 驻军框 → 列表；
  真机实测大卡底 = 中央区底（846/846）。「宽度不一致」按"与右栏底线对齐"处理（若有别解请指认）。
- ② 地图导航（方向键 / 视野 / 坐标 / 前往等）迁入底部固定导航条；地图页只剩棋盘，
  画布随之放大（1248×624）。
- ③ 将领清单 12 席/页、12 行平分高度；超过 12 位将领时底部导航条出翻页（实测翻页正常）。
- ④ 建筑弹窗：升级 / 拆 1 级 / 移动·交换 三键同排（各带一行小字）；关闭单独钉弹窗最底
  （实测离底 6px）；建筑描述与返还长备注撤除；拆除改**逐级**（Lv2→Lv1→再拆移除；
  返还 = 本步投入的 50%，Lv1 拆 = 整座移除）。
- 测试：smoke 2047/0 · e2e 696/0。
- 细节：`docs/设计规范.md` §11.2 / §11.3（更新）、§17（新增）；`docs/AI工作备忘.md` §19。

---

### v77 · 7 项（**原文**；老板编号里无第 5 条）

> 1.将领招募字太密了，整成列表，表头比如将领，等级，资质，专长，统率……俸禄。
> 2.为将领设计俸禄体系，根据属性值？等级？或其他因素，设计维持将领所需月俸，经过7个游戏日结算1次（注意不要搞崩经济，但是将领越多，经济负担越重）
> 3.左侧统计界面，资源这里的备注：本城 新城池改成附属野地 下拉框（显示当前可选野地及坐标，以及进入按钮，进入后可以进入野地界面，进行相应派遣和采集操作等）。
> 4.城池属性右边的3个按钮以及相应的计算链就不要了（建筑信息，资源生产，附属野地）
> 6.君主界面分左右两半，左边为城池列表（加一个进入按钮，点击后跳转该城的城内界面），右边为两列的信息表，列出君主姓名（给一个改名按钮），爵位（给一个晋升按钮，鼠标悬停显示下一级爵位晋升条件），声望，人口总和，将领总和，状态。
> 7.丰富商场道具（符合时代背景和游戏背景），包括不限于各级宝箱，将领技能学习书（设计将领内功修炼体系，可增加将领特性（根据功法和等级），徭役令（增加同时建筑的队列，持续24h）
> 8.铁匠铺加入装备强化系统，装备可进行强化

**落地**：
- ① 客栈候选改**表格**：表头 = 将领 / 等级 / 资质 / 专长（均衡·猛将·智将·能臣）/ 统率 / 内政 /
  勇武 / 智谋 / **月俸** / 招募；一行一人（v75「单行」原则保持，成长备注仍在资质悬停里）。
- ② **月俸体系**：`DATA.GEN_SALARY`（底俸 120 + 等级×150 + 四维和×1.2，再乘资质系数 1~7）；
  **每 7 游戏日**结算一期（锚点 world.elapsed；离线跨期一次结清、上限 30 期）；
  从各将所在城扣；缺金记欠俸（不损忠诚）。将领越多/越强 → 负担越重。
- ③ 资源区「本城 · 城名」表头退役 → **附属野地下拉框**（地形 Lv（坐标））+「进入」→ 野地界面
  （加成 / 采集队 / 定位）；所选行在弹窗内高亮。
- ④ 城池属性右三按钮（建筑信息 / 资源生产 / 附属野地）整条退役：`openBldgInfo`、
  `openProdInfo`、开工率调整 UI（`after='workrate'`）、`doJumpCell` / `doSetWorkRate` 同步下线；
  `ui.openWilds` 保留（改由 ③ 进入）。
- ⑥ 君主面板改**左右分栏**：左 = 城池列表（每城「进入」直跳城内界面）；右 = 信息表
  （姓名+改名 / 爵位+晋升（悬停 = 声望·城池·黄金·珠宝条件与现状）/ 声望 / 人口总和 /
  将领总和 / 月俸支出 / 君主领兵 / 状态）。`rankBlock` 退役。
- ⑦ 商场新货：青铜 / 白银 / 鎏金**宝箱**（随机资源·黄金·珠宝·材料·图纸·徭役令）、
  四部**内功秘籍**（孙子兵法 / 太公六韬 / 五禽戏 / 越女剑经 —— 10 重、每重 +4 属性、
  特性：庙算 / 将略 / 养生 / 剑心；每将一门，换书＝转修）、**徭役令**（24h 建造队列 +3）。
- ⑧ **百炼强化**：`DATA.ENHANCE`（+10 封顶，每级全属性 +8%，成本随等级线性上升）；
  等级按**装备种**记（同种共享、新造的继承）；入口 = 铁匠铺底栏「⚒ 百炼强化」+ 装备详情跳转。
- 测试：smoke **2067/0** · e2e **700/0**（smoke 新增第 62 节 20 条 + e2e 新增 6 条；翻转 9 处旧守卫）。
- 细节：`docs/设计规范.md` §18（新增）；`docs/AI工作备忘.md` §20。

### v78 · 3 项（**原文**）

> 1.种地秘境的灵草作物时间逐级再拉长一点。种子只有通过将领其他活动获得，而不是花金币
> 2.一个隐藏设定，将领低资质通过蕴灵草等提升资质时，能比直接招募获得额外提升（你来衡量一下，不要太失衡）
> 3.装备不要“穿给谁”这种

**落地**：
- ① 灵草时长逐级翻倍 **12 / 24 / 48 / 96 游戏小时**（化龙参 36→48、天授果 48→96；
  蕴灵草 / 洗髓芝 本就 12/24 —— 全链自此"每档翻倍"，增量 +12/+24/+48）。
- ① 种子改制：**不再花黄金** —— 10 种作物的 `seed`（金价）字段退役、改 `seedItem`；
  新增 5 种种子（凡植种子 + 蕴灵 / 洗髓 / 化龙 / 天授种子，type=seed）；
  唯一来源 = 将领活动：**采集归来**（finishGather）与**出征获胜**（expedition）各掷
  `DATA.SEED_DROP`（p = base + perLv×lv；minLv 门槛：洗髓 3 / 化龙 6 / 天授 8；战事 ×0.85；
  唯一出口 `GAME.grantSeedDrop`）。种子不进宝物随机池、不直接使用（useItem 指路秘境）；
  背包新增「种子（种田秘境）」分类，行内按钮 =「去播种」。
- ② **隐藏设定「灵淬」**：灵草升档时四维**各 +新档 ascend**（良材 2 / 英杰 3 / 名世 5 /
  天授 8，全链 +18/维）—— 升上来的将领从此**比直接招募的同档更厚**；
  机制不显式提示（界面零字样，smoke 有守卫），数值集中在 `DATA.GEN_RANKS[].ascend`，
  消费唯一出口 `GAME.rankUpUse`，计数落 `g.ascend`。
  量级按"不要太失衡"取：全链 +18 ≈ 天授基础档（108~140）的 13~17%，≈ 天授练 1 级的产出。
- ③ 装备详情撤「穿给谁」名单（`equip-to` / `doEquipTo` 连根退役）；穿戴统一走将领侧
  两个既有出口：将领档案点部位（`openEqSlot`）/ 装备页选将后点装备；详情内加一句指路。
- 测试：smoke **2088/0** · e2e **701/0**（smoke 新增第 63 节 21 条 + §59 链条改种子制；e2e 3 处翻转）。
- 细节：`docs/设计规范.md` §19（新增）；`docs/AI工作备忘.md` §二十一。

### v79 · 4 项（**原文**）

> 1.爵位加成，可看下能加成哪些数据
> 2.主城加成，每人可有1个主城，在官府界面中设置，主城名称后有【主城】标识。为主城设置特定功能
> 3.神器加成（养成，主要依靠游戏时长和特殊活动逐渐提升），神器界面在君主菜单中
> 4.装备强化是针对单件装备都，同名装备搞个区分办法

**落地**：
- ① 爵位第二层回报：`DATA.RANK_BONUS`（22 级曲线，与 DATA.RANK 同序）——
  产/税 +1%/级 · 储 +2%/级 · Lv8 起同时建造 +1（Lv16 起 +2）· 每 3 级野地 +1 · 每 4 级席位 +1；
  消费**唯一汇总口** `GAME.cityBonusNum`（名城档位 + 爵位 + 主城 + 神器四层相加）。
  两个从未消费的死字段（shiyi 食邑 / recruit 招募）清掉；爵位表「食邑」列改「加成」，
  君主面板加「爵位加成」行（实测 爵位 12：产+12% 税+12% 储+24% 造+1 野+4 席+3）。
- ② 主城：`s.mainCityId`（新档为空，**官府里设**；首设免费、迁都 10 万金）；`DATA.MAIN_CITY.bonus`
  （驻跸：产 +15% 税 +10% 储 +30% 席位 +1 野地 +1，**只作用于本城**）；
  【主城】标识走 `ui.cityLabelHTML` 唯一出口（侧栏 / 城池下拉 `…【主城】 (x,y)` / 君主城池列表）。
- ③ 神器：`DATA.ARTIFACTS` 三件（传国玉玺 · 税收/声望获得 / 九州神鼎 · 产量/仓储 /
  河图洛书 · 将领经验/仓储）+ `DATA.ARTIFACT`（10 档供奉门槛 / +3 每游戏小时 /
  占城按档 20~500 / 爵位晋升 +300）；单池供奉值 `s.artifacts.pts`，等级=翻过的门槛；
  界面 `ui.openArtifacts` 挂**君主菜单**（实测 10 游戏时 → +30 供奉；+200 活动 → Lv2）。
- ④ 装备单件化：实例模型 `{ u, id, enh }`——u=件号、同名按件号排 **甲/乙/丙…序号**、
  enh=**该件**百炼等级；`GAME.addEquip` 入包唯一出口，拆解/装备/一键最优/卸下/解雇全按件；
  `migrateEquipModel` 老档迁移（id 串→实例、旧按种强化并入首件）。
  实测：`铁刀 +2·甲` vs `铁刀·乙` —— 穿甲装备攻 +90.5、穿乙 +78，各吃各的。
- 测试：smoke **2113/0** · e2e **706/0**（smoke 新增第 64 节 25 条 + 4 处旧守卫跟进；e2e 新增 5 条）。
- 细节：`docs/设计规范.md` §20；`docs/AI工作备忘.md` §二十二。


### v80 · 3 项（**原文**）

> 1.客栈招募界面，不要将领名称后边的"史实名将"这种标签，在将领界面显示就好。招募界面的表格行宽相对固定，根据字串长度搞个适合的固定表，在换人时框架不动，只有将领信息变动。
> 2.建筑的弹窗界面，升级，拆除（拆1级），移动/交换固定放在底部，关闭的上方
> 3.兵营招募界面，兵营招募·LV11这个不需要，募兵军营 城内第 46 格 · Lv11 · 队列位 3 这个也不需要。已解锁7/15种这种备注也不要。页面分成步兵，骑兵两个界面，翻页。数量目前是-10，+10这样设计，可以直接输入，上限这个按钮保留。目前选择数量后会跳回到页面顶端，要求取消跳转这个动作。

**落地**：
- ① 客栈：招募行去「史实名将」标（将领档案清单那枚保留）；表格改**固定列宽**
  （`table-layout: fixed` + `<colgroup>` 十列：188/58/168/66/58×4/88/140 = 942px），
  换批只动内容、框架零位移（真机实测两批列宽完全一致、十列零裁剪）。
- ② 建筑弹窗：操作三键与「关闭」合成**吸底操作区** `.bldg-bottom`（上排三键 / 下排关闭，
  几何沿用 v73 校准值）；真机加压实测（注入长文）底沿差恒定 −1px。
- ③ 兵营：常备 15 种按 `DATA.TROOPS[].cat` 拆「步兵（8）/ 骑兵（7）」两页（器械页不分页，
  `ui._trainTab` + `data-page` 翻页键）；撤除重复标题「兵营招募·LvN」、工位行
  （城内第 N 格 · LvN · 队列位 N）、「N / M 种」解锁计数（空态保留）；±10 退役 →
  数量直接输入（`#train-count`）、「上限」保留；`ui.openTroops` 重绘存/还两级滚动位 ——
  实测点「上限」滚动位 67/126 → 67/126 分毫不动（修复前 0/0）。
- 测试：smoke **2121/0** · e2e **718/0**（smoke 新增第 65 节 7 条 + 3 处旧守卫翻转 +
  2 处守卫升级；e2e 新增 12 条 + 1 处翻转）。
- 细节：`docs/设计规范.md` §11.2/§11.3/§16.4/§21；`docs/AI工作备忘.md` §二十三。


### v81 · 2 项（**原文**）

> 1.没有官职，左侧玩家角色这里，官职这行放玩家名称，现在的名称位置去掉
> 2.兵营招募这里，做成3页，第一页为募兵队列，步兵骑兵底下就不要募兵队列了

**落地**：
- ① 君主卡（左栏玩家角色）：头像旁旧名称列与「官职」行退役；君主名并入信息表
  首行（`.mrow-name`，金色标题）。顺带修掉布局陷阱：`.lord-meta` 原先 `width:100%`
  与头像同抢宽度（名称被挤成 21px 竖条、头像压到 45px）—— 改 flex 伸缩后
  头像恒 68×68、名称行 173px（真机实测）。
- ② 兵营招募三页制：`que 募兵队列（首页） / inf 步兵 / cav 骑兵`；
  队列从"页脚"升为独立页，步兵/骑兵页不再带队列（队列页也不带兵种卡与数量控件）；
  `ui._trainTab` 初始 'que'、main.js 三页白名单分发；工匠作坊（器械）维持原结构。
- 测试：smoke **2129/0** · e2e **723/0**（smoke 新增第 66 节 8 条 + 2 处守卫演进 +
  5 处切页修复；e2e 更新 4 处流程 + 新增 v81 段 6 条）。
- 细节：`docs/设计规范.md` §22；`docs/AI工作备忘.md` §二十四。


### v82 · 4 项（**原文**）

> 1.君主初始资质为最差，需要逐步升级
> 2.官府界面及君主界面等不要显示（自建城）这种文字，不需要显示原名 新城池；不需要显示附属野地/城外空地及其数量；不需要显示"本城"，城市名称居中，字体稍大即可
> 3.官府不需要征收物质这个功能去除
> 4.统一各级标题，文字，备注的字体格式，大小，粗细

**落地**：
- ① 君主开局「凡品」（最低档，`DATA.LORD_GEN.rankId='fan'`，lvCap 60）——靠种田秘境
  灵草逐档提升（凡→良→英→名→天，`rankUpUse` 唯一出口，升档另有隐藏加成）。
- ② 官府面板：「本城」标签 / 档位括注（自建城）/ 原名标注 / 「附属野地 / 城外空地」行
  全部退役；城名改 `.city-title` **居中 16px 金色**（操作按钮 `.city-sub` 居中一行）；
  改名弹窗不再回显原名（域层 origName 照记）；君主面板城池列表去档位括注。
- ③ 官府征收整段退役（域 / 界面 / 分发 / 状态 / 数据五处全清）；特产直读 specialtyOf / stateOfCity；
  「城外空地」显示与数值副本双退役（机制唯一出口 EXT_CAP_BY_LV + extCap）。
- ④ 字体三档纪律：字重只留 400/700/800（500/600/900 清零）；标题族（3 处）与分区标题族
  （13 处，收编六处漏网：q-det-sec / gp-sec / fsn-t / op-zone-t / ledger-sec / seal-h）与备注族
  （13 处）各一处共享定义；重复同名规则合并（.inn-avatar / .ia.qN）。
- 顺手：battle.js / ui.js 两处「同段重复语句」收编（v67 基线带来的；战报未读计数修复为 +1）。
- 测试：smoke **2137/0** · e2e **724/0**（smoke 翻转/跟进 8 处 + 新增第 67 节 12 条；
  e2e 重整征收流程 + 新增 v82 段 4 条）。
- 细节：`docs/设计规范.md` §23；`docs/AI工作备忘.md` §二十五。
