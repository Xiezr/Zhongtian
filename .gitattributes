# .gitattributes —— 严禁 git 改写文件字节
#
# 背景：本项目做过 md5 落盘校验（.workbuddy/tools/audit/verify_v66_edits.py），
# 破坏测试收尾也一律做 md5 比对还原。所以版本库必须保证
# 「提交什么字节，checkout 回来就是什么字节」。
#
# 实测：本仓 md 文件行尾是混合的（6794 个 CRLF + 4289 个 LF，4 个文件内部混行），
# js/html/py 则全为 LF。若让 git 做行尾规范化，首次提交就会把 md 批量改写，
# 落盘核验与破坏测试的 md5 比对会全线误报。
#
# 因此：全仓关闭文本转换。所有文件按二进制原样存取。
* -text
