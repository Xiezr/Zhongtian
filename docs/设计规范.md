# 设计规范（当前）

> 本文件 = **只看这一份就够了的当前规范**（原 `DESIGN.md` 第 1~10 章）。
> **本章号与原文一致**（1~10），所以别处写的「DESIGN.md 第 N 章」都能对上。
> 逐版本编年史（v2 → v67 的全部过程与踩坑）→ `docs/_史料/设计史.md`（同章号，从第 11 章起）。


> 复刻《热血三国》核心玩法，单机网页游戏，纯前端 HTML/CSS/JS，localStorage 存档，双击即可游玩（file:// 协议，使用普通 `<script>` 而非 ES module，避免 CORS 限制）。

## 1. 总体架构

采用**全局命名空间 `GAME` + 多个普通 script 文件**，通过 `index.html` 加载。所有代码挂到 `window.GAME`，避免模块加载问题。

```
index.html      — 页面骨架 + 古典UI主题CSS（一次加载）
js/data.js      — 全部静态配置：资源、建筑、兵种、地形、任务、将领模板
js/state.js     — 游戏运行时状态、存档/读档（localStorage）、世界时间轮
js/domain.js    — 核心逻辑：资源结算、建造/升级、造兵、人口、将领任命
js/map.js       — 全国地图生成、地形渲染、城池/资源点标记、路径计算
js/battle.js    — 回合制战斗模拟（兵种克制、兵损、胜败、战报）
js/ui.js        — 各面板渲染（创建角色/城内/将领/军队/任务/统计/公文/信件/宝物/装备）
js/main.js      — 启动入口、导航路由、全局事件绑定、计时器驱动
```

## 2. 文件加载顺序（依赖顺序）

`data.js` → `state.js` → `domain.js` → `map.js` → `battle.js` → `ui.js` → `main.js`

## 3. 界面布局（复刻截图风格，暗金/木质古典）

```
┌────────────────────────────────────────────────────────────┐
│  顶部标签栏: 城内 | 城池 | 地图 | 战场 | 军队 | 将领 | 任务 | 统计   │
├───────────────┬────────────────────────────────────────────┤
│  左上: 君主卡  │                                            │
│  头像/声望/官职 │                                            │
│  ├ 资源产出   │         中央主视图                            │
│  ├ 将领       │     (城内建设/地图/列表，由当前tab决定)        │
│  ├ 军队       │                                            │
│  └ 城防       │                                            │
├───────────────┴────────────────────────────────────────────┤
│  底部: 综合 | 信件 | 报告 | 邮件 (信息流)  + 建造队列/敌军警报    │
└────────────────────────────────────────────────────────────┘
```

配色：深绿背景 `#1e2a1e`、木质框 `#6b3f1d`、暗金描边 `#c9a24b`、羊皮纸 `#efe6cf`、青蓝按钮 `#2f6b5a`、朱红强调 `#8a2b1f`。

## 4. 核心数据结构

### 4.1 资源
```js
RES = { grain, wood, stone, iron, gold, pop }  // 粮食/木材/石料/铁锭/金币/人口
```

### 4.2 建筑 BUILDINGS（城内空地格子建造，可升级）
每个建筑：`id, name, icon, desc, baseCost{build}, maxLevel, produce(res/sec per level), costPerLevel(exp), unlockLevel`

建筑列表（第一阶段核心）：
- `house` 民房 → 每级 +人口上限 & 人口增长
- `farm` 农田 → 每级 +粮食产出
- `forest` 伐木场 → 每级 +木材产出
- `quarry` 石料场 → 每级 +石料产出
- `mine` 铁矿场 → 每级 +铁锭产出
- `market` 市场 → 金币交易
- `barracks` 兵营 → 解锁造兵，每级加快造兵速度
- `wall` 城墙 → 每级 +城防
- `academy` 校场 → 每级 +统率加成/兵力上限

城内为二维网格 `grid[cols][rows]`（如 5x4=20 格），空地格可点击建造，已有建筑可点击进入升级/详情。

### 4.3 兵种 TROOPS
```js
// 兵种属性: 攻击atk/防御def/血量hp/速度spd/耗粮food/耗资cost/占用占位/解锁所需
TROOP = {
  minfu 民夫, yibing 义兵, binghou 兵侯, changqiang 长枪兵,
  daodun 刀盾兵, gongjian 弓箭兵, qingji 轻骑兵, tieji 铁骑兵,
  zhouche 辎重车, chuangnu 床弩, chongche 冲车, toudan 投石车,
  ...青州兵/藤甲兵/突骑兵/虎豹骑/西凉铁骑/南疆象兵(高阶段)
}
```
兵种循环克制：枪克骑、骑克弓、弓克枪，盾克远程，象兵克制近战等（做简化克制表 `ATK_MULT`）。

### 4.4 将领 GENERAL
```js
{
  id, name, avatar, level, exp,
  tong_unit 统率, neizheng 内政, yongwu 勇武, zhimou 智谋,
  attack, defense, speed, hp, stamina, energy,
  loyalty 忠诚, salary 俸禄, status 状态(空闲/出征)
}
```

### 4.5 地图 MAP
- 全国大地图 `grid[cols][rows]`，每个地块 `{ terrain, cityId?, resource?, level }`
- 地形：`plain 平原, hill 山丘, forest 森林, water 水, desert 沙漠, city 城`
- NPC 城池：`{ id, name, x, y, owner, level, defense, garrison{兵种:数量}, troops, loot }`
- 玩家占领城池后纳入 `state.cities[]`，可远程建设

### 4.6 任务 QUESTS（成长任务链）
参考截图：建造民房 → 奖励粮/木材/石料/铁锭/人口。任务链：`{id, title, desc, guide, target{type,count}, reward{res, ...}}`

## 5. 游戏循环 / 时间轮

- `state.time` 世界时间（游戏内分钟），`setInterval` 每 1s 现实时间 = 若干游戏分钟。
- 每次 tick：结算资源产出（所有城市建筑产出 → 资源累加）、人口增长、建造队列/造兵队列倒计时、军队行军到达。
- 存档：`localStorage['sanguo_save']`，自动每 10s 存档一次 + 手动按钮。
- 关键数值随时间推进，重新打开从存档恢复。

## 6. 战斗系统（回合制）

`battle.js`：
- 攻方 `armyAttack`（将领+兵种数量），守方 `garrison`（NPC城驻军）。
- 回合制，每回合按速度排序，计算双方伤害 = Σ(兵种数量 × atk × 克制倍率)；按防御/血量减员。
- 城防（城墙等级/驻军）影响守方加成。
- 胜负：一方兵力归零或兵力少于某比例撤退。
- 胜利 → 战报，目标城归属玩家（`city.owner='player'`），获得战利品 + 声望。
- 战报存入 `state.reports[]`（公文报告）。

## 7. 称霸 / 胜利条件

- 地图上预设若干 NPC 城池（州郡）。玩家占领全部 NPC 主城 或 声望达阈值 → 统一称霸，显示胜利界面。
- 提供「再战一局/继续游戏」。

## 8. 第一阶段 MVP 范围（核心循环主线）

1. **创建角色**：选头像(男女)、君主名、开始 → 生成 1 座玩家城 + 初始资源 + 1 名初始将领。
2. **城内建设**：城内 20 格，点击空地建建筑（民房/农田/伐木场/石料场/铁矿场/兵营/城墙/市场），升级、拆除。
3. **资源产出**：建筑按等级产资源；人口随民房增长。
4. **造兵**：兵营内选兵种造兵，消耗资源与人口，有队列与倒计时。
5. **将领**：将领列表、属性、任命守将/出征、经验升级。
6. **地图**：全国地图，可点击移动/查看，显示玩家城与 NPC 城。
7. **战斗**：选将领带兵出征攻打 NPC 城 → 回合战报 → 打赢占领，输则损失兵力。
8. **任务**：成长任务链引导（如"建造民房"），完成后领取奖励。
9. **统计/玩家信息** 面板（复刻截图）。
10. **存档/读档** + 胜利判定称霸。

## 9. 命名空间约定

所有暴露对象挂 `GAME`：
- `GAME.DATA`（data.js 配置）
- `GAME.state`（运行时状态）
- `GAME.save/load`（存档）
- `GAME.domain`（逻辑）
- `GAME.map`（地图模块）
- `GAME.battle`（战斗）
- `GAME.ui`（渲染）
- `GAME.main`（启动/路由）
- 事件用 `data-action` + 全局委托监听，按钮统一调用 `GAME.action(name, payload)`。

## 10. 复用 / 扩展

- 所有配置集中在 `data.js`，新增建筑/兵种只需加配置项即可被各系统识别。
- 后续阶段可扩展：联盟、在线多人（需后端）、装备/宝物、信件、统计图表等。

## 11. 弹窗规范（v68 考察后统一）

> 由来：老板 2026-09-14「点击建筑出来的弹窗还是不好看，要求尽量统一（除非功能多的建筑），
> 按钮位置合理，操作项的命名合理（官府那个操作入口叫征收，有点扯淡了）。
> 也考察一下其他弹窗界面，做到风格统一」。

### 11.1 骨架与尺寸
- 所有弹窗走 `ui.modalShell` / `ui.openShell` 三段式：`m-head`（标题栏）·`m-body`（内容区，滚动）·页脚（操作栏，固定）。
- 尺寸档位 `sm / md / lg / xl / xxl`（v75 增 xxl，客栈用 980×800）：按内容量挑档；**不许出现"弹窗内滚动条撑破"**（挑大了比挑小了好）。
- **弹窗不设顶部图标**（v73 老板：「建筑点开界面，顶部的图标也不要留，还是旧图标」）——
  城内 / 城外 × 正常 / 施工中 **四处建筑弹窗**的顶部图标块整体撤除；
  v72 的 `.dlg-ico` 尺寸盒随之退役（没有图标，就没有 1024px 固有尺寸撑爆的土壤）。
  历史：v72 曾用尺寸盒治「位图按 1024px 撑爆弹窗」（老板报障「官府升级中点击」）。

### 11.2 页脚与按钮位
- 四套页脚同规格：`.m-foot` / `.modal-foot` / `.panel-foot` / `.bldg-foot`（实线、同间距、flex 居中或两端）。
- 按钮惯例：**主操作在前**（`btn gold`）、关闭在后；纯关窗文案一律「关闭」。
- 「取消」只用于**中止一个动作**的确认框（取消建造 / 取消移动）；纯关闭不叫「取消」。
- 不可逆操作的确认框：主操作（`btn red`）+「取消」+ **必须逐项说清代价**。
- **建筑弹窗底栏吸底**（v73 老板：「取消升级和关闭按钮要固定在底部，对不同建筑来说位置都要固定」）：
  `.bldg-foot` 走 `position: sticky; bottom: -12px` + 左右出血（负边距 = `.inner-panel` 的 padding 12px）
  + 底部垫 22px —— 正文再长，底栏也钉在面板下沿（真机实测底沿差 1px，顶/中/底三档滚动全对齐），
  六处建筑弹窗（城内×2 / 城外×2 / 城墙×2）位置完全一致。
- **v76 更新**（老板：「建筑界面的关闭按钮在弹窗界面的最底部」）：操作键（升级 / 拆 1 级 /
  移动·交换）上移成同排三键后，底栏只剩「关闭」一枚：居中摆放 + 面板改纵向 flex（
  `.inner-panel:has(> .bldg-foot)`）+ `margin-top: auto` —— 内容再短，关闭也**钉在面板最下沿**
  （真机实测离底 6px；此前内容短时浮在中间，离底 284px）；内容超长时仍由 v73 的 sticky 接管。
- **v80 更新**（老板：「升级，拆除（拆1级），移动/交换固定放在底部，关闭的上方」）：
  操作三键与「关闭」合成**一块吸底操作区** `.bldg-bottom`（几何沿用 v73 校准值：出血 +
  `bottom: -12px` + 底垫 22px）——上排＝操作三键（各带小字），下排＝关闭；内容再长也钉在
  弹窗下沿（真机加压实测：注入长文后底沿差恒定 **−1px**，顶/中/底三档滚动不动）。
  `.bldg-bottom .bldg-foot` 让位为静态行（sticky / 出血 / 描边 / 底色收归块级）。

### 11.3 建筑详情弹窗统一骨架（城内 / 城外 / 城墙 · 动线：使用 → 建设 → 管理）

```
[头部]   「名称 · LvN」—— v73 起**不设顶部图标**；v76 起**不写建筑描述**（老板：「这种备注去掉：招募将领（每级+1停留将领），市井传闻查名将坐标」）
[底栏]   吸底（sticky）—— 滚动时钉在面板下沿，位置不随内容长短漂移
[信息区] attr 行 —— 各建筑特有（功能多的建筑允许差异化：军营队列、作坊箭塔…）
[功能行] op-zone「功能」：使用建筑（金色主按钮，进功能面板）
[吸底区] .bldg-bottom（v80）：上排＝操作三键同排（⬆ 升级 金·附费用 · ⛏ 拆 1 级 红·附降级去向 ·
           🔄 移动 / 交换 附用途 —— 每键一行小字说明后果，v76「设计小巧思」）；
           下排＝「关闭」一枚、居中 —— 内容再短也钉在面板最下沿（v73 sticky + v80 块级 flex 钉底）
```

- **施工中弹窗与正常态同构**（名称 · Lv→Lv + 描述；v73 起两态均无图标），危险操作（取消）进底栏。
- v76 起三键同排（升级 / 拆 1 级 / 移动·交换、等宽），关闭单独一行钉底 ——
  v28「拆毁左下、移动右下」的两端摆位随之退役（老板点名要"同一行"）。
- **v80：操作区与关闭合成吸底块 `.bldg-bottom`**（老板：「升级，拆除（拆1级），移动/交换
  固定放在底部，关闭的上方」）——三键在上排、关闭在下排，同块钉在弹窗下沿；
  城外/城墙/施工中等其余弹窗仍走 v76 的「底栏 + margin-top: auto」形态（未动）。
- **拆除 = 逐级**（v76 老板：「拆除（1级，只能逐级拆除）」）：`GAME.demolishAt` 每次只降 1 级、
  返还**本步投入**的 50%；拆到 **Lv1 再拆**才整座移除（腾出地块）。城外拆毁未动。

### 11.4 操作命名（`BLDG_FUNC`）
- 格式：**图标 + 用途短语**；多功能面板写「用途A · 用途B」。
- **名字必须覆盖面板的全部内容**（点名不副实就是误导）：
  官府面板含征调民力 / 本城特产 / 岁贡 / 改名 → 入口叫「官府事务」，不叫「征收」。
- 反面检查表：入口名 ⊂ 面板功能 时，改（校场·出征与伤兵 → 出征 · 伤兵；名录 → 将领名录）。
## 12. 任务列表规范（v69）

> 由来：老板 2026-09-14「已完成的任务自动浮动到最上方，右侧直接添加领取按钮」。

### 12.1 区块顺序（固定）

```
✅ 可领取奖励 → 已完成 → 随机任务 → 成长任务 · 进行中
```

- 「可领取奖励」块**自动出现 / 自动消失**：无可领取任务时整块隐藏（不留空壳）。
- **无需手动刷新**：主循环每秒比对可领取数（与导航徽标同一汇总口），一变就重绘任务面板 ——
  「达标即置顶」是真·自动，不是"切视图才生效"。
- 达标任务从原区块**浮**到顶块 —— **一处占位**：浮上去的项不再在原区块重复出现。

### 12.2 行内领取

- 顶块行右侧直接挂「领取」按钮（`btn gold sm`）—— **一步领取**，不必再进详情窗。
- 行本身仍可点（`data-action="quest-detail"`）：想看背景 / 需求 / 奖励明细照旧。
- 按钮**复用既有 action**：随机 → `claim-rand-quest`、成长 → `claim-quest`（不新造出口）。
- 非达标行**不带**领取按钮（避免误导），右侧仍显示 `当前 / 目标` 进度。

### 12.3 计数与空态

- 各区块的计数**只算本区块实际渲染的项**（浮上去的不重复计入）。
- 空态文案必须**指路**：`本批 N 项均已可领取 —— 见上方「可领取奖励」`。
## 13. 地理与坐标（v70）

> 由来：老板 2026-09-14 五条 —— 「增加一个角色将领（玩家角色本人，不可解雇，留可扩张框架）」·
> 「名城固定坐标 + 州郡县标识」·「野外城池满建筑（城内仓库 4、城外按官府等级数量表）」·
> 「城池主界面提供坐标（500×500）+ 一键随机 + 坐标切换」·「创建界面头像与将领不一致、出生地不 OK」。

### 13.1 州 · 郡 · 县（`GAME.regionOf`）
- **就近归属**（确定性、无随机）：县 = 最近的县城（65 座铺满全图）；郡 = 该县**本州内**最近的郡城；
  州 = 数据里写死的 `state`。"所辖范围"与就近认领是**同一判据**，不另画边界。
- 全称（`GAME.cityFullName`）：都城/州城 `州 · 城名`；郡城 `州 · 郡名`；县城 `州 · 郡 · 县名`。
  规范化只补后缀、不造新名（`junNameOf` / `countyNameOf`）；改名后的城用 `origName` 顶行政层。
- 野外城池标识 = `县名 · 城池名`（`GAME.fortLabelOf`）；地图名带上 `LvN · 县名`。
- 地图尺寸 500×500（`DATA.MAP_W/H`），坐标域 `0 ~ GAME.COORD_MAX(=499)`。

### 13.2 满配城池（系统城）
- **城内**（`DATA.CITY_PLAN.order`，v70 起）：官府 2×2 居中 · 军营 ×2 · **仓库 ×4** · 其余各 1 ·
  余格全民房（8×6=48 格；民房数随之为 27）。布局只与等级有关，同档两城逐格一致。
- **城外**（`DATA.EXT_PLAN_BY_LV` + `GAME.extPlanOf`，唯一出口）：
  `[农田, 伐木场, 采石场, 铁矿场]` 逐档**合计恰等于** `DATA.EXT_CAP_BY_LV`（满 = 一块不空、一块不多）；
  轮转铺格（农田起），长度即块数。设计口径：农田恒占 1/3 上下（粮是养兵主线），
  采石/铁矿从 2 长到 9（早期营建、中期装备）；13 级起每档 +4（四类各 +1）。

### 13.3 城池坐标与迁址（v70 · 老板需求 3/4；v71 显示层修正）
- 显示（v71 老板：「城池属性不要显示坐标和所在州，只显示城池命名即可」）：
  城池属性栏**只显示城池名**（短名 = 城名 + 档位标，`ui.cityLabelHTML(c, true)`）；
  坐标 / 官府 Lv 不再常显（**迁址弹窗内仍显示当前坐标**）。
  自建城保留 🎲/📍 两个操作入口，名城标「名城固定」。
- 坐标出口（v71 续 老板）：「即使只有一个城池，也保留下拉框，在这里可以显示州郡县坐标」——
  城池下拉框（`#city-switch-host`）取消「多城才渲染」：**单城也常驻**；
  选项文案 = 州郡县全称（`GAME.cityFullName`）+ 坐标（`GAME.coordText`）；
  下拉签名含城名 / 坐标 —— 改名与迁址后即刷新。
- 可迁 = **自建城**（`GAME.isMovableCity`）—— 攻占来的名城/州郡县城地望固定，不可迁。
- 判据 `GAME.canCityMoveTo(city,x,y)`（**唯一**，界面置灰与真执行同源）：界内 + 平原 + 无占用
  （我方城 / 系统城 / 野外城池 / 已占野地）。
- 执行 `GAME.moveCityTo`：旧格**归还地图**（走 `restoreCityTile` 同一出口 → 还原平原），
  新格置 `terrain='city'`，坐标落定并写日志。「替换原地块建筑」= 旧格不再是城池。
- 一键随机 `GAME.randomMoveCity`（`randomCityCoord` 可注入 rnd 供测试）：掷一处可迁平原并立即迁。

### 13.4 君主将领（v70 · 老板需求 1）
- 玩家角色本人 = 一名**真将领**（进 `state.generals`，装备 / 出征 / 守将 / 派遣 / 经验全功能），
  以 `isLord` 一位区分；恒为名单**末位**（`generals[0]` 仍是开局名将，索引口径不动）。
- 不可解雇（`dismissGeneral` 守卫）+ 不会因忠诚离去（两处离职判定守卫）——都读唯一出口
  `GAME.isLordGeneral`。
- 六维取资质区间**中值**（`DATA.LORD_GEN.rankId = ming`）——不掷骰：老档迁移可复算、两局可比。
- 头像 seed 与 `ruler.portraitSeed` **同源**（顶栏 / 将领页 / 创建界面同一张脸）。
- **扩张点**：`DATA.LORD_TRAITS`（君主特权清单）→ 将领档案里的「君主特权」块；
  普通将领返回空数组、整块不显示。加一条君主专属功能 = 表里加一行。

### 13.5 出生州（v70 · 老板需求 5）
- 创建界面「城池归属」= **随机 + 十三州**（`DATA.START_STATES`）；旧「北方/中原/江南」只是文案、已撤。
- `GAME.pickStartPos(州名, 种子)`（唯一出口）：落点 = 该州州治近旁 3~8 格环带（司隶以洛阳为锚），
  需过"不压系统城（±2）"与"`stateOfCity(落点) === 该州`"两道校验；同种子同落点。
- 出生点写进 `state.map.startPos`：地图生成的「出生圈强制平原」与据点安全半径都读它（旧档回退）。
- 创建界面头像改用**与将领同一套**头像池（`ui.avatarPool` / `assets/portraits/pool`），
  idx 即 `portraitSeed` → 创建后君主的脸与将领页一致（emoji 池仅兜底）。


## 14. 经济与成长闸门（v73）

> 由来：老板 2026-09-15 五条 —— 「限制黄金的获取 / 限制高资质将领的直接获取，
> 概率再降 10 倍 / 官府可进入种田秘境（种装备材料与资质灵草，作为高级别材料的获取途径）」。

### 14.1 黄金闸门（`DATA.GOLD_GATE`）
- 黄金四个进项各挂一处系数，**只读这张表**（调档只改一行的数字）：
  税收 ×`tax` · 爵位俸禄 ×`salary` · 州郡岁贡 ×`yield`（v73 口径均为 0.3）· 征收黄金份额 0.06 → 0.02。
- 出口清单（改的时候四处一起看）：`GAME.cityProdPerSec`（税收）· `GAME.productionPerSec`（俸禄）·
  `GAME.cityDailyYield`（岁贡 —— 展示与结算同一出口）· `GAME.prodBreakdown`（悬停分解，**必须同口径**，
  否则「分解各项之和 = 总产量」守卫红）。
- ⚠️ 早期经济是有意的紧：新档税收 ≈30 金/h vs 两将领俸禄 40 金/h，开局小幅净流出；
  靠升民房 / 征收 / 占城回正。这是「限制获取」的设计结果，不是 bug。

### 14.2 高资质概率（两轮累计）
- 客栈资质权重：天授 ÷100 · 名世 ÷80 · 英杰 ÷60（v66 的 ÷10/8/6 之上再各 ÷10）；
  客栈 1 级天授 ≈0.026%。低资质（凡品 50 / 良材 27）两轮都没动。
- 名将直取（客栈 Lv5+）：0.30 → 0.03。
- 高资质将领的主路从此是**秘境灵草养成**（下节），不是客栈直取。

### 14.3 种田秘境（`DATA.FARM` + `GAME.farm*`）
- 完整链条：**黄金买种 → 灵田播种 → 游戏时间生长 → 收获**
  - 材料作物（6 种）→ 3 阶打造主料（镔铁/檀木/犀革/蛟筋/羊脂玉/蜀锦），15% 出 4 阶
  - 灵草作物（4 种）→ 蕴灵草/洗髓芝/化龙参/天授果 → 将领资质逐档提升（`rank_up` 道具）
- 入口：官府面板「🌾 种田秘境」——**与「立即征收」同排**（独立成区实测让 1440×900 溢出 11px）。
- 生长口径：**游戏时间**（与建造/研究同一把尺）—— 在线主循环与离线补算共用 `GAME.tickFarm`；
  调时间倍率、挂机离线都有效。
- 数值全表化：加作物 = 加 `DATA.FARM.crops` 一行；种子价 / 时长只改本表。
- 灵草使用走唯一出口 `GAME.rankUpUse`：`from → to` 一一对应，档位不符或已达标都拒绝并说明；
  只改 `g.rank`，等级上限与每级成长随 `rankOf` 自动生效。
- 存档：`s.farm` 懒初始化（旧档缺失即补），不动 `SAVE_VERSION`。

### 14.4 将领页标题（v73 · 老板）
- 标题只留「将领」+ 本城/全境切换 chips：🧑‍✈️（头像+飞机）、「本城 N / M 席 · 全境 N / M 席」、
  名将计数一律撤下（席位口径仍在 ⓘ 说明里；数字去招贤馆面板看）。
- 「两栏范围分得开」的口径改由**列表内容**验证（e2e：别城将领只出现在全境栏）。

## 15. 固定画布与将领档案（v74）

### 15.1 大界面 = 固定像素画布（老板需求 2）
- 原话：「规定大界面像素，推动浏览器边框时，界面内容不要随之移动。作为一个整体界面」。
- 实现：`#screen-game` 固定 `--app-w × --app-h`（**1440×900**，定义在 `html` 上，
  改尺寸只改这两个数）；窗口更大 → 水平居中留边；窗口更小 → 页面滚动（内容不重排）。
- **全部视口断点已撤**（宽 900 / 1100 / 1300 / 860 / 820 与高 860 的 `@media` 一个不留）——
  它们都是"内容随窗口移动"的入口；v43 的固定侧栏 283px 保留。
- 弹窗尺寸改**固定 px**（不再 vw/vh 缩放）；仅留 `calc(100vw/vh − 20px)` 的极小窗口兜底。
- ⚠️ `body` 不再写 `min-height: 100vh`（高度随窗口走）与 `overflow-x: hidden`
  （会把窄窗口下的画布右半边**裁掉**、且滚不着）。

### 15.2 将领档案（老板需求 3 / 4 / 5 / 6）
- **简介两行**：`姓名 资质★ 类型（均衡/猛将…）` / `资质描述`；
  「等级上限 N，每级属性成长 +Y」进**资质徽章悬停**；Lv 进经验行（`LvN 经验 a/b`）；
  旧「Lv N/M · 状态 · 装备 n/12」行撤除（装备数在右栏标题里；非空闲状态挂小签）。
- **六维表**：每点作用进**名称悬停**（守将加成同进 title）；原「每点作用」列改 **＋ 加点**；
  属性与装备栏 **1fr / 1fr 对半分配**。
- **自由属性点**：升级每级发放 = 资质成长值（**旧档按已过等级一次性补发**）；
  表格末行常显「自由属性点 N」；分配走唯一出口 `GAME.addFreePoint`（**只增不减**）。
  速度/体力走独立加法位 `spdAdd` / `staAdd`（不污染基础值，装备贡献照旧可算）。
- **类型化成长**：`GAME.applyLevelGrowth` 的自动加点按 `DATA.GEN_STYLES.mul` 归一化分配
  （总和恒 = 4×成长值；均衡与旧行为**逐点一致**，猛将勇武最快 / 智将智谋 / 能臣内政）。
- **撤除**：「带兵 / 人口上限 / 本城产量 / 行军速度」四行备注块整块下线
  （人口上限那半条随需求 1 一起消失）。

### 15.3 人口与出征（老板需求 1 / 7）
- 人口上限**只**由民房（+ 满级专精）决定：守将统率 ×1000 的加成撤除，
  `DATA.POP_PER_TONG` 随之下线（零引用字段不留）。
- 出征界面：**兵力总览**（共派遣 N 兵 · 耗粮 X/时）+ **战力估算**
  （我方 vs 守军；复用 `story.troopPower`，守方吃 `INVASION.defDivisor` 城防系数；标注"估算"）
  + **全带 / 清空** 两键（只改输入框值，实时口径仍只有一个出口 `updateExpMarch`）。

## 16. 客栈招募界面（v75 · 老板）

> 原话：「客栈的招募界面大一点，尽量所有候选都能在同一页，美人灯标识去掉，
> 候选将领的成长+2/级这个备注也去掉，改成鼠标悬停在资质上时浮现备注，
> 尽量单个将领一行显示。地下的各资质四维，成长和概率也不显示」

### 16.1 大界面：尺寸五档（新增 xxl 980×800）
- 弹窗尺寸档从四档扩为五档 `sm / md / lg / xl / xxl`；xxl 给客栈用（980×800 固定 px + 极小窗口兜底）。
- 客栈改走三段式（`m-head` / `m-body` / `m-foot`）：标题与按钮固定，只有候选区滚动 ——
  旧版「列表 414px 上限 + 面板滚动」两道滚动条并存，这里收敛成**一处**。
- 容量（真机 1440×900 实测）：**14 位**（客栈 Lv12 + 满级专精 2）与 **16 位**（县城上限）
  零溢出一页装下；候选更多（州 / 都城）时只有候选区一条滚动条。

### 16.2 单行候选（行高 36px）与紧凑几何
- 候选行由两行改**单行**：头像 28px · 姓名（含 Lv）· 资质徽章 · 四维 · 价格 / 按钮，一行装下。
- ⚠️ 紧凑几何（`padding: 3px 10px`、`margin-bottom: 3px`）必须写在**共用卡片基线之后**：
  基线（`.q-card, …, .inn-card, … { padding: var(--sp-4) }`）在文件更后处，同权重**后者胜** ——
  写在前面的局部值会被无声覆盖（第一版行高被撑回 52px，实机测量才抓到）。

### 16.3 备注的去处：悬停而非删除
- 「成长 +N/级」不在行内出现，收进**资质徽章悬停**（`ui.rankBadge` 的 title = 资质描述 +
  「每级属性成长 +N。」）—— 徽章是全站唯一出口，客栈 / 将领清单 / 派遣 / 解雇一处改、四处受益。
- 「美人」标（`.tag-beauty`）整条退役（界面不再产出该标）；`.inn-info` 两行容器随版式退役。
- 资质一览（`ui.rankTable` / `.rk-box`）整块退役 —— 「各资质四维 / 成长 / 概率」不再显示；
  概率口径仍在 `GAME.rankWeights`（只做生成，不上界面）。

### 16.4 固定列宽表 + 去「史实名将」标（v80 · 老板）
- 原话：「招募界面的表格行宽相对固定，根据字串长度搞个适合的固定表，在换人时框架不动，
  只有将领信息变动」。
- `table-layout: fixed` + `<colgroup>` 十列（列宽表在 index.html `.inn-tbl col.c-*`）：
  将领 188 / 等级 58 / 资质 168 / 专长 66 / 四维各 58 / 月俸 88 / 招募 140（合计 942 = 容器实宽）；
  换批只动内容、框架零位移（真机实测两批列宽完全一致、十列零裁剪）。
- 招募行去「史实名将」标（原话：「不要将领名称后边的…在将领界面显示就好」）——
  标只在将领档案清单（`gcard-tag hero`）保留。

## 17. 布局与导航（v76）

### 17.1 城池页左栏「铺满到底」（老板：左侧统计框没有到底）
- 左栏是一口**大卡**（`.lord-card` 装着 君主头 + 城池属性段 + 资源段）——
  铺满链：`.auth-side`（flex 列）→ `.lord-card`（flex:1）→ 末段 `.side-block`（flex:1）
  → `.garrison-bar`（flex:1）→ `.gb-list`（撑满，`min-height: 132px` 兜底）。
- ⚠️ 选择器必须挂在 `.lord-card` 上：第一版写成 `.auth-side > .side-block` **压根匹配不到**
  （两段是 lord-card 的子级），真机测量才发现驻军框纹丝不动 —— 改选择器前先数命中数。
- 真机实测：大卡底 = 中央区底（846 / 846），末段底差 1px。

### 17.2 地图导航进底部导航条（v76）
- 老板：「底下有一个导航栏，可以考虑把地图的导航栏（上下左右，坐标之类）放到这个导航范围内」。
- 地图导航（◀▲▼▶ + 视野 / 点选 + 坐标框 + 前往 / 回主城 / 洛阳）从地图页浮标迁入 `.bottombar`
  （`ui.mapHTML` 登记到 `ui._bottom` → `paintBottom`）；地图页只剩棋盘。
- `.map-dock` 改静态排布；`.map-wrap` 撤 78px 浮标让位、`fitMapCell` 的 extraH 78 → 10 ——
  画布随之放大（真机实测 1248×624）。
- 历史：v24 收进右下浮标 → v45 浮标内水平居中 → **v76 底部固定导航条**。

### 17.3 将领清单 12 席/页 + 底栏翻页（v76）
- 老板：「12个空格每页，平分空间吧，当城池超过12个将领，自动在底部导航栏产生翻页菜单」。
- 每页固定 12 席（空席补满、席位号跨页连续）；`.gen-list > .gen-row { flex: 1 1 0 }` 平分高度
  （真机实测 12 行各 44px 等分）；超过 12 位将领时 `ui.pagerHTML('gen', …)` 落到底部导航条。
- 历史：v28 一屏 12 席 → v29 每页 6 席 → v45 取消分页整段滚动 → **v76 回归 12 席/页**。

## 18. 经济与养成（v77）

### 18.1 将领月俸（每 7 游戏日结算一期）
- 定价 =（底俸 120 + 等级×150 + 四维和×1.2）× 资质系数（凡 1 / 良 1.6 / 英 2.6 / 名 4.2 / 天 7）；
  唯一出口 `GAME.genSalaryOf`（展示与结算同源）。
- 结算锚点 = 游戏内时间（`world.elapsed`）；离线跨期一次结清（上限 30 期）；
  从**各将所在城**的府库扣；缺金只记欠俸讯息（**不损忠诚** —— v14.1 拍板）。
- 界面出口：客栈「月俸」列（悬停注明周期）、君主面板「月俸支出」行。
- 意图：将领越多、越强，负担越重；一两名将不痛，规模化的将领团才成为经济议题。

### 18.2 百炼强化（铁匠铺）
- 强化按**装备种**记等级（`s.forgeEnh`，+10 封顶），每级全属性 +8%；
  成本 = 打造基准 × 系数 ×（当前等级 + 1），随等级线性上升。
- 效果在 `genEquipBonus` 的「装备本身」一层乘入（套装加成不参与强化，避免叠上叠）。
- 入口恒在铁匠铺（底栏「⚒ 百炼强化」）；装备详情页有「前往强化」跳转。

### 18.3 内功（秘籍）
- 每将同时一门、10 重封顶；每重 +4 对应属性：
  庙算（智谋）/ 将略（统率）/ 养生（内政）/ 剑心（勇武）。
- 加成在 `GAME.genAttrs` 的 buff 段之后、派生之前并入（全军加成吃得到内功）。
- 档案显示一行「内功 · 功法 N 重」；悬停给出特性名与修习规则。换书 = 转修（旧功散去）。

### 18.4 附属野地的入口（下拉框破例 2/2）
- 资源区「附属野地」下拉框 = 野地清单（地形 Lv（坐标））+「进入」= 野地界面；
  所选行在野地弹窗内高亮。
- 全站 `<select>` 仅两处：城池切换 + 附属野地 —— 破例上账（smoke §35 点名守卫）。

### 18.5 三按钮退役（v77）
- 城池属性右侧的「建筑信息 / 资源生产 / 附属野地」三个图标按钮与各自的弹窗、
  计算链一并退役；开工率调整 UI 下线（机制与取值保留在 `state.workRate`，默认 100%）。
- 附属野地的界面（`ui.openWilds`）保留 —— 改由 18.4 的下拉框进入。

## 19. 种田秘境（v78 修订：种子制 · 灵草时序 · 灵淬）

### 19.1 种子经济（去黄金化）
- 种子**不售、不花黄金**：唯一来源 = 将领活动（采集归来 / 出征获胜），
  概率全在 `DATA.SEED_DROP`（p = base + perLv×lv；minLv 门槛：洗髓 3 / 化龙 6 / 天授 8；
  战事整体 ×0.85）；唯一出口 `GAME.grantSeedDrop`。
- 5 种种子：凡植种子（6 材料作物通用）+ 蕴灵 / 洗髓 / 化龙 / 天授种子（对应四档灵草）。
- 播种消耗 ×1；背包「种子（种田秘境）」分类，行内按钮「去播种」（不出现"使用"）。
- 种子不进宝物随机池；`useItem` 对种子一律回"去秘境播种"。

### 19.2 灵草时序（每档翻倍）
- 蕴灵草 12 / 洗髓芝 24 / 化龙参 48 / 天授果 96 游戏小时（增量 +12/+24/+48）。
- 材料作物维持 6 游戏小时不变。

### 19.3 隐藏设定「灵淬」（不写进任何提示文案）
- 灵草升档时，四维各 + `DATA.GEN_RANKS[档].ascend`（良 2 / 英 3 / 名 5 / 天 8）。
- 唯一消费点 `GAME.rankUpUse`；计数 `g.ascend`；调平衡只改 ascend 四个数。
- ⚠️ 这是**刻意隐藏**的机制：界面、悬停、提示一律不得出现相关字样（smoke §63 有守卫）。

### 19.4 装备穿戴：单一出口（v78）
- 穿戴统一在将领侧：将领档案点部位（`ui.openEqSlot`）或「装备」页选将后点装备。
- 装备详情（背包侧）只保留信息 / 百炼跳转 / 拆解；不再有「穿给谁」选人名单
  （`equip-to` / `doEquipTo` 已退役）。

## 20. 四层经营加成：名城 / 爵位 / 主城 / 神器（v79）

### 20.1 唯一汇总口 cityBonusNum
- 「本城经营加成」= 名城档位(perkNum) + 爵位(rankBonusNum) + 主城(mainCityBonusNum)
  + 神器(artifactBonusNum)，**四层只在 `GAME.cityBonusNum` 相加**。
- 消费点：产量/税收（cityProdPerSec）、仓储（storeCapOf）、同时建造（buildSlots）、
  将领席位（genSlotsOf）、附属野地上限（battle 占野地）。别处再拼 = 第二出口（禁止）。
- 可加项全集：prodPct / taxPct / storePct / buildSlot / genCap / wildCap
  （另有 repPct / genExpPct 两个非经营项，走 artifactBonusNum 直接消费）。

### 20.2 爵位加成曲线（DATA.RANK_BONUS）
- 产/税 +1%/级 · 储 +2%/级 · Lv8 起建造 +1、Lv16 起 +2 · 每 3 级野地 +1 · 每 4 级席位 +1。
- 与 DATA.RANK 同序同长（改档位要同步）；封顶：产/税 +21%、储 +42%、造 +2、野 +7、席 +5。
- ⚠️ 原 DATA.RANK 的 shiyi（食邑）/ recruit（招募）两个字段从无消费点（纯显示占位），
  v79 清掉 —— 爵位表「食邑」列改为「加成」列。

### 20.3 主城（DATA.MAIN_CITY）
- 每人 1 座（s.mainCityId）；**官府里设**（首设免费、迁都 100,000 金）。
- 标识【主城】只走 ui.cityLabelHTML（侧栏 / 下拉 / 君主列表同源）；
  城池下拉选项为纯文本追加「【主城】」（选项文本不是 HTML）。
- 驻跸加成只作用于本城：产 +15% / 税 +10% / 储 +30% / 席位 +1 / 野地 +1。

### 20.4 神器（DATA.ARTIFACTS / DATA.ARTIFACT）
- 三件共用一池供奉值 s.artifacts.pts：时长 +3/游戏小时（在线与离线两条时间链各推一次），
  活动入账（占城按档 20/40/80/200/500 · 爵位晋升 +300）。
- 等级 = 供奉值翻过 DATA.ARTIFACT.pts 的门槛数（10 档）；加成 = Σ 每件 per[key] × 等级。
- 界面在君主菜单（入口 open-artifacts → ui.openArtifacts）。

### 20.5 装备单件化（v79）
- 库存与穿戴存实例 `{ u, id, enh }`；`GAME.eqId / eqEnhOf / eqLabel / eqSerial / eqFind /
  eqGroupOf` 是**唯一取值口**（读旧 id 串也兼容 —— 老档与测试夹具照跑）。
- 同名区分办法：名字 + 强化 + 序号（`铁刀 +2·甲`）；序号在**同名群**内按件号排 甲乙丙…；
  只有一件时不显示序号。
- 强化**按件**：`GAME.enhance(件号)` 只升该件；拆解按件（在穿的件先卸下）；
  入包唯一出口 `GAME.addEquip`；老档迁移 `GAME.migrateEquipModel`（旧"按种"强化并入首件）。


## 21. v80：客栈固定表 / 建筑吸底操作区 / 兵营分页直输

### 21.1 客栈：固定列宽表（见 §16.4）
- `table-layout: fixed` + colgroup；列宽只在 CSS 按类声明（换批/换人不动框架）。

### 21.2 建筑弹窗：吸底操作区 `.bldg-bottom`（见 §11.2 / §11.3）
- 上排三键、下排关闭，同块 sticky 钉面板下沿；短内容 `margin-top: auto` 贴底。

### 21.3 兵营：步兵 / 骑兵两页 + 数量直输 + 重绘不跳顶（v80 · 老板）
- 原话：「页面分成步兵，骑兵两个界面，翻页」「数量目前是-10，+10这样设计，可以直接输入，
  上限这个按钮保留」「目前选择数量后会跳回到页面顶端，要求取消跳转这个动作」。
- 兵种数据加 `cat`（`inf` 步兵 / `cav` 骑兵）：常备 15 种拆两页（8 步 7 骑），
  器械页（工匠作坊）不分页；`ui._trainTab` 记当前页，「🗡 步兵 / 🐎 骑兵」两枚按钮翻页
  （选中页走 `btn gold`）。选中项不在本页时自动落到本页首位。
- 撤除三样：重复标题「⚔️ 兵营招募 · LvN」（弹窗标题已有一处）、所属工位行
  （「城内第 N 格 · LvN · 队列位 N」）、「N / M 种」解锁计数；无军营时的空态保留
  （按钮不可用要让人知道为什么）。
- 数量：±10 退役（`GAME.adjustTrainQty` 同步退休），直接输入（`#train-count`，
  input 事件实时同步 `ui._trainCount`，不重绘）；「上限」按钮保留。
- **重绘不跳顶**：`ui.openTroops` 重绘前存 `.inner-panel` 与 `.panel-body` 的 scrollTop、
  重绘后原样还回（仅当旧窗本来就是募兵面板）；真机实测点「上限」滚动位 67/126 →
  67/126 分毫不动（修复前为 0/0）。


## 22. v81：君主卡信息表 / 兵营三页制

### 22.1 君主卡（.lord-head）
- 结构：头像（68px，`flex: 0 0 auto`）+ 信息表（`.lord-meta`，`flex: 1 1 0`）。
  信息表首行 = 君主名（`.mrow-name`，金色标题）；后续行 = 声望 / 爵位。
- 站位规矩：**名称行只此一处**——旧头像旁名称列与「官职」行已退役（游戏里没有
  官职体系，界面与文案都不要再出现「官职」）。
- ⚠️ 布局陷阱：原先 `.lord-meta` 用 `width: 100%` 参与 flex，与头像同抢宽度 ——
  名称列被挤成 21px 竖条、头像压到 45px（flex-shrink 默认生效）。改 flex 伸缩后
  头像恒 68×68、名称行 173px（真机实测；颜色 `--gold-light`、16px 粗体）。

### 22.2 兵营招募三页制（ui.troopsHTML）
- 分页：`que 募兵队列（首页） / inf 步兵 / cav 骑兵`；`ui._trainTab` 记当前页，
  初次打开落在「募兵队列」；工匠作坊（器械）维持旧结构（无页签、底部队列）。
- 队列独占队列页：**步兵/骑兵页不再渲染队列块**；队列页无兵种卡、无数量控件。
- 增删页签 = 改三处：`tabsHtml` 按钮、`ids` 过滤（`isQueueTab` 分支）、main.js 白名单。


## 23. v82：文案外露与字体纪律（老板四条）

### 23.1 城名行（官府面板）
- 「本城」标签行 / 档位括注（自建城）/ 原名标注 / 「附属野地 / 城外空地」行全部退役；
  城名改 `.city-title`（居中 · 16px · 800 · 金）+ `.city-sub`（操作按钮居中一行）。
- 改名弹窗与君主改名弹窗不再回显原名 —— 域层 `origName` 照记（州治判定/战报仍按它对号）。

### 23.2 字重三档纪律（全站）
- **400 正文 / 700 强调（按钮·数值·名字）/ 800 标题**；500 / 600 / 900 已清零。
- 标题族 / 分区标题族 / 备注族各只有**一处共享定义**（index.html 文末两段 + .note 段）：
  - 标题外观：`.gold-heading, .m-title, .q-det-title`（16px / 800 / 金 / 字距 1px）
  - 分区标题：`.q-sec-t, .q-det-sec, .bag-sec, .gd-sec, .forge-q, .m-sec, .side-title, .wb-t,
    .gp-sec, .fsn-t, .op-zone-t, .ledger-sec, .seal-h`（13px / 800 / 金 / 字距 .5px）
  - 备注族：`.note, .ui-sub, .nt-info, .op-hint, .m-sub, .gb-empty, .q-empty, .gd-hint,
    .farm-sub, .auto-note, .create-save-note, .q-det-tip, .ds-empty`（12px / 常规 / 次要色 / 行高 1.65）
- 新增标题类名必须挂进对应共享规则，不在各自块里单写字号/字重（smoke §67 守卫盯着）。

### 23.3 征收退役（v82）
- 官府征收（`GAME.levy / levyPlan / levyReady` + `DATA.LEVY_*` + `city.lastLevy` +
  `do-levy` 分发 + 按钮冷却同步）整段退役；黄金进项收口为三口（税收 / 俸禄 / 岁贡）。
- 特产展示直读 `GAME.specialtyOf / GAME.stateOfCity`（岁贡与州治加成口径不变）。
- 「城外空地」显示与数值副本（老口径函数）双退役 —— 机制唯一出口 `DATA.EXT_CAP_BY_LV + GAME.extCap`。

### 23.4 君主开局资质（v82）
- `DATA.LORD_GEN.rankId = 'fan'`（凡品最低档，lvCap 60）—— 靠种田秘境资质灵草逐档提升
  （凡→良→英→名→天，`GAME.rankUpUse` 唯一出口；升档另有隐藏加成）。


## 24. v83：野地经验惩罚（每 12 级一个台阶）

### 24.1 口径
- 掠夺 / 占领**野地**时，把将领等级折算成「该吃满的野地等级」：
  `need = min(10, ceil(level / 12))`；野地 0 级按 1 级对待。
- 野地等级 ≥ 台阶 → **吃满**（系数 1，不设超额加成）；每低一档 ×0.65，地板 0.03。
- 只作用于野地出征结算口；城池 / 野外城池的歼灭经验口径不变。
- 数值全在 `DATA.EXP_PENALTY = { tier: 12, maxLv: 10, decay: 0.65, minMul: 0.03 }`（四个数）。
- 唯一出口：`GAME.battle.expTierOf / expPenaltyOf`（smoke §68 结构守卫盯着）。
- 战报与公文都要写明打折原因（`越级惩罚 ×0.65：Lv84 宜打 7 级野地`）——
  与「单场封顶」同一原则：不写会被当成算漏了。


## 25. v84：兵种卡去「拥有」行 / 步骑分类对调（老板两条）

### 25.1 兵种卡卡面（troop-card）
- 卡面 `.tstat` 只剩**一行**（血 / 攻 / 防 / 射 / 速）——「拥有：N」行整行退役（v84），
  `own` 读数一并退役。`.tcard-tip` 悬停浮层仍是成本 / 人口 / 耗粮 / 耗时 /
  **未解锁原因**的唯一承载处（卡面不再挂锁定红字）。
- ⚠️ 动卡面结构（增删任何一行）先回扫：smoke 有一条断言逐卡会计 `.tstat` 行数
  （v37 起 3→2→1，现为 **1**），新增行必须同步该判据（备忘 §二十七）。

### 25.2 步骑分类（DATA.TROOPS[].cat）
- `cat` 只决定**募兵分页归属**：`inf` → 步兵页 / `cav` → 骑兵页，**与战场定位无关**。
- v84 对调：**斥候**（侦察）cav → inf；**辎重车**（后勤货运）inf → cav。
- 消费方只有一处：`ui.troopsHTML` 的分页过滤；测试全是动态读值 —— 改分类不连坐。


## 26. v85：地图占满自适应 / 底部缩略地图 / 民房入口（老板四条）

### 26.1 地图观察框：搜索式自适应（fitMapCell）
- 目标：**占满 + 放大**（留白最均衡地最小、格距尽量大）。枚举 cols∈[12,22] ×
  rows∈[6,14]，cell = min(availW/cols, availH/rows)（钳 [34,128]）；
  score = min(宽覆盖, 高覆盖)；先取 score 最高，差 ≤1.2pp 时取格距更大者。
- 1440 屏输出：**13×7@104**（v84 前 12×6@104）——画布 1352×728 vs 1254×630，
  留白 154/157 → 56/59；jsdom 基准（869×758）输出 13×11@63（测试确定性）。
- 上限 `MAP_CELL_MAX=128`（仅防极端）；词面守卫：`ui.mapFrame = { spanX: cols`
  与 `MAP_TARGET_CELL_ISO = 124` 均被 smoke 正则守护，改动别丢词面。

### 26.2 缩略地图（底部导航栏 + 天下大势）
- 数据层 `GAME.map.miniBuild()`：全图 500×500 州/郡归属——**先州后郡两遍最近邻**
  （州心 13 = 都城/州城；郡单元 = 郡城 + 州城[州直辖]），按 map.seed 缓存；
  边界是**地理**划分（读 NPC_CITIES 固有 state，占领不影响）。25 万格约 15ms。
- 渲染层 `ui.miniOff()`（离屏 1000×1000 = 2px/格：地形×州染 + 界线 + 城点，
  按 seed 缓存）+ `ui.drawMini()`（叠加动态我城）+ 底部 40px 小图 + 「天下大势」面板。
- 界线两档：**州界 = 亮金 #f0d060 实线（整格满涂）**；**郡界 = 灰白 #cfcfc0
  对角 2px（细一档）**。城点：都城红 / 州城亮金 / 郡城米白；我城金点动态叠加。
- 底部缩略图由 `ui.paintBottom()` **固定拼装**（不能走 _bottom.push——会被覆盖）。
- CSS 守卫：图例色值走 `.mini-legend` 局部变量（「文字色 ≤8 条硬编码」守卫盯着）。

### 26.3 复核修正：州郡归属改「最近城距离场」（v85.1）
- 复核探针实测：旧版两遍最近邻（26.2）下 174 城中 **24 城（13.8%）色块与自身州
  归属不符**（如宛县属荆州却落在司隶色块、高陵属司隶却落在凉州色块）——其中
  14 座是会出点的郡城，图面上"城点踩在邻州色里"。
- 修法：`miniBuild` 改**多源「最近城」距离场** —— 每座城（含县城）为源、带自身
  state + jun id，两遍 chamfer 扫描（正交 5 / 对角 7，近似欧氏）传播最近源标签。
  城自身格距离 0 必归自己 → 复核实测 **0/174 错位**；县城 jun id 复用「同州最近
  郡心」；郡界 = 同州内 jun 变化处（天然不跨州）。
- 性能：冷启动 3ms（旧版 12ms）；州界/郡界竖边 1685/4226（测试阈值 >500 保持）。
- 精度：近似欧氏边界偏差 ±1 格（≈2px 显示）；远离边界的格采样误差 0.66%。
- 不变量守卫：smoke/e2e 新增「174 城零错位（城格色块 = 城自身州）」断言。
- 复核另确认：固定画布（`--app-w: 1440×900`，v74）下三档分辨率内部几何一致属
  预期；点击映射闭环（点画布中心 = 视野中心格）；底部条 56px 让位无重叠。


## 27. v86：计谋 / 锦囊（老板「按计划进行」· 第四轮 G1）

### 27.1 八计三门（DATA.SCHEMES）
- **attack（出征携带）**：妖言惑众（守军规模 −15%）／火烧粮草（城防值 −30%）／
  挑拨离间（守将忠诚 −25 · 对同一城每日一次）／趁火打劫（掠夺 +30%）；
- **march（出征携带）**：千里奔袭（本次行军 +30%）／金蝉脱壳（战败保全：阵亡 35% 转伤兵）；
- **defense（城池布防）**：空城计（6 小时内下一次来犯**不战而退**·一次性）／
  坚壁清野（8 小时内被来犯损失 −40%）。
- 效果**全部作用于战斗/入侵结算的入参**（零引擎改动）：见 `battle.js` expedition 段、
  `state.js` invasion 段；消耗 = 精力（施计将领）+ 锦囊（商城 1500 金/个，`type:'talis'`）。

### 27.2 唯一出口组（state.js 尾部）
`schemeOf` / `schemeKeyOf`（`npc:id` / `fort:id` / `w:x,y`；防御计独立前缀 `my:<cityId>`）/
`schemePrepare`（统一校验：精力·锦囊·野地挑拨拒绝·每日锁）/ `schemeUse`（扣费 + 记标记）/
`schemeMarksOf` / `schemeDefOf` / `schemeDefSet` / `schemeDefConsume`。
- 状态 `s.schemes = { key: { sid: { n, day, until } } }` **入档**（运行中会变）；
  挑拨忠诚 = 100 − 25 × n（n≥2 加成减半、n≥3 战胜 50% 归降，roll 走 `invasionRoll` 可复现）。

### 27.3 UI —— ⚠️ 面板内选择一律「内联区」，禁用子弹窗
- **弹窗是单根系统**（`ui.openModal` 直接替换 innerHTML，无栈）——从出征面板打开
  子弹窗会**销毁出征面板**，关闭后不回来（e2e 实测抓到）。计略选择做成
  `#exp-scheme-box` 内联展开区（复用 `.exp-body` 滚动样式，未新增 CSS）。
- 出征面板「计略」行（标签 + 选择）→ 内联区（六计卡片 + 选中态 + 逐计不可用原因）；
  城池面板「🎴 计略布防」行 → 布防弹窗（防御计 + 施计将领 genChips）。

### 27.4 战报与可见性
- `result.schemeNote` 进战报【计谋】行；施计/布防/空城计奏效即时 `GAME.log`；
  城池面板常显布防剩余时长（输出值可被看见——项目规矩）。

### 27.5 守卫
- smoke §71 九条：含「11 个 eff 键在逻辑模块有字面读取点」**死数据守卫**（写进表而无
  消费＝死数据，本项目经典失效模式）；e2e v86 七条（真实 DOM 全链路）。


## 28. v87：野地专属场景（老板「为各类野地设计专属弹窗场景」）

> **v88.1 已整合**：本章场景已并入「江湖游历」（见 §29.6），原表/函数/UI 区块均已删除；以下为历史存档。

### 28.1 六地形六场景（DATA.WILD_SCENES）
- **山地 · 绿林探访**（好汉赠金 / 山寨存货 / 豪杰来投 / 珠宝 / 遇袭负伤）；
- **湖泊 · 垂钓**（鱼获充粮 / 沉物锦囊 / 珠宝 / 空竿而归）；
- **沼泽 · 寻宝**（珠宝 / 军资 / 旧钱 / 木盒 / 瘴气负伤）；
- **荒漠 · 地宫探险**（三层递增：旧钱 → 遗藏 → 图纸 / 珠宝 / 塌方负伤）；
- **森林 · 狩猎**（皮毛药材 / 野味充粮 / 失物锦囊 / 空手）；
- **草原 · 牧马**（马市得资 / 牧马辎具 / 良马马鞭 / 风尘仆仆）。
- 平原除外（主战场与筑城地）；每处野地**每日一次**；消耗将领精力 + 体力；
  风险 = 将领负伤（体力损失，⛔ 不损兵——不与出征体系抢平衡）。

### 28.2 唯一出口（state.js）
`wildSceneOf(terrain)` / `wildSceneCheck(x, y, genId)`（统一校验：地形 · 精力 · 体力 ·
每日锁）/ `wildSceneDo(x, y, genId)`（扣费 + 锁 + 种子化抽结果 + 发奖 + 日志）。
- 锁存 `s.wildScenes = { 'x,y': day }`（**游戏日**，入档）；
- **结果种子化**（`invasionRoll('ws|x,y|day|…')`）——同一天同一地结果稳定，
  测试断言可精确复现（`===` 级）。

### 28.3 产出与 UI
- 产出**全走现有体系**：资源（粮/金）入当前城；材料走 `WILD_MATERIAL` 地形池
  （同 id 合并）；珠宝取低四档；道具 jinang / chest / mabian / bp_mingjiang；
  绿林豪杰走 `makeHero`（重名兜底换珍珠）。
- UI：`ui.wildSceneHTML(x, y)` 内嵌野地弹窗**两分支**（未占·出兵弹窗 / 已占·管理面板）；
  genChips 选带队将领；执行后**原地重开**弹窗回显结果与「今日已探」态。

### 28.4 守卫
- smoke §72 五条（含「权重定向 · 豪杰必出」的确定性测试）；e2e v87 四条；真机 4 图。

## 29. v88：灵气双轨装备 + 江湖游历（老板「修炼型装备系统」+「修炼培养系统」）

### 29.1 双轨与切换（核心）
- 灵气装备与军装**共用 `DATA.EQUIP` 表**（`ling: true` 标记）——`eqId -> DATA.EQUIP`
  是全站唯一寻址，共用即全部下游零改动；12 部位 / 6 阶（灵胚→道器）/ 主数值 `lingv`
  （灵力）/ 六维 = 军装同级 x0.75 量级（数值待实测）。
- `g.equipOn = 'sha' | 'ling'`（缺省 sha，老档零迁移）；**分流唯一出口 `S.equipBagOf`**
  （genEquipBonus / genSetBonus / setProgressOf / unequipAll / unequipItem 五处消费）
  —— 切换后六维（genAttrs）、体力上限（staMax）、战斗换算与界面显示**全链自动同步**（零特判）。
- 切换 UI：将领面板「装备栏」行右侧 ⚔军中 / ☯修炼 双态 tab；修炼侧另有 `ui.dollLingPanel`
  （灵力 / 总蕴养 / 精华）与「☯ 蕴养」按钮；12 格槽名走 `DATA.LING_SLOT_NAMES`。
- 灵力 `GAME.lingPowerOf(g)` = 修炼装备汇总（Σ lingv x 蕴养系数）——**独立于六维与战斗公式**，
  只用于江湖游历判定（与当前生效套无关）。

### 29.2 蕴养（修炼侧强化）
- `DATA.LING_TEMPER`：+10 上限 / 每级 +8% / 单级成本 = essBase + (lv+1) x essPerLv
  （+1 级 20 … +10 级 110，累计 650）；材料 = **灵气精华**（`s.items.lingsui`，price 0
  商城不售，游历产出）。
- 出口组（domain.js）：`lingTemperMax / lingTemperCost / lingTemper / lingTemperList`；
  等级存 `inst.enh`（与军装共实例架构，同名各件互不影响）。
- 防护：军装件不可蕴养（导流百炼）、灵气件不可百炼 / 不可拆解；`enhList` 过滤灵气件。

### 29.3 江湖游历（产出线）
- `DATA.LING_ACT` 六活动（讨伐 / 切磋 / 试炼 / 采集 / 修炼 / 拜访）x 地形矩阵（spots）；
  `DATA.LING_VISITS` 拜访事件池（每地形 2 条）。
- 出口组（state.js）：`jianghuActsAt / jianghuDone / jianghuCheck / jianghuDo`。
  五类判定：fight（灵力+等级 vs 难度，+35%/野地等级）/ trial（三轮逐层加码，止步保留已得）/
  gather（抽取 + 25% 双收）/ cultivate（稳定 + 8% 悟道）/ visit（事件池）。
- 锁存 `s.jianghu = { 'x,y|act': day }`（每处**每活动**每日一次）；种子化
  `'jh|x,y|act|day|...'`（invasionRoll，同一天同地同活动稳定可复现）。
- UI：`ui.jianghuHTML(x, y)` 内嵌野地弹窗两分支（v87 场景区块之下）；执行后**原地重开**回显。

### 29.4 顺带修复（v79 遗漏）
- `dollSlot` 原把实例对象直当 `DATA.EQUIP` 的键（装着装备时 `it` 恒 null → 格丢品质色
  显示 empty 类）—— 统一 `eqId` 规范化修复（装备名此前靠 eqLabel 兜住，故未被发现）。

### 29.5 守卫
- smoke §73 八条 · e2e v88 九条 · 真机 8 项（`_v88_shots/`）· 开发探针 27+26 条。
- v88 维护：§71[4] 妖言断言因「小守军逐兵种 Math.round(0.85x) 取整误差」偶发偏离
  （与 v88 无关的既存边界）—— 改选守军 >=300 的野地 + 容差 0.02 -> 0.03。

### 29.6 v88.1 整合：地形专属并入江湖游历（老板「统一」）
- `DATA.WILD_SCENES` 六场景 -> `DATA.LING_ACT` 的 `<terrain>_scene`（kind: 'scene'，
  单地形 spots + outcomes 权重表）；原 WILD_SCENES 表**删除**。
- `GAME.wildSceneOf / wildSceneCheck / wildSceneDo` -> `GAME.jianghuCheck / jianghuDo`
  的 scene 分支（产出逻辑原样移植：金 / 粮 / WILD_MATERIAL / 珠宝 / 道具 / 豪杰）；
  每日锁统一 `s.jianghu`（旧 `s.wildScenes` 不再读写）。
- UI：`ui.wildSceneHTML / ui.doWildScene` 删除 —— 野地弹窗内**单一区块**「江湖游历」，
  scene 排最前（本地招牌）；事件统一 `do-jianghu`（原 `do-wild-scene` 删除）。
- 守卫：smoke §72 改写为「整合结构 + 六地形招牌 + 豪杰定向（jinghu 链）」；e2e v88.1
  段 3 条；真机 5 项（`_v881_shots/`）。§73 活动表断言同步 6 -> 12 项。

## 30. v89 君主专属修炼 + 全屏江湖剧本（2026-09-16）

**一号修订：修炼线收口君主（老板「只有君主将有修炼功能，以及相应装备」）**
- 依据：功法文档 0.1「主角单修」——修炼收窄到玩家角色一人，将领维持轻量配置对象。
- **唯一闸门 `GAME.canCultivate(g)`（= 君主）**：全链收口点——`S.equipBagOf`（非君主恒军装）·
  `GAME.toggleEquipSet`（拒切）· `S.equipItem`（拒穿修炼件）· `GAME.lingTemper`（拒蕴养 +
  他人在身防御）· `GAME.jianghuCheck`（拒游历）。
- UI 收口：将领面板双轨 tab 只给君主渲染（普通将只有军装一轨 + 「钦定不假他人」说明）·
  装备总览/换装/人偶格按同闸门分流 · 游历区块选将收口（「君主亲往」；君主不在城则整块提示）。
- 老档迁移：`GAME.migrateLordLing`（loadFrom 与装备单件化迁移同点调用，`_lingLord1` 一次性）——
  非君主身上灵气件归还背包、`equipOn` 归位军装；君主不受影响。

**二号新增：全屏江湖剧本（老板「专属全屏交互 + 特定退出」）**
- 数据：`DATA.SCENE_FLOW`——12 活动各 2~3 幕「对话 / 事件」，每幕 1~3 个选择；
  选择只累计**修正系数 mods**（pow 我方差数 / reward 数值产出 / wound 负伤 / luck 小幸运），
  **不改判定公式、不改种子**——同选择必同结果（可复现不变式保持）。
- 引擎：`GAME.jianghuDo` 拆三段 Check / Spend / Roll（`jianghuSpend` / `jianghuRoll`）；
  新增 `sceneStart`（只校验不扣费）/ `scenePick`（**首次选择才扣费+落锁**）/ `sceneEscape`。
  one-shot `jianghuDo` 保留：无剧本兜底与单测共用内核，改判定只改一处。
- 退出机制（按 grade 选屏）：win / partial / lose / escape——讨伐「大捷·战果结算」/「铩羽而归」、
  试炼「功成出关」/「昏迷醒来」、修炼「功法大进」、采集「完成采集」、地宫「夺路而逃」、
  中途「鸣金收兵」等；**未动身退出零消耗；已动身退出所耗不返、今日计入**。
- 层位：#scene-fx 独立根（body 直挂，z=1500：弹窗 1000 之上、toast 2000 之下）——
  不占 modal-root（单根替换系统，互不干扰）。
- 守卫：smoke §73 换角君主 + §74 九条 · e2e v88.1/v88 剧本化 + v89 段八条 · 真机 8 项（`_v89_shots/`）。

## 31. v89.1 剧本视觉化（2026-09-16 · 老板「只有文字，能不能再丰富一点」）

**目标**：不动玩法、不改判定、不加资源 —— 把全屏剧本从「纯文字」升级为「有画面」。

**数据层（插入式补丁，原文案零改动）**
- `SCENE_FLOW[act].art`：12 活动各配**幕景水印**（大字 emoji，横幅右侧 10% 透明）。
- `SCENE_FLOW[act].stages[j].s`：36 幕各配**幕题**（2~8 字：军情来报 / 阵前对峙 / 击鼓进兵 …）。

**表现层（零新资源：地形色 / 天气 / 头像 / 主题变量全复用）**
- **幕景横幅** `.sxf-hero`：地形色染色（`TERRAIN[].color` hex→rgba 渐变）+ 幕景水印
  + 活动徽记 + 门类章（征伐/试炼/采撷/修真/访贤/游历）+ 君主头像 + 灵力·精力·体力
  + 天时日号（季节 · 第 N 日 · 天气；story 缺席静默省略）+ 进度点 + 退出钮。
- **行程时间线** `.sxf-timeline`：已走过的**幕题 + 当时抉择**（`picks`），› 分隔。
- **幕题条** `.sxf-stage-tag`：第 X / Y 幕 + 幕题。
- **对白高亮** `ui.sxfQuote`：先转义、再把「……」独立成段（`.sxf-q` 金色）——防注入。
- **倾向徽章** `ui.sxfBadges`：由修正系数直接派生——攻(pow)金 / 获(reward)绿 / 险(wound>1)红 /
  稳(wound<1)蓝 / 缘(luck)琥珀；无修正不出徽章。选前即知取舍。
- **结算卡** `.sxf-result`：光晕徽记（grade 配色：win 金 / lose 红 / escape 灰）+ 战果行（◆ 前缀）
  + **耗用账单**（耗：精力 -X · 体力 -Y　│　余：…；未动身退出无账单）。
- 动效：`.sxf-wrap` 入场 sxfIn · `.sxf-emblem` 弹出 sxfPop。

**守卫踩线记录（三条，均为配额/禁令型）**
1. CSS 裸像素字号 ≤9（原 9 处已满）→ 三个装饰字号走**令牌** `--sxf-art-sz/--sxf-act-sz/--sxf-emb-sz`；
   旧的内联 26px/44px 一并收进 CSS —— ui.js 裸像素 **0 处**。
2. `letter-spacing: 4px` 全站禁令（v24 标题字距收敛）→ 进度点用 5px。
3. 半透明白描边禁 `rgba(255,255,255,…)` → 光晕用 `rgba(var(--gold-rgb),.18)`。

**验证**：smoke §75 六条 · e2e 四条（横幅/时间线/结算卡）+ 浮点断言容差化 · audit 全 0 ·
探针 27/0 · 真机 11/11 + 零页面错误（`_v891_shots/` 8 图，含素绢主题适配图）。

## 32. v89.2 场景化（2026-09-16 · 老板「纯选择，缺少场景交互 …… 我想看见个湖，而不是一行字」）

**目标**：把「读三行字 + 点三个一样的按钮」改成「看着一幅画 + 在画里做事」。

**① 场景插画（每活动一幅程序化风景）**
- `GAME.map.paintScene(ctx, scene, seed)`：12 种场景 —— 湖 / 山寨 / 古阵 / 山野 / 林间 / 柴扉 /
  山道 / 沼泽 / 地宫 / 行猎 / 草原 / 演武场。
- 全部由 map.js 既有画师原语（drawTree / mountain / grassTuft / flower / dot / seg）程序化绘制 ——
  与地图同一套笔法，风格天然一致；逻辑画布 860×260，DPR 2 倍。
- 确定性：种子 = 格子坐标（同一地点每次打开画得一样）；画布缺失（测试桩）静默跳过。

**② 三种交互轮转（打破「千篇一律的按钮」）**
| 幕 | 交互 | 说明 |
|---|---|---|
| 第 1 幕 | **场景热点（spot）** | 选项直接落在画里（芦苇荡边 / 湖心石上 / 柳荫之下），点画选点 |
| 第 2 幕 | **应对卡** | 按钮带派生图标（⚔️进 / 🎁获 / 🛡️稳 / 🔥险 / 🍀缘） |
| 关键幕 | **时机条（timing）** | 指针来回扫，看准了停手 —— 正中 / 不错 / 脱手 三档 |

- 判定 `ui.sxfTimingGrade`：|p−0.5| ≤ 0.06 → 0 档；≤ 0.22 → 1 档；其余 2 档。
  桩位即选项 → 停手照走 `scenePick(i)`，**引擎零改动、可复现不变式保持**（同档同结果）。
- 数据：`SCENE_FLOW[act].scene`（12）+ 时机幕 `t2`（12 处：提竿！/ 击鼓！/ 收功！…），
  三档结局由数据给出（首档增益 / 末档折损）。

**③ 验收方法（看不见图也要能验）**
- 真机脚本对画布**取像素**：湖景水面区 B > R、沙丘 R > B、明暗跨度 > 阈值、非纯色；
- 探针核对三幕各自的 DOM 结构（热点数 = 选项数、时机条三件套、按钮图标）；
- smoke 用「记录式桩 ctx」把 12 幅画全画一遍（未实现的方法会直接抛错）。

**守卫踩线（本轮两条）**
1. map.js **全文件禁 `Math.sin/cos/tan` 调用与仿射矩阵**（v50：等距绘制纯线性变换）
   → 古阵刻度改**预置单位圆表**；DPR 缩放改 `save + scale + restore`。
2. 全文正则守卫连注释一起匹配 → 注释里也**不许出现被禁字样**。

**验证**：smoke 2206/0（§76 七条）· e2e 772/0 · audit 全 0 · 探针 29/0 · 真机（`_v892_shots/` 8 图 + 像素采样）。

## 33. v89.3 文案统一 · 灵物志（2026-09-17 · 老板「那鹿不是普通鹿，那鱼并非寻常鱼」）

**目标**：全部活动文案与命名收进「三国 × 修炼」话语体系 —— 灵物有灵物的名，活动有活动的雅号。

**① 灵物志（旧称清退 · 命名唯一）**

| 旧称 | 新名 | 线索（湖 / 林 / 采 / 原） |
|---|---|---|
| 鱼 · 大物 | **灵鲤** | 不似凡鱼 → 灵鲤咬钩 → 灵鲤满篓 |
| 野物 | **白鹿** | 蹄印灵光 → 白鹿现踪（通体如雪，驻步回望） |
| 花蛇 | **灵蛇**（青鳞） | 青鳞灵蛇盘于灵草之上，信子轻吐，并不惧人 |
| 马群 · 烈马 · 良马 | **灵驹** | 灵驹成群 → 灵驹相人 → 灵驹贴耳，愿随君行 |

点睛：沼泽「水雾深处隐有青光游走」· 地宫「壁画画的正是方士祭炼之景」· 采集「灵菌与矿苗」。

**② 活动命名三规则**

- **通用六活动 · 2 字名**：讨伐 / 切磋 / 试炼 / 采集 / 修炼 / 拜访（本身已统一，锁定为规则）；
- **地形六招牌 · 4 字名**：绿林探访 / **临湖垂钓**（原「垂钓」）/ 沼泽寻宝 / 地宫探险 / 林中狩猎 / 草原牧马；
- **每活动两枚章**（横幅显示）：门类章 `cat`（2 字）+ 雅名 `alias`（4 字）：

| 活动 | 门类章 | 雅名 | 活动 | 门类章 | 雅名 |
|---|---|---|---|---|---|
| 讨伐 | 征伐 | 拔寨擒王 | 绿林探访 | 绿林 | 山道会盟 |
| 切磋 | 论武 | 以武会友 | 临湖垂钓 | 垂纶 | 金鳞之约 |
| 试炼 | 试炼 | 石门三重 | 沼泽寻宝 | 寻珍 | 瘴泽寻珍 |
| 采集 | 采撷 | 采灵撷芳 | 地宫探险 | 探幽 | 古冢探幽 |
| 修炼 | 修真 | 吐纳周天 | 林中狩猎 | 行猎 | 林中逐鹿 |
| 拜访 | 访贤 | 柴扉清谈 | 草原牧马 | 牧驹 | 草原驯驹 |

**③ 数据与渲染**：`DATA.LING_ACT[k].cat / .alias` 为唯一来源；横幅 `a.cat || SXF_KIND[a.kind]` 兜底；
结算卡（`exits` + `outcomes[].t`）同步改用灵物名。**纯文案改动：判定 / 种子 / 可复现不变式零变化。**

**验证**：smoke 2212/0（§77 六条）· e2e 773/0 · audit 全 0 · 探针 20/0 · 真机（`_v893_shots/` 7 图 + 像素采样）。

## 34. v89.4 野地生态（2026-09-17 · 老板「不同野地有不同活动，不是所有野地都有活动，概率出现，活动难度跟奖励与野地等级有关」）

**目标**：把「按地形铺活动」升级为「逐地分布 + 概率出现 + 等级联动收益」。

**① 逐地分布（`GAME.jianghuActsAt(x, y)`）**
- 候选 = 地形过滤后的「全部可能活动」（`GAME.jianghuCands(terrain)` —— 分布与测试共用）；
- 每格先掷**荒僻率**（`DATA.JH_SPREAD.noneP = 0.30`）—— 不是所有野地都有活动；
- 有事格 **1~3 事**（`p2 = 0.62` / `p3 = 0.87` 两档），候选按种子洗牌取前 N —— 不同野地不同组合；
- **确定性**：组合 = `f(格子坐标)` 的 FNV 伪随机（`GAME.invasionRoll('jhsp|…')`），**同格恒同貌**；
  不加日盐（与 wildLevel 的「每日 +1」解耦 —— 避免每日重抽破坏可复现与测试）。

**② 闸门**：`jianghuCheck` 在地形门之后新增「此事不在本格」门（文案：「野地之事随缘而现，换一处看看」）。

**③ 等级联动（lv = 该格野地等级 0~10，系数全走 `DATA.JH_SPREAD`）**

| 项 | 公式 | 出口 |
|---|---|---|
| 难度（战斗 / 试炼） | ×(1 + lv×0.35) | `need = a.power * lvN` |
| 负伤 | ×(1 + lv×0.20) | `mw()`（战斗败 / 场景负伤） |
| 收益 | ×(1 + lv×0.30) | `mi()`（精华 / 金 / 粮 / 材料…全出口） |

lv10 ≈ 难度 ×4.5 · 负伤 ×3 · 收益 ×4.0；**lv0 = v88 原值逐位一致**（可复现不变式保持）。

**④ UI**：游历区块恒显；荒僻格给空态提示；标题下行显示「野地 LvN · 难度 ×… · 收益 ×…」——
数字由公式生成（smoke §78 锁死）。

**验证**：smoke 2221/0（§78 八条）· e2e 775/0 · audit 全 0 · 探针 14/0 · 真机（`_v894_shots/` 4 图）。

## 35. v89.5 灵机之地（2026-09-17 · 老板「按建议进行」—— v89.4 收尾建议的地标提示）

**目标**：活动已逐地概率化（v89.4），玩家需要"一眼看到哪有江湖事"的地标。

**① 判定（唯一出口 `GAME.jianghuSpotInfo(x, y)`）**
- **灵机 = 事数 × 野地等级**；`DATA.JH_MARK.minScore = 16`（3 事地 Lv6+ / 2 事地 Lv8+；1 事地不满阈）；
- 返回 `null` = 此地无江湖事（荒僻 / 非野地地形）；返回 `{ n, lv, score, mark }`；
- 实测密度（抽样 4.97 万格）：**12.8% 野地格 / 18.3% 有事格** 为灵机 —— 真机整屏 ≈ 10~20 面旗。

**② 绘制（`drawJhPennant`，map.js 私有画笔）**
- 青旗悬于**等级角标右侧**（菱形格内唯一不撞 名带/角标 的空当）；
- 青碧旗面 `rgba(126,226,198,.96)` + 深青旗杆与描边 —— 现有调色盘（土黄/绿族/水蓝/金）无同族；
- ⛔ 零三角函数、零仿射变换（v50 全文件守卫）：只用 moveTo / lineTo / closePath 直线原语。

**③ 点选信息（`ui.mapPickText`）**：野地格补后缀 —— 有事「· 江湖事 ×n」（灵机者「· 灵机」）；
荒僻「· 荒僻」；非野地（平原/城）无后缀。

**④ 调参**：只改 `DATA.JH_MARK.minScore`（0 → 全亮；9999 → 全灭）—— 已在测试与探针双向验证。

**验证**：smoke 2230/0（§79 九条）· e2e 779/0（19b 四条）· audit 全 0 ·
探针 13/0（**青旗数 == 复刻视野计数**，三中心精确相等）·
真机 7/7（`_v895_shots/` 4 图 + 像素采样：整屏 408 jade · 逐格 12/17/17 · 负向 0 · 阈值拉满 0 → 复原 554）。

## 36. v89.6 奇遇 · 见闻录（2026-09-17 · 老板「没有达到我想要的探索性和趣味性。赛博许愿术，发动！」）

**目标**：野地此前"全图可见、点开即得"——没有隐藏、没有发现、没有收集。
本轮加**探索层**三件套：隐藏点位 → 现形 → 收录。

**① 隐藏点位（`GAME.wonderSites()`）**
- map.seed 的确定性函数（一次生成缓存，50±处/图）；只落在六种野地，不压据点/城池；
- 三带密度：近郊 `0.0016` / 远野 `0.0006` / 绝域 `0.00024` —— **愈远愈奇**（档位随带加权：
  近=逸闻/中=逸闻·奇珍·少量绝景/远=奇珍绝景为主）；
- **全池轮转保底**：点位按带排序后逐位取「类型轮转表」（前 24 位即全部 24 类；余篇模式
  [奇珍·绝景·奇珍·逸闻] 循环，保住「愈远愈奇」）—— 无论 seed，24 类**必**全出现。
  ⚠️ v1「档内轮转」在逸闻点位 <12 时漏类（门禁实测 2/3 红），已改。

**② 现形（双通道）**
- **线索**：走完任一江湖活动，按 `clueP = 0.18`（日盐确定性）闻得一处未现形点位，
  文案含方向（八向、零三角函数）+ 里数 + 地带：「闻得城西南约 560 里 绝域 之地（x,y）……」
- **就近探察**：打开野地弹窗时，方圆二格（切比雪夫）内未现形点位自动现形（「环顾四周」）；
- **真隐藏**：未现形时 UI 零暗示（无灰点、无文案）——这是探索层的地基。

**③ 探奇三段（`wonderCheck / wonderSpend / wonderRoll`，与江湖活动同形状）**
- 进入免费（「未动身离去零消耗」）；**首次选择才扣费**（精 6 / 体 2）并锁点位；
- 无论成败，**此缘即断**（`s.wonders.d` 落账，重复探奇被拒）；
- 奖励三档（区间不重叠）：逸闻 `精华 4-9 + 金 60-150` ／ 奇珍 `+ 材料 2-3` ／
  绝景 `精华 26-44 + 材料 4-6 + 珠宝 1`；选项的 `reward` 放大、`wound` 折体（贪字头上一把刀）；
- 场景机与江湖活动**共用**（`GAME.sceneBegin`）：幕题/时间线/退出账本/专属退出零改动继承，
  分流点只有两处（扣费 / 结算）。

**④ 见闻录（24 条三档：逸闻 ×12 / 奇珍 ×8 / 绝景 ×4）**
- 地图底栏「📜 见闻录」→ 图鉴：已录显名显文、未录「？？？+ 未录」；进度「已录见闻 N / 24」；
- 「待探线索」清单（前 10 条）带「前往」按钮（回地图并居中）。

**⑤ 视觉**：奇缘星 ✦（四芒星 · 青莲紫 `--wonder`，角标**左侧**；青旗在右侧，✦ 是"未知的邀约"、
旗是"已知的价值"）；奇遇场景横幅「奇缘紫」底纹；结算屏线索行（紫框虚线）；
`--wonder / --wonder-rgb` 四主题各配（浅色系加深）。

**验证**：smoke 2241/0（§80 十一条）· e2e 793/0（19c 十四条）· audit 全 0 ·
探针 16/0（紫星数 == 复刻视野计数 三中心精确相等 · 奖励档梯度 逸闻<奇珍<绝景 · 线索概率贴设计）·
真机 12/12（`_v896_shots/` 7 图 + 像素：整屏 49 紫 · 逐格 16 · 负向 0 · 已探 0）。

## 37. v89.7 头像可更换 + 供奉公文静默（2026-09-17 · 老板「头像可更换；供奉+1这个公文不要显示」）

**① 头像可更换（唯一事实源 = `s.ruler.portraitSeed`）**
- `GAME.setLordAvatar(idx)`：按性别取池（m / f 各 20 张），越界一律拒绝（不动任何字段）；
  一处改、**两处同源** —— 顶栏 `#lord-avatar`（`syncHeader` 读 rulerSeed）与君主将领
  （`makeLordGeneral` 写入的 `g.portraitSeed`）同时换脸（v70「同脸」口径）。
- 入口两个：① 顶栏头像（`data-action` 挂在**外层 div** —— syncHeader 每秒只换内部立绘，
  动作不丢）；② 君主面板「头像」行（`.lord-av-mini` 缩略图 + 更换按钮）。
- `ui.openAvatarPick`：`.modal-lg` + `noClose`（底部「完成」自管：回君主面板，同改名流程）；
  网格 `.av-grid` 5×4（`.av-cell` 96px · `aspect-ratio: 1/1` · 当前脸 `.cur` 高亮）；
  点选即换并**原地重绘**（高亮跟走）。
- 存档：只存 seed（几字节），老档免迁移；换脸不写公文、只 toast。

**② 供奉公文静默（`GAME.artGain` 两条规则）**
- 写公文的条件 = **有缘由**（`why` 非空：占城 `开疆拓土 · X` / 晋升 `爵位晋升 · X`）；
  时长累积（`artTick` 传空 why）一律静默 —— 升阶那条报喜保留。
- 小数寄存 `s.artifacts.frac`（入档）：`add = floor(n)`，小数攒入 frac、满 1 进位。
  ⚠️ 修前 120× 每秒 n=0.1 被 `Math.round` 吃掉 → 供奉**永远不涨**；600× 每秒 n=0.5 →
  每秒一条「供奉 +1」公文（老板所见）。修后各倍率 / 在线离线口径一致：**3 点 / 游戏小时**。
- 边界：`pts` 永远整数（界面与门槛判定不动）；frac ∈ [0,1)；`artGain` 返回**实际入账**整数。

**验证**：smoke 2251/0（§82 四条）· e2e 806/0（§82 四条真实点击）· audit 全 0 ·
探针（120× 15s → +1 且零公文 · 120× 120s → +11（名义 12）零公文 · 600× 10s → +5 零公文 ·
事件 +500 照写公文 · `setLordAvatar` 越界拒绝且不动 seed）。

## 38. v89.8 文字游戏 v2：5~7 段结构 + 壁画变换（2026-09-17 · 老板「3段式可读性好，交互性差，适当增长篇幅，5-7段选择。最好还能有背景壁画的变换」）

**① 结构（全路径同层）**
- 第 1 段启幕 → 第 2~N-1 段各 2 个**变体幕** → 第 N 段收束（3 选 → 3 结局）；N ∈ 5~7。
- 判据：幕选项只能指向 depth+1；结局只能由 depth=N 的幕指向；同幕选项不得指向同一结局（check.py 判据 4/8）。
- `GAME.SG.rankCount(st)` = 层数（BFS 最深层），界面「第 N 段 · 共 M 段」与测试共用。

**② 壁画（13 键 · 程序化 SVG）**
- `ui.SG_MURAL`：yat/ku/zhai/yuan/jiu/xiang/hu/hud/shan/ying/men/fu/xiao。
  画法：底色吃主题变量（明暗四主题成立）、剪影半透明墨色、灯火月水暖色低透明；viewBox 1200×800 slice。
- 阅读器骨架：两层 `.sgr-bg` + `.sgr-scrim` + `.sgr-body`（一次建成，换段只换 body 与壁画）。
- `ui.sgBg(key)`：同键不闪；否则把新壁画写入空闲层 → `data-on` → 旧层摘除 → CSS `transition: opacity .55s` 交叉淡入。
- 沙幕：`linear-gradient(180deg, var(--bg-dark) 0%, transparent 15%)` + `(transparent 30%, var(--bg-2) 74%)` —— 主题自适应、保正文对比度。

**③ 交互补强**
- 段进度：「第 N 段 · 共 M 段」+ 进度点（`.sgr-dots`，已完成点亮）。
- 段入场动效 `@keyframes sgrIn`（340ms fade+rise，每段重放）。
- 样式守卫：`.sgr-bg svg` 走 v38「图标容器」守卫的**第四类豁免**（天生矢量、永不为位图）。

**验证**：check.py 全绿（5 篇 · 段数 6/6/5/7/6 · 均 3446 字）· smoke 2253/0 · e2e 807/0 · audit 全 0。

## 39. v89.9 故事库铺量与建筑入口（2026-09-17 · 老板「开始铺量」）

**① 建筑逸闻块脱三元（接线口径）**
- `ui.openBuildModal` 的 `ui.SG_BLOCK('building', …)` **移出 `BLDG_FUNC[b.id] ? … : ''` 三元**、与功能块同级。
- 原因：`BLDG_FUNC` 只收录 12 座建筑 —— **民房 / 驿站 / 烽火台 / 鸿胪寺 4 座被整个漏掉**。
- 口径：**逸闻块对所有建筑统一渲染**（无故事时 `SG_BLOCK` 自返空串，不占位）；与野地 / 城池入口同构。

**② 空态断言动态化（测试口径）**
- smoke / e2e 不再写死「某建筑无故事」；改为**从 16 座建筑里动态找一座尚无故事的**做空态样本（全有则自动放行）。

**③ 铺量节奏（每批 6 篇 · 混锚点）**
- 流水线：写 → `check.py` → 补字（<240 的幕）→ 接线（index / smoke / e2e）→ audit / smoke / e2e → `gen_manifest.py` → README 进度。

**验证**：check.py 11 篇全绿 · smoke 2255/0 · e2e 811/0 · audit 全 0。

## 40. v89.10 故事库铺量二批与「双转义」防线（2026-09-17 · 老板「直接做完」）

**① 全建筑点位点亮**：16 座城内建筑 + 4 座城外建筑入口全部有故事可读（野地 5/6 · 城池 2/4）。
**② 校验器判据 10（新增）**：文本字段（幕文 / 幕题 / 选项 / 结局）不允许出现字面反斜杠字符 ——
   专拦「双转义」类事故（症状：游戏内露出「\n」字样、段落不换行）。
**③ 批量质检口径（铺量配套）**：新卷入库前必过「三道」：check.py 十条判据 · **显示层抽读**（完整读 1~2 篇）·
   句子回声扫描（同节点句子相似度 ≥0.5 逐条复核）；补字一律「新增信息」，禁复述；「两条路」引导句前必须换段。

**验证**：check.py 29 篇全绿 · smoke 2256/0 · e2e 813/0 · audit 全 0。
