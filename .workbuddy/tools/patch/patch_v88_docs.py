# -*- coding: utf-8 -*-
"""v88 文档：设计规范 §29 / 备忘 §三十一 / 档案 v88 段 / 规划 §十八补记。探针幂等 + 行尾适配。"""
import io

def patch(path, seg, nl, probe):
    t = io.open(path, encoding='utf-8', newline='').read()
    if probe in t:
        print('SKIP ' + path.split(chr(92))[-1])
        return
    seg2 = seg.replace('\n', nl) if nl != '\n' else seg
    t = t.rstrip('\r\n') + nl + nl + seg2
    io.open(path, 'w', encoding='utf-8', newline='').write(t)
    print('OK ' + path.split(chr(92))[-1])

# ============ 1) 设计规范 §29（CRLF） ============
SEG1 = """## 29. v88：灵气双轨装备 + 江湖游历（老板「修炼型装备系统」+「修炼培养系统」）

### 29.1 双轨与切换（核心）
- 灵气装备与军装**共用 `DATA.EQUIP` 表**（`ling: true` 标记）——`eqId -> DATA.EQUIP`
  是全站唯一寻址，共用即全部下游零改动；12 部位 / 6 阶（灵胚→道器）/ 主数值 `lingv`
  （灵力）/ 六维 = 军装同级 x0.75 量级（数值待实测）。
- `g.equipOn = 'sha' | 'ling'`（缺省 sha，老档零迁移）；**分流唯一出口 `S.equipBagOf`**
  （genEquipBonus / genSetBonus / setProgressOf / unequipAll / unequipItem 五处消费）
  —— 切换后六维（genAttrs）、体力上限（staMax）、战斗换算与界面显示**全链自动同步**（零特判）。
- 切换 UI：将领面板「装备栏」行右侧 ⚔军中 / ☯修炼 双态 tab；修炼侧另有 `ui.dollLingPanel`
  （灵力 / 总蕴养 / 精华）与「☯ 蕴养」按钮；12 格槽名走 `DATA.LING_SLOT_NAMES`。
- 灵力 `GAME.lingPowerOf(g)` = 修炼装备汇总（Σ lingv x 蕴养系数）——**独立于六维与战斗公式**，
  只用于江湖游历判定（与当前生效套无关）。

### 29.2 蕴养（修炼侧强化）
- `DATA.LING_TEMPER`：+10 上限 / 每级 +8% / 单级成本 = essBase + (lv+1) x essPerLv
  （+1 级 20 … +10 级 110，累计 650）；材料 = **灵气精华**（`s.items.lingsui`，price 0
  商城不售，游历产出）。
- 出口组（domain.js）：`lingTemperMax / lingTemperCost / lingTemper / lingTemperList`；
  等级存 `inst.enh`（与军装共实例架构，同名各件互不影响）。
- 防护：军装件不可蕴养（导流百炼）、灵气件不可百炼 / 不可拆解；`enhList` 过滤灵气件。

### 29.3 江湖游历（产出线）
- `DATA.LING_ACT` 六活动（讨伐 / 切磋 / 试炼 / 采集 / 修炼 / 拜访）x 地形矩阵（spots）；
  `DATA.LING_VISITS` 拜访事件池（每地形 2 条）。
- 出口组（state.js）：`jianghuActsAt / jianghuDone / jianghuCheck / jianghuDo`。
  五类判定：fight（灵力+等级 vs 难度，+35%/野地等级）/ trial（三轮逐层加码，止步保留已得）/
  gather（抽取 + 25% 双收）/ cultivate（稳定 + 8% 悟道）/ visit（事件池）。
- 锁存 `s.jianghu = { 'x,y|act': day }`（每处**每活动**每日一次）；种子化
  `'jh|x,y|act|day|...'`（invasionRoll，同一天同地同活动稳定可复现）。
- UI：`ui.jianghuHTML(x, y)` 内嵌野地弹窗两分支（v87 场景区块之下）；执行后**原地重开**回显。

### 29.4 顺带修复（v79 遗漏）
- `dollSlot` 原把实例对象直当 `DATA.EQUIP` 的键（装着装备时 `it` 恒 null → 格丢品质色
  显示 empty 类）—— 统一 `eqId` 规范化修复（装备名此前靠 eqLabel 兜住，故未被发现）。

### 29.5 守卫
- smoke §73 八条 · e2e v88 九条 · 真机 8 项（`_v88_shots/`）· 开发探针 27+26 条。
- v88 维护：§71[4] 妖言断言因「小守军逐兵种 Math.round(0.85x) 取整误差」偶发偏离
  （与 v88 无关的既存边界）—— 改选守军 >=300 的野地 + 容差 0.02 -> 0.03。
"""
patch(r'E:\Deepseekdb\docs\设计规范.md', SEG1, '\r\n', '## 29. v88：灵气双轨装备')

# ============ 2) 备忘 §三十一（LF） ============
SEG2 = """## 三十一、v88 灵气双轨 + 江湖游历（2026-09-16）

### 31.1 双轨改造的「引用面清单法」（本项目的标准姿势）
- 大改一个**横切字段**（g.equip -> 双袋）之前，先全仓扫描 `.equip` 引用，
  把每一处标注「改 / 不改 / 为什么」（本次 30+ 处 -> 实改 20 处）；
- 收敛原理：**唯一寻址共用**（灵气装进 DATA.EQUIP，全站 eqId 寻址零改动）+
  **分流唯一出口**（equipBagOf 五处消费）—— 把「改 30 处」收敛成「改 1 处 + 20 处签字」。
- 增删装备袋时的检查单：eqPieces / eqFind（寻址）/ 解雇归还 / 统计计数 /
  wornBy 标记（背包页与详情）/ 强化与拆解防护 —— 一袋新加、六处跟手。

### 31.2 偶发测试失败的定位术（两次运行失败点不同）
- 现象：同一代码两次跑 smoke，失败段不同 -> 必是**随机地图 + 边界值**，不是回归；
  先查「这段断言依赖哪些随机量」，再定位取整 / 阈值精度；
- 本次实录：妖言断言（0.85 折扣）在**小守军**时因逐兵种 Math.round 取整误差偶超
  0.02 容差。修法：选格门槛抬到「守军 >=300」（大样本误差 <1%）+ 容差 0.03。
- 定式：**边界精确断言先保证样本量**（大数才配 2% 级容差）。

### 31.3 大补丁的分段与探针
- 本次八个补丁按文件分层递进（data -> toolsx2 -> systems -> state -> uix2 -> main ->
  tests/e2e），每段落盘即验语法 + 幂等二跑；
- 探针字符串必须能在**替换后的文本**中命中原样（差一个引号 / 全角空格就撞 assert ——
  本次 V5d 因探针前缀多一个引号而误报，修法：直接对齐替换后文本的子串）。
"""
patch(r'E:\Deepseekdb\docs\AI工作备忘.md', SEG2, '\n', '## 三十一、v88 灵气双轨')

# ============ 3) 需求档案 v88 段（CRLF） ============
SEG3 = """### v88 · 落地（老板「修炼型装备系统」+「修炼培养」）

> 设计另外一套全新的修炼型装备系统，与现有以煞气为基础的军中装备并存。在当前将领装备栏位置增加切换按钮……打造一个修炼培养的系统，将领将有机会去野地游历……

**落地**（「按建议进行」+「逐步实现」）：
- **灵气装备（双轨）**：72 件（12 部位 x 6 阶：灵胚→灵器→法器→宝器→灵宝→道器）；
  与军装共用 DATA.EQUIP（ling 标记）；主数值灵力（游历战力，不进六维不入战斗）；
  六维约军装同级 75%（作战略逊不撼动军装主战力）；术语江湖化（灵剑/道袍/云履…）。
- **切换按钮**：将领面板「装备栏」行 ⚔军中 / ☯修炼 双态 tab；零成本可来回；
  g.equipOn 分流唯一出口（equipBagOf）→ 六维 / 体力 / 战斗 / 界面显示全链自动同步。
- **蕴养**：+10 上限 / 每级 +8% / 灵气精华（+1 级 20 → +10 级 110）；
  军装件导流百炼、灵气件不可百炼 / 不可拆解。
- **江湖游历（MVP 六活动）**：讨伐 / 切磋 / 试炼 + 采集 / 修炼 / 拜访；地形矩阵绑定；
  每处每活动每日一次；种子化可复现；产出精华 + 低概率灵气装；负伤不损兵。
- **顺带修复**：dollSlot 的 v79 实例寻址遗漏（装着的装备格丢品质色）。
- 测试：smoke **2185/0**（§73 八条）· e2e **755/0**（v88 九条）· 真机 8/8
  （截图 `_v88_shots/`）· 开发探针 27+26 条 · audit 全 0。
  维护：§71[4] 偶发边界修复（大守军选格 + 容差 0.03）。
- 细节：`docs/设计规范.md` §29；`docs/AI工作备忘.md` §三十一。
"""
patch(r'E:\Deepseekdb\需求档案.md', SEG3, '\r\n', '### v88 · 落地')

# ============ 4) 规划 §十八 补记（LF） ============
SEG4 = """## 十八·补记：v88 落地（2026-09-16）

> 本节设计（灵气双轨 + 江湖游历）**已按建议落地 MVP**：
> - **双轨切换**（g.equipOn + equipBagOf 分流唯一出口）· **蕴养**（灵气精华，独立材料）·
>   **江湖六活动**（讨伐/切磋/试炼 + 采集/修炼/拜访）· 顺带修复 dollSlot 品质色遗漏；
> - 第二批候选 X1-X10（素材库 §4.1）与 V3 六项（§4.2）留待 v89 / v90 分批上线；
> - 全程：smoke 2185/0 · e2e 755/0 · audit 全 0 · 真机 8/8（zero page error）。
> - 详见 `docs/设计规范.md` §29 与 `docs/AI工作备忘.md` §三十一。
"""
patch(r'E:\Deepseekdb\docs\玩法扩展规划.md', SEG4, '\n', '## 十八·补记：v88 落地')

print()
print('全部完成。')
