# -*- coding: utf-8 -*-
"""v89.5 文档补丁 —— 五份文档追加（设计规范 §35 / 备忘 §37 / 规划二十三 / 档案 v89.5 / 设计文档 v1.7）"""
import io

def patch(path, marker, block, tail_expect):
    d = io.open(path, encoding='utf-8', newline='').read()
    if marker in d:
        print('SKIP', path.split('\\')[-1])
        return
    nl = '\r\n' if '\r\n' in d[:4000] else '\n'
    t = d.rstrip('\r\n')
    if not t.endswith(tail_expect):
        raise SystemExit('!! tail 校验失败 %s : %s' % (path, tail_expect[:48]))
    t = t + nl + nl + block.replace('\n', nl) + nl
    io.open(path, 'w', encoding='utf-8', newline='').write(t)
    print('OK', path.split('\\')[-1])

# ---------- 设计规范 §35 ----------
patch(r'E:\Deepseekdb\docs\设计规范.md', '## 35. v89.5', '''## 35. v89.5 灵机之地（2026-09-17 · 老板「按建议进行」—— v89.4 收尾建议的地标提示）

**目标**：活动已逐地概率化（v89.4），玩家需要"一眼看到哪有江湖事"的地标。

**① 判定（唯一出口 `GAME.jianghuSpotInfo(x, y)`）**
- **灵机 = 事数 × 野地等级**；`DATA.JH_MARK.minScore = 16`（3 事地 Lv6+ / 2 事地 Lv8+；1 事地不满阈）；
- 返回 `null` = 此地无江湖事（荒僻 / 非野地地形）；返回 `{ n, lv, score, mark }`；
- 实测密度（抽样 4.97 万格）：**12.8% 野地格 / 18.3% 有事格** 为灵机 —— 真机整屏 ≈ 10~20 面旗。

**② 绘制（`drawJhPennant`，map.js 私有画笔）**
- 青旗悬于**等级角标右侧**（菱形格内唯一不撞 名带/角标 的空当）；
- 青碧旗面 `rgba(126,226,198,.96)` + 深青旗杆与描边 —— 现有调色盘（土黄/绿族/水蓝/金）无同族；
- ⛔ 零三角函数、零仿射变换（v50 全文件守卫）：只用 moveTo / lineTo / closePath 直线原语。

**③ 点选信息（`ui.mapPickText`）**：野地格补后缀 —— 有事「· 江湖事 ×n」（灵机者「· 灵机」）；
荒僻「· 荒僻」；非野地（平原/城）无后缀。

**④ 调参**：只改 `DATA.JH_MARK.minScore`（0 → 全亮；9999 → 全灭）—— 已在测试与探针双向验证。

**验证**：smoke 2230/0（§79 九条）· e2e 779/0（19b 四条）· audit 全 0 ·
探针 13/0（**青旗数 == 复刻视野计数**，三中心精确相等）·
真机 7/7（`_v895_shots/` 4 图 + 像素采样：整屏 408 jade · 逐格 12/17/17 · 负向 0 · 阈值拉满 0 → 复原 554）。''',
'真机（`_v894_shots/` 4 图）。')

# ---------- 备忘 §37 ----------
patch(r'E:\Deepseekdb\docs\AI工作备忘.md', '### 37. v89.5', '''### 37. v89.5 · 灵机旗（工程视角）

1. **密集 HUD 的「唯一空当」定位法**：菱形格内 名带（下半）+ 角标（上顶）已占满，
   新标记先算**空间预算**再设计形状 —— 实测唯一可用空当 = 角标右侧 ~13.5px 起、
   宽 ~9px（且旗尖 ≤ 东邻名带起点 c.x+26）。先画预算草图，再画元素。
2. **阈值型标记的「记录式 ctx 数色法」**：旗面颜色定义成唯一常数
   （`rgba(126,226,198,.96)`），用记录 fillStyle 的桩 ctx 真跑 `map.render`，
   数「旗面填色」次数；**双向断言**：有灵机 ≥ 1、阈值拉满必须 == 0。
3. **精确计数复刻**：探针复刻 render 的视野范围数学（四角反投影 ±2 格余量 + 屏幕包围盒 ±cell
   + city/非野地/无名跳过），青旗数 == 复刻计数 —— **三中心精确相等**（55=55 / 49=49 / 97=97）。
   "接线正确"要用等式证明，不是"看着有"。
4. **真机像素分类器**：jade 判定 = `g ≥ 208 && g−r ≥ 60 && 155 ≤ b ≤ g+25` ——
   先扫全仓色值排除撞色（水蓝 rgba(111,208,232) 与城框 rgba(123,201,111) 均被排除）；
   整屏计数 + 逐格 zone + 负向格归零 + 阈值开关（0 ↔ 554）四重证据。
5. **命名精度**：叫「灵机旗」（质量标记 = 值得专程一访），不叫"有事旗" ——
   有事率 70%，若标记表达"有事"会全员满旗；语义与文案必须对齐。''',
'要用样本量证明，而不是看单例。')

# ---------- 规划 二十三 ----------
patch(r'E:\Deepseekdb\docs\玩法扩展规划.md', '## 二十三·补记：v89.5', '''## 二十三·补记：v89.5 灵机之地（2026-09-17）
> - 灵机 = 事数 × 等级；`DATA.JH_MARK.minScore = 16`（实测 12.8% 野地格）· 青旗悬于等级角标右侧；
> - 点选信息补后缀（「江湖事 ×n · 灵机」/「荒僻」）；调参只改一处阈值；
> - 全程：smoke 2230/0 · e2e 779/0 · audit 全 0 · 探针 13/0 · 真机 7/7（zero page error + 像素采样）。
> - 详见 `docs/设计规范.md` §35 与 `docs/AI工作备忘.md` §37。''',
'与 `docs/AI工作备忘.md` §36。')

# ---------- 需求档案 v89.5 ----------
patch(r'E:\Deepseekdb\需求档案.md', '### v89.5 · 灵机之地', '''### v89.5 · 灵机之地（2026-09-17 · 老板「按建议进行」—— v89.4 收尾建议）

**落地**
- 判定：`GAME.jianghuSpotInfo(x, y)` —— 灵机 = 事数 × 野地等级，`DATA.JH_MARK.minScore = 16`
  （3 事地 Lv6+ / 2 事地 Lv8+）；荒僻 / 非野地 → null；确定性（同格恒同貌）。
- 绘制：青旗悬于等级角标右侧（`drawJhPennant`）；青碧色系（现有调色盘无同族）；
  零三角函数、零仿射（v50 守卫）。
- 点选：`ui.mapPickText` 野地补「· 江湖事 ×n · 灵机」/「· 荒僻」；非野地无后缀。
- 实测密度 12.8% 野地格（真机整屏约 10~20 面）；调参只改 `minScore` 一处。

**验证**：smoke 2230/0（§79 九条）· e2e 779/0（19b 四条）· audit 全 0 · 探针 13/0 ·
真机 7/7（`_v895_shots/` 4 图：整屏 / 近景 / 阈值拉满归零 / 点选文案；像素采样四重证据）。
- 细节：`docs/设计规范.md` §35；`docs/AI工作备忘.md` §37。''',
'`docs/AI工作备忘.md` §36。')

# ---------- 设计文档 v1.7 ----------
patch(r'E:\Deepseekdb\docs\灵气装备与江湖游历设计.md', '## 附 · v1.7 修订', '''## 附 · v1.7 修订（2026-09-17 · 灵机之地）

1. 逐地分布（v1.6）之上新增**可读性层**：「灵机之地」= 事数 × 等级 ≥ 16 —— 大地图在
   等级角标右侧悬青旗（`GAME.jianghuSpotInfo` 唯一出口；`DATA.JH_MARK` 一处调参）。
2. 青旗是**质量标记**（值得专程一访），不是"有事标记"（有事率 70%）；
   点选信息由此补全三级：江湖事 ×n · 灵机 / 荒僻 / 非野地无后缀。
3. 平衡三红线（单套生效 / 75% 基准 / 两线不转化）不变；X1-X10 第二批候选排期不变；
   地标为"愈险愈丰"的等级梯度提供寻路指引 —— 与 v89.4 的收益放大互为表里。''',
'形成梯度。')

print('文档补丁完成')
