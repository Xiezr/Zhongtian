# -*- coding: utf-8 -*-
"""v89.1 文档：设计规范 §31 · 备忘 §33 · 需求档案 v89.1 · 规划补记 · 设计文档 v1.3。探针幂等。"""
import io

def append(path, sec, tag):
    d = io.open(path, encoding='utf-8', newline='').read()
    if tag in d:
        print('SKIP ' + tag)
        return
    nl = '\r\n' if '\r\n' in d[:4000] else '\n'
    if not d.endswith('\n'):
        d += nl
    body = sec.replace('\n', nl)
    d = d + nl + body + nl
    io.open(path, 'w', encoding='utf-8', newline='').write(d)
    print('OK ' + tag)

append(r'E:\Deepseekdb\docs\设计规范.md', '''## 31. v89.1 剧本视觉化（2026-09-16 · 老板「只有文字，能不能再丰富一点」）

**目标**：不动玩法、不改判定、不加资源 —— 把全屏剧本从「纯文字」升级为「有画面」。

**数据层（插入式补丁，原文案零改动）**
- `SCENE_FLOW[act].art`：12 活动各配**幕景水印**（大字 emoji，横幅右侧 10% 透明）。
- `SCENE_FLOW[act].stages[j].s`：36 幕各配**幕题**（2~8 字：军情来报 / 阵前对峙 / 击鼓进兵 …）。

**表现层（零新资源：地形色 / 天气 / 头像 / 主题变量全复用）**
- **幕景横幅** `.sxf-hero`：地形色染色（`TERRAIN[].color` hex→rgba 渐变）+ 幕景水印
  + 活动徽记 + 门类章（征伐/试炼/采撷/修真/访贤/游历）+ 君主头像 + 灵力·精力·体力
  + 天时日号（季节 · 第 N 日 · 天气；story 缺席静默省略）+ 进度点 + 退出钮。
- **行程时间线** `.sxf-timeline`：已走过的**幕题 + 当时抉择**（`picks`），› 分隔。
- **幕题条** `.sxf-stage-tag`：第 X / Y 幕 + 幕题。
- **对白高亮** `ui.sxfQuote`：先转义、再把「……」独立成段（`.sxf-q` 金色）——防注入。
- **倾向徽章** `ui.sxfBadges`：由修正系数直接派生——攻(pow)金 / 获(reward)绿 / 险(wound>1)红 /
  稳(wound<1)蓝 / 缘(luck)琥珀；无修正不出徽章。选前即知取舍。
- **结算卡** `.sxf-result`：光晕徽记（grade 配色：win 金 / lose 红 / escape 灰）+ 战果行（◆ 前缀）
  + **耗用账单**（耗：精力 -X · 体力 -Y　│　余：…；未动身退出无账单）。
- 动效：`.sxf-wrap` 入场 sxfIn · `.sxf-emblem` 弹出 sxfPop。

**守卫踩线记录（三条，均为配额/禁令型）**
1. CSS 裸像素字号 ≤9（原 9 处已满）→ 三个装饰字号走**令牌** `--sxf-art-sz/--sxf-act-sz/--sxf-emb-sz`；
   旧的内联 26px/44px 一并收进 CSS —— ui.js 裸像素 **0 处**。
2. `letter-spacing: 4px` 全站禁令（v24 标题字距收敛）→ 进度点用 5px。
3. 半透明白描边禁 `rgba(255,255,255,…)` → 光晕用 `rgba(var(--gold-rgb),.18)`。

**验证**：smoke §75 六条 · e2e 四条（横幅/时间线/结算卡）+ 浮点断言容差化 · audit 全 0 ·
探针 27/0 · 真机 11/11 + 零页面错误（`_v891_shots/` 8 图，含素绢主题适配图）。''',
    '## 31. v89.1')

append(r'E:\Deepseekdb\docs\AI工作备忘.md', '''### 33. v89.1 · 剧本视觉化（工程视角）

1. **"只有文字"的解法不是加字，是把已有数据画出来**：幕题/水印是新数据（12+36 处），
   但对白高亮、倾向徽章、耗用账单**全部从既有字段派生**（`t` 的「」引号、`e` 的系数、`act` 的能耗）
   —— 零玩法改动、零判定改动，回归面最小。
2. **守卫有两种踩法：明面禁令 vs 配额制**。明面禁令好躲（`letter-spacing: 4px` /
   `rgba(255,255,255,` / `color:#`），配额制难防 —— CSS 裸像素字号上限 9（原有 9 处已满）、
   ui.js 内联上限 4。**新增前先数配额**；优先**令牌化**（`--sxf-*-sz`）而不是吃掉例外额度；
   顺手把 ui.js 既有 2 处内联字号也收进 CSS（→0 处）。
3. **浮点再生值不能严格相等**（本轮抓出的脆弱断言）：`energy` / `stamina` 是**连续再生**的
   （`+ enePerHour × 游戏小时`），两次读取之间会多出小数（实测 85.0666）。
   e2e「首选定扣 15」写成**差值容差**：`Math.abs((前 - 后) - 15) < 3`；
   「未扣费」写成**未减少语义**：`>= 前值 - 0.01`。渲染变重带来的毫秒级计时偏移，
   会暴露所有这类"以前碰巧通过"的断言。
4. **测试要"锁表现"**：smoke 断言幕题数=36（防部分补丁）、徽章逐档映射、markup 类名全有样式；
   e2e 断言 `.sxf-stage-tt === stages[0].s`（数据与渲染一旦脱钩就红）。
5. **字符串拼接的类名扫描要过滤脏 token**：`class="sxf-dot' + (…) + '"` 这种拼接会污染类名采集
   （`[^"]*` 会吞进引号外的碎片）—— 用 `/^sxf-[a-z-]+$/` 白名单过滤（本轮 smoke 第六条踩过）。''',
    '### 33. v89.1')

append(r'E:\Deepseekdb\需求档案.md', '''### v89.1 · 剧本视觉化（2026-09-16 · 老板「只有文字，能不能再丰富一点」）

**落地（不动玩法、零新资源）**
- 幕景横幅：地形染色 + 幕景水印（12 活动）+ 活动徽记 + 门类章 + 君主头像 + 天时日号。
- 36 幕各配**幕题**；行程时间线（幕题 + 抉择）；对白「」高亮；选项**倾向徽章**（攻/获/险/稳/缘）。
- 结算卡：光晕徽记 + 战果行 + **耗用账单**（耗/余 精力体力）；入场/弹出两段动效。
- 数据为**插入式**（原文案一字未改）；ui.js 内联字号清零；三处装饰字号令牌化。

**验证**：smoke 2198/0（§75 六条）· e2e 全绿（v89.1 四条 + 浮点断言容差化）· audit 全 0 ·
探针 27/0 · 真机 11/11 + 零页面错误（`_v891_shots/` 8 图）。
- 细节：`docs/设计规范.md` §31；`docs/AI工作备忘.md` §33。''',
    '### v89.1 · 剧本视觉化')

append(r'E:\Deepseekdb\docs\玩法扩展规划.md', '''## 十九·补记：v89.1 剧本视觉化（2026-09-16）
> - 幕景横幅（地形染色 + 水印）· 36 幕幕题 · 行程时间线 · 对白高亮 · 倾向徽章 ·
>   结算卡（光晕/战果/耗用账单）；入场/弹出两段动效；
> - 零玩法改动、零新资源：对白/徽章/账单一律由既有字段派生（引号 / 修正系数 / 活动能耗）；
> - 守卫：装饰字号令牌化（CSS 裸px 配额已满）· ui.js 内联字号清零 · 规避 letter-spacing:4px 禁令；
> - 全程：smoke 六条全绿 · e2e 全绿（含「浮点再生值不可严格相等」断言容差化）· audit 全 0 ·
>   探针 27/0 · 真机 11/11（zero page error）。
> - 详见 `docs/设计规范.md` §31 与 `docs/AI工作备忘.md` §33。''',
    '## 十九·补记：v89.1')

append(r'E:\Deepseekdb\docs\灵气装备与江湖游历设计.md', '''---

## 附 · v1.3 修订（2026-09-16 · 表现层）

1. 剧本表现升级（v89.1）：幕景横幅 / 幕题 / 行程时间线 / 倾向徽章 / 结算卡 ——
   **不改任何判定与产出**；选择修正系数（pow / reward / wound / luck）与可复现不变式原样。
2. 结算卡显式呈现「耗 / 余」（耗：精力 -X · 体力 -Y　│　余：…）——
   与退出账本同一语义：未动身零消耗（无账单）、已动身所耗不返。
3. 平衡三红线（单套生效 / 75% 基准 / 两线不转化）不变；X1-X10 第二批候选排期不变。''',
    '## 附 · v1.3 修订')

print()
print('全部完成。')
