# -*- coding: utf-8 -*-
"""v89.6 文档补丁 —— 五份文档追加（设计规范 §36 / 备忘 §38 / 规划二十四 / 档案 v89.6 / 设计文档 v1.8）"""
import io

def patch(path, marker, block, tail_expect):
    d = io.open(path, encoding='utf-8', newline='').read()
    if marker in d:
        print('SKIP', path.split('\\')[-1])
        return
    nl = '\r\n' if '\r\n' in d[:4000] else '\n'
    t = d.rstrip('\r\n')
    if not t.endswith(tail_expect):
        raise SystemExit('!! tail 校验失败 %s : %s' % (path, tail_expect[:48]))
    t = t + nl + nl + block.replace('\n', nl) + nl
    io.open(path, 'w', encoding='utf-8', newline='').write(t)
    print('OK', path.split('\\')[-1])

# ---------- 设计规范 §36 ----------
patch(r'E:\Deepseekdb\docs\设计规范.md', '## 36. v89.6', '''## 36. v89.6 奇遇 · 见闻录（2026-09-17 · 老板「没有达到我想要的探索性和趣味性。赛博许愿术，发动！」）

**目标**：野地此前"全图可见、点开即得"——没有隐藏、没有发现、没有收集。
本轮加**探索层**三件套：隐藏点位 → 现形 → 收录。

**① 隐藏点位（`GAME.wonderSites()`）**
- map.seed 的确定性函数（一次生成缓存，50±处/图）；只落在六种野地，不压据点/城池；
- 三带密度：近郊 `0.0016` / 远野 `0.0006` / 绝域 `0.00024` —— **愈远愈奇**（档位随带加权：
  近=逸闻/中=逸闻·奇珍·少量绝景/远=奇珍绝景为主）；
- **全池轮转保底**：点位按带排序后逐位取「类型轮转表」（前 24 位即全部 24 类；余篇模式
  [奇珍·绝景·奇珍·逸闻] 循环，保住「愈远愈奇」）—— 无论 seed，24 类**必**全出现。
  ⚠️ v1「档内轮转」在逸闻点位 <12 时漏类（门禁实测 2/3 红），已改。

**② 现形（双通道）**
- **线索**：走完任一江湖活动，按 `clueP = 0.18`（日盐确定性）闻得一处未现形点位，
  文案含方向（八向、零三角函数）+ 里数 + 地带：「闻得城西南约 560 里 绝域 之地（x,y）……」
- **就近探察**：打开野地弹窗时，方圆二格（切比雪夫）内未现形点位自动现形（「环顾四周」）；
- **真隐藏**：未现形时 UI 零暗示（无灰点、无文案）——这是探索层的地基。

**③ 探奇三段（`wonderCheck / wonderSpend / wonderRoll`，与江湖活动同形状）**
- 进入免费（「未动身离去零消耗」）；**首次选择才扣费**（精 6 / 体 2）并锁点位；
- 无论成败，**此缘即断**（`s.wonders.d` 落账，重复探奇被拒）；
- 奖励三档（区间不重叠）：逸闻 `精华 4-9 + 金 60-150` ／ 奇珍 `+ 材料 2-3` ／
  绝景 `精华 26-44 + 材料 4-6 + 珠宝 1`；选项的 `reward` 放大、`wound` 折体（贪字头上一把刀）；
- 场景机与江湖活动**共用**（`GAME.sceneBegin`）：幕题/时间线/退出账本/专属退出零改动继承，
  分流点只有两处（扣费 / 结算）。

**④ 见闻录（24 条三档：逸闻 ×12 / 奇珍 ×8 / 绝景 ×4）**
- 地图底栏「📜 见闻录」→ 图鉴：已录显名显文、未录「？？？+ 未录」；进度「已录见闻 N / 24」；
- 「待探线索」清单（前 10 条）带「前往」按钮（回地图并居中）。

**⑤ 视觉**：奇缘星 ✦（四芒星 · 青莲紫 `--wonder`，角标**左侧**；青旗在右侧，✦ 是"未知的邀约"、
旗是"已知的价值"）；奇遇场景横幅「奇缘紫」底纹；结算屏线索行（紫框虚线）；
`--wonder / --wonder-rgb` 四主题各配（浅色系加深）。

**验证**：smoke 2241/0（§80 十一条）· e2e 793/0（19c 十四条）· audit 全 0 ·
探针 16/0（紫星数 == 复刻视野计数 三中心精确相等 · 奖励档梯度 逸闻<奇珍<绝景 · 线索概率贴设计）·
真机 12/12（`_v896_shots/` 7 图 + 像素：整屏 49 紫 · 逐格 16 · 负向 0 · 已探 0）。''',
'复原 554）。')

# ---------- 备忘 §38 ----------
patch(r'E:\Deepseekdb\docs\AI工作备忘.md', '### 38. v89.6', '''### 38. v89.6 · 奇遇 · 见闻录（工程视角）

1. **探索层三件套**：隐藏 → 现形 → 收录。隐藏必须**真隐藏**（未现形零暗示，断言正反两面）；
   现形**双通道**（被动线索 + 主动探察）；收录按**类型**计数（同类型点位重复不再计入）。
2. **收集保底 = 全池轮转（分档轮转会漏）**：类型池 M 类、点位 N 个（N>M）时逐点独立随机会有
   ≈20% 类型缺席（coupon collector）；**按档分组轮转也不保险**——某档点位数 < 该档类型数时
   必然漏类（实测：逸闻 12 类、单档点位数常 <12，门禁 2/3 红）。做法：点位按带排序，
   逐位取**全池类型轮转表**（前 M 位覆盖全部类型），后继用「余篇」模式调分布。
3. **场景机共用**：全屏剧本抽核 `sceneBegin(chk, fly, extra)`，分流点收敛为**两处**
   （扣费 / 结算）；奇遇白拿幕题/时间线/退出账本/专属退出全套。
4. **数值梯度保序断言**：三档奖励区间不重叠（4-9 / 12-22 / 26-44）→ 断言"低<中<高"有确定真值。
5. **概率采样纪律**：采样组合**必须独立**——踩坑：伪独立组合（周期 1200 被重复 25 次）
   把 18% 测成 16%，误报概率偏差；改成 `i%500 / (i/500)%80 / i%7` 后贴回设计值。
6. **标点纪律**：断言括号必须与文案**同形**（半角断言测全角文案 = 假红重跑）。''',
'语义与文案必须对齐。')

# ---------- 规划 二十四 ----------
patch(r'E:\Deepseekdb\docs\玩法扩展规划.md', '## 二十四·补记：v89.6', '''## 二十四·补记：v89.6 奇遇 · 见闻录（2026-09-17）
> - 探索层：隐藏点位（seed 确定性 · 三带密度 · 轮转保底 24 类全出现）→ 线索（活动 18%）+
>   就近探察 → 探奇三段（精 6 体 2 · 首选定锁点「缘止于此」）→ 见闻录收录（24 条三档）；
> - 视觉：奇缘星 ✦（青莲紫）悬角标左侧 · 奇遇横幅「奇缘紫」· 结算线索行 · 见闻录图鉴（dock 入口）；
> - 全程：smoke 2241/0 · e2e 793/0 · audit 全 0 · 探针 16/0 · 真机 12/12（zero page error + 像素采样）。
> - 详见 `docs/设计规范.md` §36 与 `docs/AI工作备忘.md` §38。''',
'与 `docs/AI工作备忘.md` §37。')

# ---------- 需求档案 v89.6 ----------
patch(r'E:\Deepseekdb\需求档案.md', '### v89.6 · 奇遇', '''### v89.6 · 奇遇 · 见闻录（2026-09-17 · 老板「没有达到我想要的探索性和趣味性。赛博许愿术，发动！」）

**落地**
- 隐藏点位：`GAME.wonderSites()`（map.seed 确定性 · 50±处 · 三带密度「愈远愈奇」·
  轮转保底 24 类全出现）；「真隐藏」——未现形零暗示。
- 现形双通道：线索（江湖活动结算 `clueP=0.18`，含方向/里数/地带文案）+ 就近探察（开格半径 2）。
- 探奇三段：`wonderCheck/Spend/Roll`（精 6 体 2 · 首选定锁点「此缘即断」· 重复被拒）；
  奖励三档（逸闻/奇珍/绝景，区间不重叠）；场景机与江湖活动共用 `sceneBegin`。
- 见闻录：24 条三档图鉴（已录显名/未录？？？）+ 待探线索「前往」；dock 新增「📜 见闻录」。
- 视觉：奇缘星 ✦（青莲紫 --wonder · 角标左侧）· 奇缘紫横幅 · 结算线索行（紫框虚线）。

**验证**：smoke 2241/0（§80 十一条）· e2e 793/0（19c 十四条）· audit 全 0 · 探针 16/0 ·
真机 12/12（`_v896_shots/` 7 图：地图悬星 / 近景 / 探奇入口 / 奇遇 / 结算 / 见闻录 / 线索；
像素采样：整屏 49 紫 · 逐格 16 · 负向 0 · 已探 0）。
- 细节：`docs/设计规范.md` §36；`docs/AI工作备忘.md` §38。''',
'`docs/AI工作备忘.md` §37。')

# ---------- 设计文档 v1.8 ----------
patch(r'E:\Deepseekdb\docs\灵气装备与江湖游历设计.md', '## 附 · v1.8 修订', '''## 附 · v1.8 修订（2026-09-17 · 奇遇 · 见闻录）

1. 「江湖游历」之上加**探索层**：24 条奇遇（逸闻/奇珍/绝景三档）藏在全图 50±处**隐藏点位**中——
   线索（活动结算 18%）与就近探察两条现形通道；探奇为君主亲往（精 6 体 2），
   首选定即「此缘即断」，结算入**见闻录**（三档图鉴，可集全）。
2. 奇遇与江湖活动共用全屏剧本机（`sceneBegin`）、共用奖励出口（精华/金/材料/珠宝）、
   共用「退出账本」纪律；点位密度按「近郊 / 远野 / 绝域」三带递减、档位递增 —— **愈远愈奇**。
3. 平衡三红线（单套生效 / 75% 基准 / 两线不转化）不变；奇遇奖励区间与活动产出区间同量级，
   绝景（每图 4 类）为一次性大目标；X1-X10 第二批候选排期不变。''',
'互为表里。')

print('文档补丁完成')
