# -*- coding: utf-8 -*-
"""v77 · 文档补丁：设计规范 §18 / AI工作备忘 §20 / 需求档案 v77。"""
import io, sys

SPEC = r'E:\Deepseekdb\docs\设计规范.md'
MEMO = r'E:\Deepseekdb\docs\AI工作备忘.md'
ARCH = r'E:\Deepseekdb\需求档案.md'


def patch(path, old, new, tag):
    t = io.open(path, encoding='utf-8', newline='').read()
    old_c = old.replace('\n', '\r\n'); new_c = new.replace('\n', '\r\n')
    if old in t:
        if t.count(old) != 1:
            print('  ✗ %s：锚点命中 %d 次' % (tag, t.count(old))); sys.exit(1)
        t = t.replace(old, new, 1)
    elif old_c in t:
        if t.count(old_c) != 1:
            print('  ✗ %s：锚点(CRLF)命中 %d 次' % (tag, t.count(old_c))); sys.exit(1)
        t = t.replace(old_c, new_c, 1)
    elif (new in t) or (new_c in t):
        print('  · %s：已改过（跳过）' % tag); return
    else:
        print('  ✗ %s：锚点不匹配' % tag); sys.exit(1)
    io.open(path, 'w', encoding='utf-8', newline='').write(t)
    print('  ✓ %s' % tag)


# =====================================================================
# ① 设计规范：追加 §18
# =====================================================================
patch(SPEC,
"""- 历史：v28 一屏 12 席 → v29 每页 6 席 → v45 取消分页整段滚动 → **v76 回归 12 席/页**。
""",
"""- 历史：v28 一屏 12 席 → v29 每页 6 席 → v45 取消分页整段滚动 → **v76 回归 12 席/页**。

## 18. 经济与养成（v77）

### 18.1 将领月俸（每 7 游戏日结算一期）
- 定价 =（底俸 120 + 等级×150 + 四维和×1.2）× 资质系数（凡 1 / 良 1.6 / 英 2.6 / 名 4.2 / 天 7）；
  唯一出口 `GAME.genSalaryOf`（展示与结算同源）。
- 结算锚点 = 游戏内时间（`world.elapsed`）；离线跨期一次结清（上限 30 期）；
  从**各将所在城**的府库扣；缺金只记欠俸讯息（**不损忠诚** —— v14.1 拍板）。
- 界面出口：客栈「月俸」列（悬停注明周期）、君主面板「月俸支出」行。
- 意图：将领越多、越强，负担越重；一两名将不痛，规模化的将领团才成为经济议题。

### 18.2 百炼强化（铁匠铺）
- 强化按**装备种**记等级（`s.forgeEnh`，+10 封顶），每级全属性 +8%；
  成本 = 打造基准 × 系数 ×（当前等级 + 1），随等级线性上升。
- 效果在 `genEquipBonus` 的「装备本身」一层乘入（套装加成不参与强化，避免叠上叠）。
- 入口恒在铁匠铺（底栏「⚒ 百炼强化」）；装备详情页有「前往强化」跳转。

### 18.3 内功（秘籍）
- 每将同时一门、10 重封顶；每重 +4 对应属性：
  庙算（智谋）/ 将略（统率）/ 养生（内政）/ 剑心（勇武）。
- 加成在 `GAME.genAttrs` 的 buff 段之后、派生之前并入（全军加成吃得到内功）。
- 档案显示一行「内功 · 功法 N 重」；悬停给出特性名与修习规则。换书 = 转修（旧功散去）。

### 18.4 附属野地的入口（下拉框破例 2/2）
- 资源区「附属野地」下拉框 = 野地清单（地形 Lv（坐标））+「进入」= 野地界面；
  所选行在野地弹窗内高亮。
- 全站 `<select>` 仅两处：城池切换 + 附属野地 —— 破例上账（smoke §35 点名守卫）。

### 18.5 三按钮退役（v77）
- 城池属性右侧的「建筑信息 / 资源生产 / 附属野地」三个图标按钮与各自的弹窗、
  计算链一并退役；开工率调整 UI 下线（机制与取值保留在 `state.workRate`，默认 100%）。
- 附属野地的界面（`ui.openWilds`）保留 —— 改由 18.4 的下拉框进入。
""",
'D1 设计规范 §18')

# =====================================================================
# ② AI工作备忘：追加 §20
# =====================================================================
patch(MEMO,
"""### 19.4 逐级拆除的账
- 返还口径 = **本步投入**的 50%（`investedIn(L) − investedIn(L−1)`），逐级拆到 Lv1 再拆才移除。
- 用"累计差"而不是新造一份 levelCost 口径：一份账目、两处消费（返还预览与执行），
  `investedIn` 也因这条继续保活（不再成为孤儿函数）。""",
"""### 19.4 逐级拆除的账
- 返还口径 = **本步投入**的 50%（`investedIn(L) − investedIn(L−1)`），逐级拆到 Lv1 再拆才移除。
- 用"累计差"而不是新造一份 levelCost 口径：一份账目、两处消费（返还预览与执行），
  `investedIn` 也因这条继续保活（不再成为孤儿函数）。

## 二十、v77：月俸 / 强化 / 内功 / 宝箱 · 结构要点

### 20.1 月俸为什么不是"取消俸禄"而是"改月结"
- 旧账：逐秒按 `等级×20金/h` 扣 —— 玩家永远看不清"将领在花我多少钱"，
  且 v73 把税收闸到 30/h 后，级别稍高就长期净流出（开局必亏，体验差）。
- 新账：`DATA.GEN_SALARY` 定价 + **每 7 游戏日**一期 + 结算进讯息（`GAME.settleGenSalary`）；
  展示与结算唯一出口 `GAME.genSalaryOf`（客栈月俸列 / 君主面板月俸支出都读它）。
- 离线防护：跨期一次结清但上限 `maxPeriods: 30`；**老档锚点 = 载入时刻**（不追溯补扣）。
- 度量参考（默认 120×，1 游戏日 = 12 现实分钟）：良材 Lv1 ≈ 490 金/期（≈3 金/游戏时）。

### 20.2 强化记在"装备种"上（本项目装备是量产件模型）
- 背包/穿戴只存 id、不存实例 → 强化按 id 记（`s.forgeEnh`）：同种共享、新打造的继承。
  若日后要做"每把刀各自的强化"，先改装备实例化，再谈逐件强化（当前不引这个复杂度）。
- 效果乘在 `genEquipBonus` 的"装备本身"层（套装加成不参与），唯一出口。

### 20.3 内功加成的接入点
- `g.ng = {id, lv}` 每将一门；加成在 `genAttrs` 的 buff 段之后、派生（atkVal/pct）之前并入 ——
  放在派生**之后**会出现"内功对全军加成不生效"的隐性断链（本项目老病）。
- 换书 = 转修（`S._neigongUse` 一处判定）。

### 20.4 "下拉框破例"要上账
- v22 曾全站去下拉框；v45 城池切换破例一次；v77 附属野地第二次破例。
  判据从"≤1"改成"**=2 且两者实名在册**"（smoke §35）——破例靠登记，不靠自觉。

### 20.5 反复踩到的两条老坑（本轮各犯一次，供后来者）
- **大段替换**：标题型函数（如 `ui.openLordInfo`）替换后"起点标记仍在"，
  幂等守卫不能只看起点 —— 要加"完成标记"（如 `lord-split`）。已固化进
  `replace_span(..., done_marker=...)`。
- **同权重后置覆盖**（v75 旧账复现于应用层）：`#wild-pick-host` 必须独立于
  `#res-bar` 的每秒重绘 —— 与 `#city-switch-host` 同一套路（否则下拉展开每秒被合上）。""",
'D2 备忘 §20')

# =====================================================================
# ③ 需求档案：追加 v77
# =====================================================================
patch(ARCH,
"""- 测试：smoke 2047/0 · e2e 696/0。
- 细节：`docs/设计规范.md` §11.2 / §11.3（更新）、§17（新增）；`docs/AI工作备忘.md` §19。""",
"""- 测试：smoke 2047/0 · e2e 696/0。
- 细节：`docs/设计规范.md` §11.2 / §11.3（更新）、§17（新增）；`docs/AI工作备忘.md` §19。

---

### v77 · 7 项（**原文**；老板编号里无第 5 条）

> 1.将领招募字太密了，整成列表，表头比如将领，等级，资质，专长，统率……俸禄。
> 2.为将领设计俸禄体系，根据属性值？等级？或其他因素，设计维持将领所需月俸，经过7个游戏日结算1次（注意不要搞崩经济，但是将领越多，经济负担越重）
> 3.左侧统计界面，资源这里的备注：本城 新城池改成附属野地 下拉框（显示当前可选野地及坐标，以及进入按钮，进入后可以进入野地界面，进行相应派遣和采集操作等）。
> 4.城池属性右边的3个按钮以及相应的计算链就不要了（建筑信息，资源生产，附属野地）
> 6.君主界面分左右两半，左边为城池列表（加一个进入按钮，点击后跳转该城的城内界面），右边为两列的信息表，列出君主姓名（给一个改名按钮），爵位（给一个晋升按钮，鼠标悬停显示下一级爵位晋升条件），声望，人口总和，将领总和，状态。
> 7.丰富商场道具（符合时代背景和游戏背景），包括不限于各级宝箱，将领技能学习书（设计将领内功修炼体系，可增加将领特性（根据功法和等级），徭役令（增加同时建筑的队列，持续24h）
> 8.铁匠铺加入装备强化系统，装备可进行强化

**落地**：
- ① 客栈候选改**表格**：表头 = 将领 / 等级 / 资质 / 专长（均衡·猛将·智将·能臣）/ 统率 / 内政 /
  勇武 / 智谋 / **月俸** / 招募；一行一人（v75「单行」原则保持，成长备注仍在资质悬停里）。
- ② **月俸体系**：`DATA.GEN_SALARY`（底俸 120 + 等级×150 + 四维和×1.2，再乘资质系数 1~7）；
  **每 7 游戏日**结算一期（锚点 world.elapsed；离线跨期一次结清、上限 30 期）；
  从各将所在城扣；缺金记欠俸（不损忠诚）。将领越多/越强 → 负担越重。
- ③ 资源区「本城 · 城名」表头退役 → **附属野地下拉框**（地形 Lv（坐标））+「进入」→ 野地界面
  （加成 / 采集队 / 定位）；所选行在弹窗内高亮。
- ④ 城池属性右三按钮（建筑信息 / 资源生产 / 附属野地）整条退役：`openBldgInfo`、
  `openProdInfo`、开工率调整 UI（`after='workrate'`）、`doJumpCell` / `doSetWorkRate` 同步下线；
  `ui.openWilds` 保留（改由 ③ 进入）。
- ⑥ 君主面板改**左右分栏**：左 = 城池列表（每城「进入」直跳城内界面）；右 = 信息表
  （姓名+改名 / 爵位+晋升（悬停 = 声望·城池·黄金·珠宝条件与现状）/ 声望 / 人口总和 /
  将领总和 / 月俸支出 / 君主领兵 / 状态）。`rankBlock` 退役。
- ⑦ 商场新货：青铜 / 白银 / 鎏金**宝箱**（随机资源·黄金·珠宝·材料·图纸·徭役令）、
  四部**内功秘籍**（孙子兵法 / 太公六韬 / 五禽戏 / 越女剑经 —— 10 重、每重 +4 属性、
  特性：庙算 / 将略 / 养生 / 剑心；每将一门，换书＝转修）、**徭役令**（24h 建造队列 +3）。
- ⑧ **百炼强化**：`DATA.ENHANCE`（+10 封顶，每级全属性 +8%，成本随等级线性上升）；
  等级按**装备种**记（同种共享、新造的继承）；入口 = 铁匠铺底栏「⚒ 百炼强化」+ 装备详情跳转。
- 测试：smoke ——/—— · e2e ——/——（第 62 节新增 15 条 + e2e 新增 6 条）。
- 细节：`docs/设计规范.md` §18（新增）；`docs/AI工作备忘.md` §20。""",
'D3 需求档案 v77')

print('\n文档层 v77 补丁执行完毕。')
