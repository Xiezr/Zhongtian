# -*- coding: utf-8 -*-
"""把老板第二轮玩法清单（9 项）并入 docs/玩法扩展规划.md。

做两件事：
  ① 在「八、优先级建议」表后插一行指引（指向文末的合并路线图）
  ② 文件末尾追加第二轮章节（十一 ~ 十五）

规矩（照项目既有）：
  · 幂等：重复执行不产生变化
  · 锚点必须唯一命中，否则拒绝写盘（不留半截状态）
  · 行尾保持：追加用二进制写，全部 LF；改动区先读后写、核验行尾未变
"""
import io
import os
import sys

ROOT = r'E:\Deepseekdb'
DOC = os.path.join(ROOT, 'docs', '玩法扩展规划.md')

MARK1 = '第八节指引'
MARK2 = '## 十一、第二轮清单盘点'

GUIDE_OLD = ('| **第 6 期** | **§七 野地军略化 / 势力动态版** | 两项都是大改，需要单独评估几何耦合与 AI 平衡 |\n')
GUIDE_NEW = (GUIDE_OLD +
             '\n> ⚠️ **2026-09-14 晚 · 已扩展**：老板给出第二轮清单（9 项）。'
             '逐项实测后与第一轮合并，**完整路线图见文末 §十三** —— 本表前六期之外新增 §八~§十六。\n')

NEW_CHAPTERS = '''## 十一、第二轮清单盘点（2026-09-14 晚）

> 老板第二轮给出 9 项方向。**逐项实测现有地基后**：2 项已在第一轮覆盖（将领培养 → §一、野地/江湖历险 → §二），
> 1 项（装备来源）**地基比预想厚得多**，6 项是真新增。
> 本章只规划不动代码；每条给：实测现状 / 缺口 / 设计 / 唯一出口 / 可验证判据 / 工作量级。

### 11.0 老板清单 → §号对照

| 老板原话 | 落点 | 判定 |
|---|---|---|
| 花园种植 / 捕鱼 / 庄园界面 | **§八 庄园** | 🆕 新建 |
| 主线剧情打怪（将领组团 / 卡牌战斗） | **§九 主线 + 卡牌战斗** | 🆕 新建（本轮最大） |
| 将领能力倾向（物理/魔法/辅助/回复） | **§十 将领技能** | 🆕 新建 |
| 将领技能生成 | **§十 将领技能** | 🆕 新建 |
| 将领培养 | 并入第一轮 **§一 武学**（武学=被动线，技能=主动线） | 🔗 合并 |
| 钓鱼功能 | **§八 庄园**（子项） | 🆕 新建 |
| 神兽（桌面宠物） | **§十一 神兽** | 🆕 新建 |
| 丰富装备来源 | **§十二 装备来源** | 🔧 扩展（地基已厚） |
| 主角个人冒险 / 城池探索 / 野地江湖历险 | 并入第一轮 **§二 游历** + **§十六 主角** | 🔗 合并 |
| 时装装扮界面 | **§十三 时装装扮** | 🆕 半新建（头像已有三层） |
| 节日比赛 / 节日专属活动 | **§十四 节日系统** | 🆕 新建 |
| 贸易（限制税收金币 / 名城贸易） | **§十五 贸易收口** | 🔧 改造（平衡类，数据先行） |

---

### 11.1 §八 庄园：种植 / 养殖 / 钓鱼（新增）

**实测现状**
- 城外 4 建筑（农田/伐木/采石/铁矿）是**大宗资源自动产出**（农田 Lv12 = 7800/h，表内还有更高档位），
  与"庄园玩法"不同层 —— 庄园不能和它们抢粮。
- `GATHER` 采集系统：basePerHour 2 / levelBonus 0.35 / maxActive 3 / 体力 6，且**带宝概率 0.02 + 0.001×将级** ——
  "耗时产出去"的模型已经存在。
- 湖泽地形（lake）已有；马厩（majiu）建筑 + 驯马科技 + 坐骑装备 —— 养殖的现成咬合点。
- **缺**：种子→生长→收成的时间玩法、钓鱼、养殖 —— 全无。

**设计**（一个界面 = 三个子玩法，共享时间轮）

```
DATA.MANOR = {
  plots: 6,                                        // 地块数（庄园等级解锁）
  crops:     [ { id, name, hours, seed:{}, yield:{}, rare:{ id, chance } } ],
  livestocks:[ { id, name, hours, feed:{},  yield:{} } ],
  fishing:   { rods: [], fish: [ { id, weight, price } ], treasure: 0.02 },
}
```

- ⛔ **不得成为第二出口**（本项目最经典失效模式）：庄园**不产出** grain/wood/stone/iron 大宗
  （那是城外 4 建筑的地盘）；产出走**新品类** —— 接入现有 `MATERIALS` 24 材料体系（MAT_SERIES 6 组）。
- **用途闭环**（否则就是死系统）：作物/渔获 → ① 将领技能书材料（§十）② 犒军/宴会（民心·忠诚）
  ③ 贸易变现（§十五）④ 炼丹（临时 buff）。
- 唯一出口：`GAME.manorOf()` / `manorPlant()` / `manorHarvest()` / `fishCast()`。
- 时间轮：在线/离线**共用 step**（照 `starveStep` / `invasionTick` 先例，⛔ 不许各写一套）。
- UI：新 tab 或并入 ext 面板；卡片复用 `.troop-card` 样式系（已有 selected/disabled 态）。
- **判据**：① 到点必熟（时间轮推进）；② 离线回来结算不多不少；③ 庄园产出**不进**
  `prodBasePerHourOf`（防第二出口，加审计断言）；④ 地块状态入档（运行中会变），生长速率表派生不入档。
- **工作量级**：中。

### 11.2 §九 主线剧情 + 卡牌战斗（新增 · 本轮最大一件）

**实测现状**
- 任务系统是"指标→奖励"（50 成长 + 36 随机/日 + 7 类型 + 35 指标），**metric 引擎成熟**；
  但**没有剧情 / 章节 / 首领概念**。
- 战斗全是"军队对军队"：`tactic.js` 回合制距离轴（野地 2000 / 野外城 3000 / 城池 4000），按速度行动。
- 卡片 UI 传统已有：`.story-card` / `.troop-card` / `.inn-card`。
- 主角：`INITIAL_GENERAL = '赵子龙'`。

**设计**（章节数据 + 卡牌战斗模式）

```
DATA.STORY_CH = [ { id:'ch1', name:'…', nodes:[
    { type:'story',  text, choices:[ { text, effect } ] },
    { type:'battle', monsters:[…] },
    { type:'reward', … } ] } ]
DATA.SKILLS   = [ … ]     // ⚠️ 与 §十 共享同一个技能库（单一出口）
DATA.MONSTERS = [ … ]     // 怪物 = 复用将领结构（四维 + 技能）
```

- **卡牌战斗**：3~5 将领组队（不带兵）；每回合抽手牌（将领技能 = 卡）→ 消耗"将令"出牌 → 结算。
  - ⛔ **不做第二出口**：伤害基数走 `GAME.genAttrs`（现有唯一属性出口），口径与 tactic 相同
    （攻击 ÷ 生命）；卡牌层只加"行动经济"（手牌/将令/回合），不另造一套伤害公式。
- 与出征的边界：主线战斗**不损兵**（小队制），产出走剧情奖励（新装备 / 技能书 / 神兽蛋）。
- **判据**：① 节点顺序推进不可跳；② 分支由胜负驱动（可翻转测试）；③ 卡池与 §十 技能库同源（审计断言）。
- **工作量级**：**大**。分期：先"线性 3 章 + 简化卡牌"验证乐趣，再扩章节。

### 11.3 §十 将领技能：能力倾向 / 生成 / 培养（新增）

**实测现状**
- 将领 = 四维（统 tong / 内 nz / 勇 yw / 智 zm）+ 5 档资质 + 4 将格（均衡/猛将/智将/能臣）+ 装备 + 等级。
- **无技能层** —— 全项目"技能"一词只出现在科技描述里。
- `genAttrs` 是唯一属性汇总出口（已登记于工作备忘 §十一 清单）。

**设计**
- **能力倾向**（老板点名四类）：`phys 物理 / magic 魔法 / support 辅助 / heal 回复`。
  生成时按将格映射默认倾向（猛将→物理、智将→魔法、能臣→辅助…）+ 资质加权。
- **技能生成**：`GAME.rollSkills(gen)` —— 倾向权重 + 资质决定数量与品质；**种子化**
  （同一将领稳定，读档不重 roll；照 `portraitSeed` 先例）。
- **培养两条线不打架**：武学（§一）= **被动属性线**；技能（§十）= **主动行动线**；
  技能升级用"技能书"道具（可由 §八 庄园材料合成）。
- 唯一出口：`GAME.skillsOf(gen)` / `GAME.skillPower(sk, lv)`。
- 消费点：卡牌战斗（主）+ 常规战斗（次要，走 `atkMult` 现有链）。
- **判据**：① 生成分布符合权重（1000 次统计）；② 技能数值只从 `skillPower` 出（审计断言）；
  ③ 技能书可升技能（可翻转）。
- **工作量级**：中。**与 §九 强耦合 —— 先做本项，卡牌才有"牌"**。

### 11.4 §十一 神兽 / 桌面宠物（新增）

**实测现状**：只有"坐骑"（mount 装备槽 + 马厩 + 马鞭道具）。**无独立养成单位**。

**设计**

```
DATA.BEASTS = [ { id, name, icon, rarity, grow:{}, aura:{ prodPct:0.02 }, likes:[…] } ]
```

- 获得：奇遇 / 主线奖励 / 节日限定 / 马厩孵化。
- 养成：亲密度（互动）+ 等级（喂食）；提供**全局光环**（+1~3% 量级，不破坏平衡）。
- "桌面宠物"形态：主界面角落常驻（可关），点击互动，状态表情（emoji / SVG —— 项目已有程序化 SVG 传统）。
- 唯一出口：`GAME.beastOf()` / `beastFeed()` / `beastAura(key)`。
- **判据**：① aura 有消费点（audit）；② 亲密度/数量上限；③ 入档。
- **工作量级**：中偏小（调味性质，建议排在 §九 之后）。

### 11.5 §十二 装备来源：从"有"到"看得见"（扩展）

**实测：地基比预想厚 —— 已有 6 条链路**

| 链路 | 实现 |
|---|---|
| 打造 | `FORGE` + `BLUEPRINTS`(3 图纸) + `CRAFT_SLOTS`(12 槽 × 4 阶：铁刀→神兵) |
| 野地 | `WILD_MATERIAL`（7 地形 × 材料）+ 采集带宝（2% + 0.1%/将级） |
| 城战战利品 | `LOOT_EQUIP` 五档：fort 图纸 4%/装备件 10% → capital 80%/90% |
| 市场 | `marketTrade` + 商铺 |
| 熔炼 | `salvageEquip` |
| 任务 | `QUESTS` 奖励 |

**真缺口**：① **来源不可见** —— 87 件装备没有"从哪来"的标注（图鉴感缺失）；
② 新玩法带来的新来源尚未接（主线 / 钓鱼 / 庄园 / 神兽 / 节日）。

**设计**
- **装备图鉴 + 来源标注**：每件装备标"产出地"（打造 / 掉落表 / 活动）—— 数据补 `src` 或由现有表派生。
- 新来源接线清单：主线 boss 掉落表 / 钓鱼稀有物 / 庄园作物→材料→打造 / 节日限定图纸。
- **工作量级**：小（图鉴 = 读现有数据 + 一栏 UI）。**可随时插做**。

### 11.6 §十三 时装装扮（半新建）

**实测现状**：头像已有**三层体系**（`portraits.js`：pool 40 张 / 名将 28 / 程序化 SVG），
且程序立绘已按"四维→甲胄主色、资质→冠帽"从脸上读信息；emoji 头像（`AVATARS` 2 组）用于快速位。
**无装扮栏位**。

**设计**

```
DATA.FASHION = [ { id, name, slot:'head|body|waist|foot|bg', src, effect:{}, text } ]
```

- 栏位 5 个；效果**以展示为主**（换装改变外观），可带极轻加成（声望/魅力）。
- ⛔ 与 `EQUIP_SLOTS`（12 装备槽）**互不干扰** —— 装扮不是第二套装备。
- ⚠️ 美术资产：走已有 AI 图集管线（`assets/icons` 即此产出）；**风格与需求清单需老板先定**（见 §十四 拍板项）。
- **判据**：① 换装持久化（入档）；② 不侵入装备数值链（审计断言）。
- **工作量级**：中（成本主要在美术）。

### 11.7 §十四 节日系统（新增）

**实测现状**：**全无节日**。但历法基建齐（`CALENDAR` / `SEASONS` / `ERAS` + `story.js` 历法层每天在跑）。

**设计**

```
DATA.FESTIVALS = [ { id, name, cal:{}, days, activities:[
    { type:'contest', … }, { type:'quests', … }, { type:'shop', … } ] } ]
```

- 触发：`story.js` 历法层（**游戏历法**，非现实日期）—— 每年循环。
- 比赛：**直接复用 metric 引擎**（限时指标 + 奖励）；专属任务同样走 metric；限定商品接 §十五。
- **判据**：① 跨节日推进必触发（时间轮测试）；② 奖励只从表出；③ 节日加成有消费点。
- **工作量级**：中。

### 11.8 §十五 贸易与经济收口（改造 · **平衡类，数据先行**）

**实测现状（金币产出 5 条链）**

| 链 | 实现 |
|---|---|
| 税收 | `taxGold = popCap × (hearts/100) × tax × (1+taxPct)` —— **随人口线性、无上限** |
| 俸禄 | `RANK[爵位].salary`（府库层面） |
| 岁贡 | `cityDailyYield` 每日汇总 |
| 征调民力 | `LEVY_RES_RATE.gold = 0.06`（每日） |
| 其他 | 市场 / 战斗掠夺 / 任务 |

**设计**
- **税收收口**（老板点名）：给税收金币加**边际递减或上限**，或把"金币主力来源"转向**贸易变现**
  （卖资源/材料）—— 让经济有经营感。
- **贸易扩展**（老板点名）：① **名城通商**（`CITY_OPTS` 加一项，按 `STATE_SPECIALTY`
  13 州特产差异定价 —— 把 13 州特产真正用起来）；② **野地城池作为商路节点**
  （`TRANSPORT` 已有运输损耗模型，接上收益侧）。
- ⚠️ **必须量化**：先出"现状金币收入构成"审计（照 `ladder_audit.js` 先例做 `economy_audit.js`），
  改前改后对照，**不拍脑袋**。
- **工作量级**：中。

### 11.9 §十六 主角冒险（并入 §二 游历，补设计）

老板这轮"主角的个人冒险 / 城池探索 / 野地江湖历险" = 第一轮 §二「游历」同一族，**合并**（不做两个入口）：

- **主角**：开局将领（`INITIAL_GENERAL = 赵子龙`）或玩家自建 —— 与 §十三 装扮咬合（装扮的对象就是主角）。
- **城池探索**：进入 NPC 城的"事件层"（非战争）—— 市井 / 酒馆 / 官府概率事件，产出去见闻 / 线索 / 小交易。
- **历险**：与游历共用时间轮与事件引擎（复用 `ENCOUNTERS` 结构：weight + text + reward）。
- **工作量级**：中（合并后不重复计）。

---

## 十二、必要性分级（回答"拓展的必要性"）

| 级 | 判据 | 项 | 不做的代价 |
|---|---|---|---|
| **A 核心缺失** | 影响"有没有可玩的目标" | §九 主线 + 卡牌 · §十 将领技能 | 有任务但没**故事**；有将领但没**成长选择** —— 这是"能玩多久"的天花板 |
| **B 强改善** | 影响"每天上线干什么" | §八 庄园 · §二扩 主角冒险 · §十五 贸易收口 | 中后期只剩"攒兵打城"单循环 |
| **C 调味** | 情绪 / 搜集价值 | §十一 神兽 · §十四 节日 · §十二 装备来源可见 | 不影响可玩性，影响"留得住的感受" |
| **D 展示层** | 需美术资产 | §十三 时装装扮 | 最贵且依赖美术，放最后 |

---

## 十三、合并路线图（第一轮 + 第二轮 · 2026-09-14 修订）

| 期 | 内容 | 为什么这个顺序 | 状态 |
|---|---|---|---|
| 1 | 空承诺接线 | — | ✅ 实测后判定无需做 |
| 2 | §五 防守（被攻打） | 修好空转的城墙/箭塔 | ✅ 已落地 |
| **3** | **§十 将领技能** | **§九 的前置**（先有技能库，卡牌才有"牌"） | ➡ 建议下一期 |
| **4** | **§九 主线 + 卡牌战斗** | 老板清单核心件；先"3 章 + 简化卡牌"验证 | ➡ |
| **5** | **§八 庄园** | 独立大件，"每天有事干" | ➡ |
| 6 | §十五 贸易收口 | 平衡类，须先做 `economy_audit.js` | ➡ |
| 7 | §三 势力层（静态） | 第一轮保留项 | ➡ |
| 8 | §二 游历 + §十六 主角冒险 | 合并做，与 §十三 装扮咬合 | ➡ |
| 9 | §十一 神兽 + §十四 节日 | 调味，且都依赖前面系统的产出 | ➡ |
| 10 | §十三 时装装扮 | 需美术先行 | ➡ |
| 插 | §十二 装备来源可见（小） | 可随时插做 | ➡ |
| 后 | §七 野地军略化 / 势力动态版 | 大改，单独评估 | ⏸ |

---

## 十四、新增拍板项（第二轮）

| # | 事项 | 影响 |
|---|---|---|
| 6 | **主角是谁？** 开局将领（赵子龙）or 玩家自建角色（命名 + 捏脸，与装扮咬合） | 决定 §十六 / §十三 的设计 |
| 7 | **卡牌战斗规模**：独立小队战（3~5 将）是否够？要不要"带兵卡"（部队也成卡） | 决定 §九 体量 |
| 8 | **节日取真实节气（春节/端午/中秋/重阳）还是纯游戏历法（自造节日）** | 决定 §十四 内容 |
| 9 | **税收金币是否要压**：现在金币主要靠人口增长，压了改变节奏 —— 须先看 economy 实测数据 | 平衡红线 |
| 10 | **神兽排期**：调味件（情绪价值高、玩法价值低），放后面还是提前 | 排期 |
| 11 | **装扮美术**：用 AI 图集自产（项目已有管线）还是暂不做 | §十三 前置 |

---

## 十五、动手前的补充约定（第二轮新增三条）

7. **新系统必须先定"与旧系统的边界"**（防第二出口）：庄园 vs 城外建筑、技能 vs 武学、
   装扮 vs 装备、主线战斗 vs 出征 —— 每对都要写清"谁管什么"。
8. **平衡类改动一律数据先行**：经济/数值先出审计脚本（`economy_audit.js`），改前改后对照。
9. **卡片 UI 复用 `.story-card` / `.troop-card`**，不新造视觉语言。
'''


def read_text(p):
    with io.open(p, 'rb') as f:
        b = f.read()
    return b


def main():
    b = read_text(DOC)
    txt = b.decode('utf-8')
    crlf_before = b.count(b'\r\n')

    changed = []

    # ---- ① 幂等检查：已写过则整体跳过 ----
    if MARK2 in txt:
        print('· 第二轮章节已存在，跳过全部修改（幂等）')
    else:
        # ---- ② 插指引（锚点必须唯一）----
        n1 = txt.count(GUIDE_OLD)
        if n1 != 1:
            print('✗ 指引锚点命中 %d 次（应为 1），拒绝写盘' % n1)
            sys.exit(1)
        txt = txt.replace(GUIDE_OLD, GUIDE_NEW, 1)
        changed.append('指引行')

        # ---- ③ 追加新章节 ----
        if not txt.endswith('\n'):
            txt += '\n'
        txt += '\n' + NEW_CHAPTERS
        changed.append('新增章节 十一~十五')

    if not changed:
        # 幂等路径：仍做一次落盘核验
        print('落盘核验：文件 %d 字节，CRLF=%d（应保持 0）' % (len(b), crlf_before))
        return

    # ---- ④ 写盘（核验行尾未变）----
    out = txt.encode('utf-8')
    if out.count(b'\r\n') != crlf_before:
        print('✗ 行尾被改写（CRLF %d → %d），拒绝写盘' % (crlf_before, out.count(b'\r\n')))
        sys.exit(1)
    with io.open(DOC, 'wb') as f:
        f.write(out)

    # ---- ⑤ 落盘核验 ----
    chk = read_text(DOC)
    ctxt = chk.decode('utf-8')
    ok1 = MARK2 in ctxt
    ok2 = chk.count(b'\r\n') == crlf_before
    ok3 = '§十六 主角冒险' in ctxt
    print('本次改动: %s' % ' / '.join(changed))
    print('落盘核验: 新章节=%s  行尾未变=%s  字节 %d → %d（+%d）'
          % (ok1, ok2, len(b), len(chk), len(chk) - len(b)))
    print('行数: %d → %d' % (b.count(b'\n') + 1, chk.count(b'\n') + 1))
    if not (ok1 and ok2 and ok3):
        print('✗ 核验未过')
        sys.exit(1)
    print('✓ 完成')


if __name__ == '__main__':
    main()
