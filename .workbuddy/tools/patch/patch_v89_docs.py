# -*- coding: utf-8 -*-
"""v89 文档：设计规范 §30 · 备忘 §32 · 需求档案 v89 · 规划补记 · 设计文档 v1.2。探针幂等。"""
import io

def append(path, sec, tag):
    d = io.open(path, encoding='utf-8', newline='').read()
    if tag in d:
        print('SKIP ' + tag)
        return
    nl = '\r\n' if '\r\n' in d[:4000] else '\n'
    if not d.endswith('\n'):
        d += nl
    body = sec.replace('\n', nl)
    d = d + nl + body + nl
    io.open(path, 'w', encoding='utf-8', newline='').write(d)
    print('OK ' + tag)

append(r'E:\Deepseekdb\docs\设计规范.md', '''## 30. v89 君主专属修炼 + 全屏江湖剧本（2026-09-16）

**一号修订：修炼线收口君主（老板「只有君主将有修炼功能，以及相应装备」）**
- 依据：功法文档 0.1「主角单修」——修炼收窄到玩家角色一人，将领维持轻量配置对象。
- **唯一闸门 `GAME.canCultivate(g)`（= 君主）**：全链收口点——`S.equipBagOf`（非君主恒军装）·
  `GAME.toggleEquipSet`（拒切）· `S.equipItem`（拒穿修炼件）· `GAME.lingTemper`（拒蕴养 +
  他人在身防御）· `GAME.jianghuCheck`（拒游历）。
- UI 收口：将领面板双轨 tab 只给君主渲染（普通将只有军装一轨 + 「钦定不假他人」说明）·
  装备总览/换装/人偶格按同闸门分流 · 游历区块选将收口（「君主亲往」；君主不在城则整块提示）。
- 老档迁移：`GAME.migrateLordLing`（loadFrom 与装备单件化迁移同点调用，`_lingLord1` 一次性）——
  非君主身上灵气件归还背包、`equipOn` 归位军装；君主不受影响。

**二号新增：全屏江湖剧本（老板「专属全屏交互 + 特定退出」）**
- 数据：`DATA.SCENE_FLOW`——12 活动各 2~3 幕「对话 / 事件」，每幕 1~3 个选择；
  选择只累计**修正系数 mods**（pow 我方差数 / reward 数值产出 / wound 负伤 / luck 小幸运），
  **不改判定公式、不改种子**——同选择必同结果（可复现不变式保持）。
- 引擎：`GAME.jianghuDo` 拆三段 Check / Spend / Roll（`jianghuSpend` / `jianghuRoll`）；
  新增 `sceneStart`（只校验不扣费）/ `scenePick`（**首次选择才扣费+落锁**）/ `sceneEscape`。
  one-shot `jianghuDo` 保留：无剧本兜底与单测共用内核，改判定只改一处。
- 退出机制（按 grade 选屏）：win / partial / lose / escape——讨伐「大捷·战果结算」/「铩羽而归」、
  试炼「功成出关」/「昏迷醒来」、修炼「功法大进」、采集「完成采集」、地宫「夺路而逃」、
  中途「鸣金收兵」等；**未动身退出零消耗；已动身退出所耗不返、今日计入**。
- 层位：#scene-fx 独立根（body 直挂，z=1500：弹窗 1000 之上、toast 2000 之下）——
  不占 modal-root（单根替换系统，互不干扰）。
- 守卫：smoke §73 换角君主 + §74 九条 · e2e v88.1/v88 剧本化 + v89 段八条 · 真机 8 项（`_v89_shots/`）。''',
    '## 30. v89')

append(r'E:\Deepseekdb\docs\AI工作备忘.md', '''### 32. v89 · 君主专属 + 全屏剧本（工程视角）

1. **能力闸门收敛**：新增"只有谁能做"的能力边界时，先找**已有高频出口**（本项目：
   `equipBagOf` / `jianghuCheck`）在出口加一次判断——比在 N 个 UI 入口加 N 次更稳。
   UI 隐藏入口是体验，出口闸门才是正确性。
2. **老档迁移挂在 loadFrom 同点**：`migrateLordLing` 与 `migrateEquipModel` 同点调用，
   一次性 flag + 幂等——老档回访不炸、新档零开销。
3. **全屏层与弹窗单根的关系**：`#scene-fx` 直挂 body、独立 z 层级。
   教训来源：v86 计略弹窗曾因"单根替换"把出征面板顶掉——**新交互层一律不占 modal-root**。
4. **"未动身免费"的实现**：校验（check）与扣费（spend）拆开，流程首幕选择才 spend——
   比"先扣再退"少一整类回滚 bug。
5. **测试换角**：行为收口后，旧断言里 `generals[0]`（普通将）需全部改 `GAME.lordGeneralOf()`，
   并补负向断言（非君主必被拒）。**改行为必须同步改测试的"角色"**，否则红一片。
6. **e2e 尾部状态可能只剩君主**（前序测试换过档）——负向验证不要依赖"现成的普通将领"，
   临时 `makeGeneral` 补一位、用完移出（本次 e2e 中断 1 条的根因）。''',
    '### 32. v89')

append(r'E:\Deepseekdb\需求档案.md', '''### v89 · 君主专属修炼 + 全屏江湖剧本（2026-09-16）

**① 修炼线收口君主**
- 唯一闸门 `GAME.canCultivate`：装备分流/切换/穿戴/蕴养/游历全链；非君主三拒（切换/穿戴/游历）。
- UI：双轨 tab 只给君主 + "钦定不假他人"说明；游历区块「君主亲往」；蕴养面板无君主拒开。
- 老档迁移 `migrateLordLing`：非君主灵气件归包、归位军装。

**② 全屏江湖剧本（12 活动）**
- 讨伐（对话·事件三幕：斥候 → 阵前对话 → 决战）；其余 11 活动同构（2~3 幕 + 选择）。
- 专属退出：大捷结算 / 铩羽而归 / 昏迷醒来 / 功法大进 / 完成采集 / 鸣金收兵（按 grade）。
- 退出账本：未动身零消耗；已动身所耗不返、今日计入。
- 层位 #scene-fx（z=1500，不占 modal-root）；事件 sxf-choice / sxf-escape / sxf-exit。

**验证**：smoke **2192/0**（§73 君主化 + §74 九条）· e2e **767/0**（v88.1/v88 剧本化 + v89 八条）·
audit 全 0 · 真机 8/8（`_v89_shots/`）。
- 细节：`docs/设计规范.md` §30；`docs/AI工作备忘.md` §32。''',
    '### v89 · 君主专属修炼')

append(r'E:\Deepseekdb\docs\玩法扩展规划.md', '''## 十八·补记：v89 君主专属 + 全屏剧本（2026-09-16）
> - 修炼线（装备 / 蕴养 / 12 活动游历）全链收口**君主专属**（主角单修），老档迁移自动归还；
> - 12 活动各配**全屏对话/事件剧本**（2~3 幕 + 选择修正）+ **专属退出结算**
>   （大捷 / 铩羽 / 昏迷醒来 / 功法大进 / 完成采集 / 鸣金收兵 / 夺路而逃）；
> - 退出账本：未动身零消耗，已动身所耗不返；`#scene-fx` 独立层（不占弹窗单根）；
> - 全程：smoke 2192/0 · e2e 767/0 · audit 全 0 · 真机 8/8（zero page error）。
> - 详见 `docs/设计规范.md` §30 与 `docs/AI工作备忘.md` §32。''',
    '## 十八·补记：v89')

append(r'E:\Deepseekdb\docs\灵气装备与江湖游历设计.md', '''---

## 附 · v1.2 修订（2026-09-16 · 落地跟随）

1. **修炼主体收口君主**：原「将领轻量化」进一步收紧——灵气装备 / 蕴养 / 江湖游历
   **仅君主可用**（唯一闸门 `GAME.canCultivate`）；其余将领只走军装一轨。
   老档非君主身上的灵气件自动归还背包。
2. **活动中文化（全屏剧本）**：12 活动全部升级为**全屏对话/事件剧本**（2~3 幕 + 选择），
   选择以修正系数（pow / reward / wound / luck）参与结算——不改判定公式、不改种子；
   专属退出结算：大捷 / 铩羽 / 昏迷醒来 / 功法大进 / 完成采集 / 鸣金收兵 / 夺路而逃。
3. **退出账本**：未动身退出零消耗；已动身退出所耗不返、当日计入（防"预览重开"）。
4. 平衡三红线（单套生效 / 75% 基准 / 两线不转化）不变；X1-X10 第二批候选排期不变。''',
    '## 附 · v1.2 修订')

print()
print('全部完成。')
